# 仅开发环境生效（生产构建不会加载本文件）。
#
# REST 走同源相对路径 /backend（由 app/backend/[...path]/route.ts 在运行时代理，
# 后端地址由服务端环境变量 BACKEND_ORIGIN 决定，换主机无需重新构建）。
#
# WebSocket 不能走同一条路：Route Handler 不代理 WS 升级。
# 所以开发环境显式指向后端直连地址，保留实时推送；
# 生产环境若不加此变量，useQuoteStream 会自动降级为 5s 轮询（功能完整，只是不够实时）。
# 要在生产也用上 WS：前置 nginx 反代后端并放开 Upgrade 头，或直接设置该变量。
NEXT_PUBLIC_WS_BASE=ws://127.0.0.1:8000
