# 当前交接现场

> 定位：只存当前工作区、最新验收与交付入口。任务唯一状态从 [总账 §6.0](retro-and-gaps.md#60-阶段索引) 进入所属阶段，不在这里重复记账。

## 1 现场

- **接手来源（2026-09-18）**：用户明确要求 ChatGPT 接替 Codex Desktop「实施 AShare AI Trader 融合方案」未完工作；原会话 `01a0acfd-221b-7763-9aae-299d78b8738a`。本轮执行者为 **ChatGPT / GPT-6 Astra Pro，经 Remote Desktop Commander**，不是原 Codex 会话自行恢复；原会话文件未修改。
- **工作区与继承点**：`/tmp/ashare-plan-led-backlog`，分支 `codex/plan-led-backlog`，接手 HEAD `84434a4`，基于 `master@d4ebf94`（PR #30）。继承暂存 28 文件及 README/总账的未暂存修改，共 29 个改动文件；原始补丁、文件副本和哈希均已保存。
- **本批范围**：[GOV-019](stages/w08-governance.md#gov-019)。Codex 已完成十阶段、旧号处置和知识库导航主体；ChatGPT 完成独立复核、遗漏校验补强、退出/合并终态、README 纠偏、浏览器/干净检出验收及接续留痕。未混入通知、策略或生产业务逻辑改动。
- **最终后端全量**：3760 collect = **3682 passed + 78 skipped**，0 failed / 0 error，pytest 90.41s；外层进程计时 93.24s。终态增补后的专项 78 passed；相对接手前记录增加 16 项回归，不把跳过项算覆盖增强。
- **前端与静态检查**：本地/UTC 各 **681 passed / 72 文件**，外层 41.79s / 38.49s；tsc、eslint、pyflakes 均通过，Next 生产构建通过。终态增补不改前端源码；最后 pyflakes 已复核。
- **独立验收**：123 个旧号对账无缺失/多出（43 已完成、22 退出、36 合并、22 保留）；102 份真实文档的隔离浏览器导航通过，含任务锚点及返回；1422 跟踪文件的干净检出 doc-health 通过。去掉新增漏号/空交接检查的隔离变异副本，6 个反例均判红。
- **前提与限制**：原仓 8000/3000 在运行，本批未重启它们；本地重检查串行、工作区无 backend/.env，隔离页面不连生产 API。本地结果不是云端 CI、生产加载或策略效果证明；首轮浏览器夹具的样式工作目录/过严滚动断言已纠正，无需修改产品代码。
- **交付状态**：本地验收完成，GOV-019 保留“待交付”；本轮未建立 PR、未触发 CI、未合并 master。当前账单 API 返回 403；Safari 可读页面源码但未取得动态余额，不能把 PR #30 后“60 分钟剩余、预留 40”旧快照当作新实测。不得绕过完整 CI 或启用付费。
- **恢复证据**：本工作区 `artifacts/chatgpt-takeover-20260918-090419/` 保存接手快照、完整日志/回执和测试输入哈希；任务文档由 Git 保存。原仓三个无关未跟踪资产保留，未迁生产库、未发送真实通知。

## 2 接续

先核当前分支 HEAD/差异及本页，不依据旧会话最后消息重建阶段账本。完成发布前的余额核实后，在既有授权内按 AGENTS §6.5 建 PR，验证准确 head/base 与最新 attempt 的全部必需 job，合并后再验 master；工程门禁不能因本地全绿而省略。

之后优先进入 W02 的 [IMP-044](stages/w02-notifications.md#imp-044)，核类型化回执及仍直发的机会卡入口；规则 Outbox 首片已交付，不能重复建设。W08 的 [GOV-018](stages/w08-governance.md#gov-018) 目录退出仍独立开放，不因账本改造完成而一并销账。旧 PR #4 不直接合并。

## 3 给 Codex 的恢复提示

本批由 ChatGPT 接手续作，Codex 恢复时先确认 Git 最新内容再执行。保留前任/接手者、来源任务和各自贡献；不覆盖接手改动、不伪造历史消息、不把“待交付”读成“尚未实现”。需要定位具体任务时，从阶段总账进入任务唯一记录。
