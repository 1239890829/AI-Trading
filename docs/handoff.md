# 当前交接：IMP-046 参数影子评估与生效边界

> 当前方案仍为 [v9.2](implementation-plan.md)，21条原Codex要求对账保留在§8.1；状态唯一归 [W05](stages/w05-agents.md)，长期权责见 [协作规范](collaboration-workflow.md)。

> 2026-09-18后续治理评估：用户提供Codex Bridge，本轮只追加协作规范§12及GOV-022候选说明；未修改下述业务实现。静态源码与无网络DOM夹具核到上下文完整性风险，未安装App/扩展或调用模型。读取协作规范再决定有限试用，不能按“已打通自动闭环”继续。实际当前HEAD以Git为准，业务验证基点仍是d2cd90e。评估证据在原项目 artifacts/runs/codexbridge-assessment-20260918；本轮仅跑文档相关检查，旧业务测试数不是重新运行。

> 用户安装后的接续核验：Codex Bridge/Chrome扩展1.1.1已运行和注册，本项目网页及原Codex任务已被收录，安装后的原生接收器受理合成测试ok=true；不是“尚未用上”。远程UI权限、导入后回查和稳定安装路径仍有未完成项，未填入/发送模型草稿、未跑双向闭环。详见协作规范§12末尾与原项目 artifacts/runs/codexbridge-live-check-20260918-151106；下方业务代码/测试保持原基点，不重测冒报。

## 1. 当前现场与继承

- 用户“继续”授权ChatGPT在Codex无额度期间临时代执行；没有调用Codex、外部模型或真实行情/通知。作者自检不冒充独立审核。
- 工作区 `/tmp/ashare-plan-led-backlog`，**当前分支 `codex/shadow-review-required`**，继承 `codex/code-proposal-only@ceb00ea98c001f720c7b465778fe0f97eb4f8d10`，向前包含BUG-025/027和全部方案治理；父分支保留，未清理无关数据或覆盖前任成果。
- 本轮只处理影子自动转正、通用apply绕过、评估记录与直接消费者。不改策略参数数值、金融规则、数据库schema、生产开关或既有回滚算法；不创建新审批平台。
- 发布仍为本地完整验证后功能分支push，独立审阅与准确版本CI未齐不合主干；不创建PR、不改工作流或启用付费。

## 2. 已实现边界

- 调度器保留原evaluate_and_promote_shadow名称和异步调度方式，实际只评估：漂移温和保留shadow_review_required，危险结构仍可shadow_rejected；不调用promote/apply或挂已生效实验。
- 旧promote_shadow入口明确拒绝；通用apply只保留合法人工草稿和已生效记录的幂等读取。影子、拒绝、未知状态及非人工草稿不得直接生效；evidence内approved等字段、mutation_source=user不当作独立批准。
- 写入前重验存量值，记录任务期间候选内容/身份变化则失败关闭；保留普通手工配置、原基线重定和已有拥有者感知回滚。手工配置不宣称研究效果已证明。
- 评估证据绑定原候选内容，条件更新不能覆盖并发撤回/改值/改证据；基线已漂移则保留shadow_stale，不自动重定后转正。同结论重复评估不重复写入。
- 三个标量评估入口原来因多传market_gains而失败，本轮修正分发参数，不改其研究算法；离线supports、not_applicable均不产生批准。
- A类议程仍保留执行器完成状态以兼容预算，但显式execution_scope=shadow_only、runtime_applied=False；新旧A类都不自动把复盘改进项标applied。摘要单列shadow_queued，界面区分影子未生效、历史待复核与普通人工草稿。

## 3. 证据与验收

在继承代码上新增22项反例均判红；使用隔离数据库和固定输入，没有运行真实模型。真实HTTP消费测试检查422拒绝及合法人工配置正例；测试明确替换认证依赖，只证明应用行为，不冒称认证/不同主体权限隔离已验收。

本批完整门禁、最终数量、输入哈希和干净检出结果见原项目忽略的 `artifacts/runs/imp046-shadow-review-20260918/`。before-files.tar保存修改前文件；red、dispatcher-baseline、focused及UI日志保留反例与失败历史。本轮完整结果：后端3929 collect = 3851 passed + 78 skipped，0失败；新增46项、原测试ID删除0。前端本地/UTC各692 passed/73文件，tsc/ESLint/pyflakes/Next构建和文档检查通过。最终业务源码与冻结输入哈希一致；最后任务/交接回填另做相关复验，不把本地验证当云端CI或上线。

## 4. 未完成边界与成本纪律

本轮是阻止无证据晋级的安全切片，**不是正式晋级审批系统已建成**。独立审批来源、候选/基线/样本/成本/版本绑定、撤销到期及一次性原子消费仍待实施；当前影子候选必须留待审，不添加一个可自填的approved字段重新开放转正。
`source_type=manual`及现有写token属于原有信任边界，不能防持有同等凭据/本机任意写权限者伪造记录；不宣称身份隔离、跨进程全部写路径或长期效果已证明。保留旧人工配置不等于把它认证为研究晋级。
既有实验裁决/回滚、完整usage/token/取消和跨进程预算仍未全面修复；BUG-026样本单位、BUG-022原图、GOV-018目录退出等继续保留。下一行为切片优先BUG-026统计真实性；正式晋级在研究数据和可信批准条件齐备后再派，不为等待审批建设阻塞独立正确性修复。未决影子候选的长期去重/取消和队列上限一并留在IMP-046预算/取消后续范围。字段与DOM测试不等于生产视觉/运行加载验收。
每批继续记安装/测试/构建/返工次数和慢项。本轮对同结论评估用SQL事件检查不重复写入；完整测试在接口和差异检查后冻结输入，最后纯交接回填只做相关复验。未运行云端CI不写通过，也不把局部零写入当作全项目性能提升。
恢复可停止影子评估或保留只读证据，不恢复无批准自动转正；无需数据库迁移，不重写历史报告、已生效参数或生产配置。

## 5. Codex恢复后的接续提示

```text
先核最新HEAD和工作区/tmp/ashare-plan-led-backlog；当前分支为codex/shadow-review-required，父分支ceb00ea及以前成果完整继承。
读取AGENTS、此交接、W05/IMP-046及IMP-025；本轮ChatGPT按用户临时代执行授权修影子评估/生效边界，未调用模型。
评估现在只保存证据或拒绝结构，不能自动promote；通用apply不接受影子/非人工草稿，正常人工配置和历史回滚保留。
先检查真实diff、候选并发改动、HTTP/DOM及全量测试回执，不重复实施或生成作者自批。
正式候选晋级审批仍未实现，不能放一个approved=True绕过；独立批准/证据/原子激活需另派切片，研究证据前置按v9.2执行。
独立审阅和准确版本CI未齐不合并，功能分支保存不是上线。持续节省保留每批成本/失败/复用边界，不删断言求快。
```
