# 当前交接：账本驱动，用户只发完成提示

当前方案仍为 [v9.2](implementation-plan.md)；现行流程见 [协作规范](collaboration-workflow.md)。原Codex的21条要求对账保留在方案§8.1。用户最新选择替代旧全自动目标：网页统筹审核，Codex执行，用户只在两端提示完成，不搬运任务卡或导出文件。

## 1. 固定入口与唯一当前任务

固定协调入口：仓库 `1239890829/AI-Trading`，分支 **`codex/collaboration-runtime-state`** 的本文件。当前施工目录 `/tmp/ashare-plan-led-backlog`，施工分支与入口相同。入口未回写前不自行换分支；不要从master或原项目目录猜现状。
唯一当前任务：[W08/GOV-022](stages/w08-governance.md#gov-022)。本轮派工、执行记录、审核结论和下一步只在该条目维护，本页不复制状态。Codex首次接续按其ledger-readback-01范围做只读核对，不因旧文档要求继续补自动化链。
本批从 `e852b03d687c66e8b14e63d7b4dd21f7ed3ebe19` 接续，此前BUG-025/027、C类纯提案与影子安全成果全部继承；最终HEAD以Git为准。用户说“Codex执行完了”或“ChatGPT审核完了”后，接收方自行核真实版本和记录，再决定审核、整改、开工或停止。

## 2. 本轮实际改动与验证

本轮仅收敛9份既有计划/规则/索引/Skill文档，停止继续建设自动调度、网页唤醒及自动回执消费；未改业务代码，未删除原型/专属测试、卸载Bridge、发送模型请求或改变权限/费用。
已核本机与远端e852b03一致且初始工作区干净；跟踪引用检查只发现原型的专属测试及三份原协作文档，没有发现业务调用。此检查不证明本机所有外部启动项都已审计，因此物理清理另按任务中的消费者核验执行。
本轮文档体检通过、151项相关回归通过；32个任务的编号/状态保持不变，协作规范从235行收敛为85行。业务源码与原型文件未改，不重复跑业务全量。阶段一致性、精确版本复验与回执保存原项目 `artifacts/runs/ledger-manual-handoff-20260918-175518/`。原before-docs.tar保留回退点，最终交付以delivery.json及Git为准。不把历史3895/78或44项原型测试写成此次新测，也不把文档通过写成Codex已实测新流程。

## 3. 如何继续

用户只发短提示；Codex先读AGENTS、本文件、现行协作规则、方案及当前阶段条目，网页审核端也从同一入口定位实际代码/证据。先提交并推送回填，再声称已交付；读不到的必要证据或写入失败须明确说明，不凭聊天批准。
收到重复提示或新会话时，先核当前轮次、执行占用、工作区和最新共享记录；已完成同一轮不重复执行，不清理归属不明的改动。新会话若不在本项目、没有仓库上下文，先建立项目定位一次。
网页审核本轮后还须评估消费者链、遗漏、重复实现、性能/成本与低成本替代，有证据再调整计划和优先级，不机械执行旧队列。业务下一候选BUG-026等以届时事实与单点任务为准，自动化退出不应继续占用业务优先级。

## 4. 保留与退出边界

自动化原型及44项专属测试属于后续成对退出候选，不再修其未实现能力；历史失败、Git提交和运行库保留，不伪称已完成无人值守。Bridge及扩展不再是依赖，本轮没有卸载或删除个人聊天。当前阶段记录、handoff、原始方案和独有证据不能随重复下载稿一起删除。
本地自检、网页独立审核、CI、合并及运行加载继续区分；没有审阅/准确版本CI不合主干。此方式降低转递负担，不绕过平台拒绝、模型额度或Git权限。Codex真正按新规则执行一次、网页再审核的使用验收尚待完成。
