# 当前交接与 Codex 接续指引

> 本页只记录当前现场、执行者、验收范围和恢复入口；任务状态只在 [阶段总账 §6.0](retro-and-gaps.md#60-阶段索引) 索引的阶段页。
> 当前实施修订：[实施校准 v9.1](implementation-plan.md)。原 v9 未被覆盖，本版明确修订之外的原目标与安全约束仍有效。

## 1. 当前现场与接手关系

- 日期：2026-09-18；本轮审查/文档校准由 **ChatGPT / GPT-6 Astra Pro** 完成；下一轮业务代码、测试、集成与交付由 **Codex** 执行。
- 原 Codex 任务：“实施 AShare AI Trader 融合方案”，会话 `01a0acfd-221b-7763-9aae-299d78b8738a`。Codex 已完成阶段账本与导航主体，不能根据其旧会话最后一条消息重建。
- 工作区：`/tmp/ashare-plan-led-backlog`；分支：`codex/plan-led-backlog`。本轮开始基点 `4e7bfcbf5c6994dfcb4529b19dcf9a72ec9368bc`，主干核对为 `d4ebf94`；接手前先读实际最新 Git HEAD 与差异。
- 继承链：Codex 的 `84434a4` 及之后暂存/未暂存成果 → ChatGPT 收尾提交 `4e7bfcb` → 本轮 v9.1 文档校准提交。原会话未修改，原成果归前任，不冒称均由本轮开发。
- **本轮只改方案、索引、阶段任务、交接与技能指引；新发现的业务缺陷尚未修复。** 不修改业务源码、数据库、模型参数、生产开关、原 v9/HTML、账单或仓库权限。
- 本地证据持久目录：原项目 `artifacts/runs/plan-calibration-20260918-codex/`；含接手前文档包、源哈希/阅读范围及 findings.json。前轮证据在 `artifacts/runs/chatgpt-takeover-20260918-090419/verification/`，不只依赖临时工作区。
- 原项目位置：`/Users/hezifeng/Desktop/project/ms/ashare-ai-trader`；原有未跟踪资产和服务不作本批清理对象。代码改完不代表这些运行实例已加载新版本。
- 云端发布：本批不新建 PR、不触发 CI、不合并 master。上一轮读取账单 API 为 403，本轮未把它重查成新事实；余额、付费上限和最新 CI 需在发布时按实际权限核实。

## 2. 本轮改了什么，为什么

| 类别 | 本轮处理 | Codex 应如何继承 |
|---|---|---|
| 方案 | 新增 implementation-plan：v9→v9.1 差异、依据顺序、先安全/真实性、后业务闭环与能力证据 | 不机械照 v9 旧章节；也不推倒模块化单体或重做已交付能力 |
| 账本 | 保留十阶段；新增 BUG-025/026/027 和 IMP-048，细化原任务，区分开工/发布/效果依赖 | 新项不是已修复；状态仍只更新所属阶段 |
| 主动补缺 | 单位样本、双时间/修订、标签版本、实验污染、恢复副作用、反馈曝光偏差、授权撤销/乱序、运行版本及治理成本 | 每项先用既有资产补最小缺口，不新建十套平台 |
| 导航与规则 | AGENTS/INDEX/plan-registry/旧蓝图/技能入口指向当前修订，纠正旧“勿重开”等执行误导 | 历史规则可有据推翻；金融、安全、权限与零付费边界不随意扩大 |
| 证据 | 固定输入复现 KB 引用、评分卡分母、成熟度与准入缺失/NaN；记录局部源码风险 | 局部 E1/E2 不等于生产事故，也不等于代码已经修好 |

## 3. 验证边界

上一轮 `4e7bfcb`：后端 3682 passed / 78 skipped，前端本地及 UTC 各 681 passed / 72 文件，静态/构建、浏览器导航和干净检出已验。这些是前轮证据，不能手抄为本轮新结果。

本轮独立重跑：后端 **3682 passed / 78 skipped / 0 failure/error**；前端本地/UTC 各 **681 passed / 72 文件**；tsc、eslint、pyflakes、Next 构建与文档检查均通过。31 个任务唯一、阶段依赖无环、旧编号可解析。完整命令与耗时见本批 verification.json；本轮继续补充后的串行复验另存 continuation-verification.json，最终精确版本与检出结果见 delivery.json。业务代码与前端源码未改，不宣称修复了新缺陷、测完所有策略或刷新了生产运行状态。

原方案正文已全文复核；附件使用范围见 source-manifest.json。没有重新逐行审查全部历史 patch、数千文件或外部框架；未读范围仍由 GOV-020 分片推进，不能把清单数量当审计完成率。

## 4. Codex 下一轮具体顺序

仅使用 GitHub 的 Codex 须检出本功能分支，不只读尚未合并的 master；仓内 implementation-plan/阶段页已承载当前指令。原 v9/私有回执不可读时，只标记依赖它们的证据缺口，不编造读取或整体停工。

先读 AGENTS → 本页 → implementation-plan → 对应阶段；首次需要某主题时再读原 v9/附件及实际源码，已读未变的材料不每轮无差别重扫。已有工作区先核继承，不 reset/clean，不另建空分支后漏掉本批。

第一组看 W05 的 [IMP-046](stages/w05-agents.md#imp-046) 与 [BUG-025](stages/w05-agents.md#bug-025)、W04 的 [BUG-026](stages/w04-research.md#bug-026) 和 [BUG-027](stages/w04-research.md#bug-027)：先核安全链并做可独立恢复的修正，再处理引用/统计/准入真实性；每次只做一个行为切片。

之后按当前证据推进 W00/BUG-020、W01/BUG-022、W02/IMP-044，并连接 W03/IMP-006 和 IMP-048。RSH-026 原始采样可提前，IMP-020 效果判定等待成熟证据。已有规则 Outbox、快照、回放、参数 CAS 回滚不得再造一套。

W08/GOV-018 的目录退出仍开放，按本机清单与恢复验证分批做，不随账本完成而销账。W06 测瓶颈再改；W07 无消费者/收益证据不搬目录或拆大文件。GOV-021 四层验收贯穿每批，方案和账本可持续有据修订。

## 5. 发布与恢复

Codex 按已有授权完成代码、测试、提交、功能分支、PR、准确 SHA 的完整 CI、合并及主干复验；本批未替它完成这些业务开发步骤。先刷新主干/在途PR与当前预算，旧 PR #4 不直接合并。权限/余额未知阻塞相关发布，不为此跳 CI 或购买额度；安全的本地取证可继续，避免无限积压未发布变更。

恢复本轮文档可使用本批 before-docs.tar 或父提交 `4e7bfcb`；仅恢复本批明确文档范围，不清理用户文件，不把代码回退当数据/外发副作用恢复。后继有改动先对账，不覆盖其成果。

## 6. 可直接交给 Codex 的接续指令

```text
请接续 AI-Trading，不要从头重写方案。
工作区 /tmp/ashare-plan-led-backlog，分支 codex/plan-led-backlog；先核实际 Git HEAD/差异及远端。
先读 AGENTS.md、docs/handoff.md、docs/implementation-plan.md，再读所属阶段与实际消费者代码。
这是 ChatGPT 接替你进行的 v9.1 文档校准，保留了你已完成的主体；上一轮收尾为 4e7bfcb。
本轮没有修业务代码。BUG-025/026/027、IMP-048 是待你实施的任务，不是已完成成果。
先核 IMP-046 的宿主执行/自动晋级边界，再按证据逐片修 KB 引用、评分卡和研究准入。
每片先复现，记录现状/最小替代/收益与风险；写代码、测试和消费者验证，再更新任务唯一状态。
之后推进数据、用户图表、通知可靠性、统一执行事实、事件修订和研究闭环；不要重复构建已有能力。
区分开工、发布、效果前置；安全取证和样本采集不等待整个阶段全部完成。
发现更合理的设计可修改本方案和任务，但要给事实、可行性、验收和恢复；无证据不扩架构。
保留前任/接手者贡献及准确版本，原会话与历史证据不改写；不用报告篇数或测试数量证明选股效果。
按当前授权执行功能分支与PR/完整CI，未知预算不付费、不绕检查、不改权限；未上线如实说明。
```
