# 当前交接：全产品 v9.3 继续审查，流程图不是完工结论

当前方案：[实施校准v9.3](implementation-plan.md)。全范围读 [产品闭环](product-closure-design.md)、[细功能覆盖](feature-closure-audit.md)、[开放情境猎场](hunting-decision-design.md)，协作按 [账本短提示规则](collaboration-workflow.md)。原v9/21条Codex要求及U01–U39叠加保留；用户中途补充猎场要求不替代其他模块及小功能审查。

## 1. 固定入口与唯一当前任务

仓库 `1239890829/AI-Trading`；固定入口分支 **`codex/collaboration-runtime-state`**；已知施工目录 `/tmp/ashare-plan-led-backlog`。分支名是历史名称，不表示运行协作自动化。实际HEAD、差异和并行执行者须先核，不能从旧主目录、master或聊天标题猜现状。
唯一当前协调项：[W00/GOV-020](stages/w00-foundation.md#gov-020)，轮次 `product-closure-review-01` 持续取证。9d439c5交付设计基础、索引与旧互调退出，不是该任务完成。产品图确认只约束布局/导航及新猎场契约切换，不阻止读取、审查、隔离复验和已明确派工的独立正确性纠错。
本轮从 `9d439c5e9d9a3454ed1244fd3ca40d8323bcbdad` 远端版本继续；非猎场复盘回调的新证据写入 [W07/IMP-050](stages/w07-simplification.md#imp-050)，从 `443b1a0a5b42bd8d9fe7872609197ab611c35e26` 可追溯。范围/优先级复评仍在原阶段，不新建平行任务表。
本轮通过GitHub原生连接回写；不能推断本机工作树已取得这些远端提交。Codex接手先保全本机差异、核远端与任务，再正常同步；不得强制重置或覆盖未知改动。旧 `ledger-readback-01` 已被取代，不再修建自动互调。

## 2. 本次实际完成与未完成

本次核实ReviewTab准确源版本，提取原toggle回调，仅删除一处TypeScript类型注解，在独立容器注入状态setter和可控API Promise。三个场景中单次日期读取正常；两个乱序场景复现“B日期下显示A正文”和“旧A失败清掉已成功B正文”。它们属于同一请求身份缺陷；原始输入、时序、源blob与修复验收范围已进入W07条目，不依赖用户搬运附件才能理解。
验证范围是原回调隔离执行，不是真实React组件、浏览器或生产实测。未修业务文件、未更换界面、未调用Codex/模型、未启动服务；不能把“已复现”写成“已修复”。任务状态、原金融硬门与发布约束不变。
本次只回填W07证据、W00全范围与依赖边界、本交接。远端版本读取和文件回写已实际完成；整仓doc-health、React测试、本机日志和CI本次未重跑。前批3851/151等结果仅属于其准确版本，不能当本次新测。

## 3. 前批成果与证据仍然继承

9d439c5保留三份设计/细功能专题、39条需求映射、37条KB用途映射、29个后台注册去向；原32任务状态保留，新增BUG-028/029、IMP-049/050。原型和其44项专属测试成对回收；业务代码、前端实现和CI未随该批更改。该批doc-health、3851 passed / 78 skipped、pyflakes及151项相关文档复验通过，不代表全模块运行或策略效果验收。
原项目证据目录：`artifacts/runs/product-loop-redesign-20260918-190452/` 与 `artifacts/runs/product-closure-finalization-20260918-194637/`。最后delivery.json写入/读取曾超时，未取得确认，不能仅因文档提到它就当回执存在；已成功返回的检查日志、Git提交和远端读回分别作为证据。
结构盘点267个后端文件、2429个定义、182个路由声明、29处调度、133份前端源文件只是覆盖分母；未逐行审读/逐动作实测的部分仍未验收。原八份知识/策略入口批量加注曾被拒绝，未写入、未换通道重放；原文和历史身份保留，现行使用裁定以v9.3及已同步专题为准。外部“等8策略”的全部原定义仍待RSH-003取得和裁定。

## 4. 后续实施顺序与停止条件

先继续身份/日期/状态与反馈等高影响问题的取证，独立纠错不等待猎场全部重构。BUG-028的点时截断优先候选，BUG-029/026及复盘日期等非猎场问题按真实风险和证据重新排序；行为实施由网页在所属任务写明确切片后交Codex，不能凭一条“继续”扫描所有任务自行开工。
复盘纠错需用真实组件复验初始化、focusDate、toggle、处置后刷新、收起/卸载/重开；成功与失败都绑定当前请求。之后扫描搜索、板块抽屉、资金历史日等同族消费者，公共hook已修不等于调用方的setState安全。未验路径在细功能覆盖保留，不用父模块完成覆盖子项。
用户在网页说“Codex执行完了”，网页读真实diff和证据，先审核成果，再评估大小消费者、遗漏、替代与净收益；通过/整改/阻塞和下一动作写回原任务。用户在Codex说“ChatGPT审核完了”，Codex读实际结论，非自动通过；提交推送后才报告已交付，重复通知不重复实施。
后台情境开放，前台按状态简洁呈现；未知线索不强归类，不因新名字启用被否决策略。旧双端互调、网页唤醒及Bridge依赖不恢复；业务后台采集、风控、通知和复盘保留。整站UI切换、研究效果和运行验收仍各有条件；主干合并另需准确版本的独立审核与完整CI，不直接推主干或扩大费用/权限。
