# 当前交接：RSH-031 待条件；下一候选 G2／IMP-007

## 1. 权威入口与批准范围

用户已批准全域方案 v1.3（含 v1.0–v1.2），仓库实施方案 v9.13／U52–U55；
累计用户要求范围为 U01–U55。规划 PR #172 已合并，批准与历史拒绝见
[确认记录](review/system-plan-v13-approval-20260926.md)。
读取链：最新 master → AGENTS → INDEX → 本 handoff → 总方案/相关蓝图 →
`docs/ai/jev-integration.md`（Jev 现役蓝图）→
`docs/ai/continuous-evolution.md`（开放世界持续演进入口）→
retro-and-gaps §5.9/§6.0 → 所属 stage。任务状态、优先级与门序仅归 stage。

B 五任务主页为首选比较候选，C 四入口须对照；JEV 对自选/持仓一级拆分弃权保留。
50 子功能、18 域、16 组件族、73 源、30 后台声明是设计映射，不是固定上限或运行通过。
界面融合不合并资金、收益口径、数据事实或权限；后台先验触发、单写者、幂等、
持久结果、失败/取消/恢复、权限与维护入口。

## 2. 临时协作授权与当前选择

**U50 降级授权回执**：2026-09-24 用户明确授权本机 Codex 在
`DEGRADED_FULL_CONTROL` 下接续实施、U49 作者反证、自审、PR/CI、发布、
合并与清理，直到明确撤销。作者发布回执只能是绑定准确 HEAD 的
`DegradedRelease`，不得称独立 Review；本地门禁、三项 required CI、
`release_check.py`、合并后 master CI 与分支清理不降级。授权不扩到模型费用、
真实通知、券商、部署或生产数据库。

2026-09-26 用户要求以后**整项任务连同所有切片同轮完成**。本轮从干净的
`origin/master` `692706275b9c0cc54f423bac6915f3a80458b816` 重算
`scripts/ledger-runtime-selection.py --json`：static/effective 均为 G1／RSH-031。
前一任务 IMP-040 已由 PR #174 合入；详细交付见其 stage、
`docs/data/data-sources.md` §8.10 与 GitHub 合并记录，本轮不重做。

**U49 主动审计回执**：开工前核 master、selector、W04、研究蓝图与既有数据源；
重点反证赢家倒推、缺负分母、历史 ST／上市豁免、当前题材成员回填、
后发文本倒填、未来日期污染、未匹配控制、含除权日代理和无法证明的可成交性。
本轮只形成作者自审；准确 HEAD 发布回执和 required CI 以 PR 现场为准。

## 3. RSH-031 已完成实测与未完成条件

`backend/scripts/rsh031_phase0.py` 只读本机 marketdb，冻结 2016–2023 探索段、
2024 验证段与 2025 起最近保留段。探索段 6,766,333 条日线、292,127 条
名义涨幅**代理**事件、384 条未匹配对照；除权日和早期无足够历史条目独立标记。
脚本、测试、输入 SHA、逐行研究资产与缺口详见
[研究蓝图 §14](research/limit-up-dragon-research.md#14-phase-0-首轮实测与停止点2026-09-26)。
本机大产物位于忽略的 `artifacts/runs/rsh031-phase0-final-20260926/`；
其余本轮临时草稿已经由 `safe-trash` 留下可恢复记录。

用户随后允许以可取得的近期数据继续。先提交固定窗口和评分协议，
`backend/scripts/rsh031_recent_pools.py` 对 2025-10-09 至 2026-08-31 的
220 个交易日采集同花顺沪深涨停 17,393 条、炸板 5,571 条，440 个池的
分页、计数、去重与交集检查通过。`rsh031_recent_analysis.py` 以前一日连续
20 个交易日的日线建立 1,122,700 个沪深证券日分母；13,117 条首板可评估。
固定组合在保留段 Top20 命中 57／1300，简单相对强弱为 61／1300，
无可晋级的稳定增益。发现源返回无北交证券后，已排除北交负例并保留
最初诊断资产。详细 SHA、分段指标和排除理由见
[研究蓝图 §15–16](research/limit-up-dragon-research.md#15-近期可得数据的预注册补充2026-09-26)。
近期原始池和分析 Parquet 只在忽略的 `artifacts/runs/rsh031-recent-*20260926/`，
不进公共仓库；本次未新购付费源、未改生产库或策略。

源池不回显请求交易日，返回的 timestamp 只是查询时间；北交没有标签，
历史题材成员、新闻/公告首次可见、临板/封板前事件、历史证券身份与
可成交/成本证据仍不足。东财历史请求回 0 且 `qdate` 指向 2026-09-24，
不能当独立证实。2026-08-28 曾在旧策略文档复盘，保留段也不能称全然
未污染。RSH-030 的事件 human gold 仍为 0/240，不能自行填充。
完整层级机制、purged OOS、题材/龙头、Jev 效果和交易效果验收未达到。
现有授权来源无法提供必要点时语料与成交证据，按 W04 停止／重开规则，
RSH-031 转`待条件`；该状态不是业务效果完成。

2026-09-27 按用户“只要不要钱”继续查免费来源，巨潮公开历史查询实测
2020-08-24、2025-10-09、2026-08-31 单页均返回公告源披露时间与附件；
2025-10-09 的定向单股查询也返回 2 条，PDF 范围请求确认文件签名。原始小样仅留忽略的
`artifacts/runs/rsh031-free-source-audit-20260927/`，来源、SHA、页数字段异常、
会员墙／积分门、历史逐笔可用窗口等见[研究蓝图 §17](research/limit-up-dragon-research.md#17-免费历史来源的定向实测2026-09-27)。
巨潮完整单日分页得到 995 个唯一公告 ID，但 590 条只有北京 00:00 日期占位。
BaoStock 逐股历史采集又为近期沪深 5,214 只证券取得 1,138,688 行 ST／停牌状态；
原 1,122,700 证券日分母和 17,393 条涨停池 ST 均零缺失，池内源间 ST 零冲突，
原分母中识别出 35,445 ST 日和 481 停牌日。版本化采集／核验脚本、SHA 和边界在
[研究蓝图 §17](research/limit-up-dragon-research.md#17-免费历史来源的定向实测2026-09-27)。
这补的是**近期证券身份覆盖及公告源声称的披露时间**，不是逐日身份发布时间、公告／新闻的历史首次可见与版本全证据；
也没有拿到完整历史题材成员、封板前全样本或委托队列 fill；固定评分未重算，
RSH-031 仍`待条件`。本轮没有购买、启用付费服务或改变原用户数据。

不得把研究代理或本轮探针当作任务完成或生产候选。重开条件是取得授权与
覆盖明确的题材成员、早期证券身份及近期身份发布时间、
新闻/公告 `available_at` 与修订，以及封板前和成交证据，再完成其余机制
和独立验证；当前数值候选已否证，不为凑进度调用 Jev。
本轮从更新后的 stage 重算 selector：static/effective 均为 G2／IMP-007
（模拟动作预检与恢复一致性）。它是下一次“继续任务”的候选，须在合并后
由最新 master 再算；本轮不提前执行。当前门候选顺位为
IMP-007 → BUG-016 → IMP-032 → BUG-009。

- **当前主门**：G2
- **主切片首选**：IMP-007
