# 当前交接：BUG-025 引用真实性修复

> 方案仍为 [v9.2](implementation-plan.md)；任务单点状态见 [W05 / BUG-025](stages/w05-agents.md#bug-025)，总入口见 [阶段账本](retro-and-gaps.md#60-阶段索引)。长期分工见 [协作规范](collaboration-workflow.md)。

## 1. 当前现场

- 2026-09-18 用户因Codex无额度，明确要求ChatGPT临时代执行；本轮只处理BUG-025，不调用Codex或其它模型。长期网页统筹审核/Codex执行分工不取消。
- 工作区 `/tmp/ashare-plan-led-backlog`，分支 `codex/plan-led-backlog`，本轮开始HEAD `a9d4adc5db862c6160f2df0299d89f59c5900c93`。保留前任已做计划/账本/代码，不从旧主干重建或清理用户文件。
- 原Codex任务 `01a0acfd-221b-7763-9aae-299d78b8738a` 未启动；用户原项目的未跟踪业务文件和8000/3000服务不属于本批操作对象。
- 本批业务改动仅 `backend/app/picks/kb_routing.py`，回归归原 `backend/tests/test_kb_routing.py`；不改策略权重、交易/通知偏好、数据库schema或生产开关。
- 本轮执行者同时是作者，测试属于作者验证，不冒充独立网页审核。尚未创建PR、运行云端CI、合并主干或更新生产实例。

## 2. 本轮完成内容与边界

- 不可用/空索引、未解析行、未知册、重复ID造成的覆盖不一致都不能被认证为有效引用；条目ID/册/状态/标题异常同样拒绝。
- 保留 `not_consulted / cited / rejected` 三态：无引用请求仍是not_consulted；失败原因保留。新字段requested记录请求，kb_ids/status/support只包含验证通过项；合法ID仍不等于实际检索过正文。
- 保留唯一索引解析器及其诊断，不将部分读取成功冒称证据完整；同一次快照只读索引一次，兼容一次性迭代输入和保序去重。
- 原始快照不回写；重放和汇总使用存档引用，索引后来失效不会改写旧结果。正文引用追溯/知识库消融仍归RSH-027，IMP-046权限/晋级、BUG-026/027仍未实施。

## 3. 验证记录

旧实现上新增17项反例全部判红；修复后专项60 passed，含23项新增BUG-025用例，并覆盖机会归档、通知归档、重放/汇总、legacy和配置边界。
五组正常输入的存档结构与旧实现对照，除requested外相同。局部9轮×100次微基准：无引用旧1.339ms/新1.342ms；混合合法/拒绝引用旧1.352ms/新1.348ms。不据此宣称生产性能最优或全负载已验收。
本轮全量：后端3783 collect = 3705 passed + 78 skipped，0失败；比父版本新增23项BUG-025测试，原测试ID无删除。前端本地/UTC各681 passed/72文件，tsc/eslint/pyflakes/Next构建与doc-health通过。最后任务状态/交接回填后单独复验相关回归及精确提交检出，结果见final-checks.json和delivery.json。

## 4. 恢复与发布

持久回执在原项目 `artifacts/runs/bug025-citation-integrity-20260918/`：原文件包/哈希、红绿回归、性能协议及实测、完整日志和最终delivery。此前计划/治理历史由Git及既有artifacts保存，不在本页堆叠旧轮次数字。
本片无数据迁移，回退只涉及明确文件；保留已归档引用与失败理由。代码回退后不得将失效索引重新当作已验证来源，需要恢复时可先停止相关引用功能，不删除历史。
因本轮作者与执行者相同，不签发独立APPROVED；本地完成后保持待交付，交后续独立审阅与准确版本完整CI。预算/权限未核实时不创建付费或绕过检查，也不承诺已上线。

## 5. Codex恢复后的接续指令

```text
先核 codex/plan-led-backlog 最新HEAD和本页，不覆盖ChatGPT在你无额度时按用户授权实施的BUG-025。
读取AGENTS、协作规范、实施方案及W05任务，核kb_routing.py和原测试文件的实际差异。
BUG-025新增索引完整性/身份校验及requested字段；三态不变、旧快照不回写、无数据库迁移。
先复核本片回归、消费者和待交付边界，不重复从零开发；作者自检不算独立审核通过。
正文引用/消融、权限及晋级、评分卡/研究准入仍为后续独立任务，不据本片全绿批量销账。
按长期分工和网页明确派工继续；没有有效审核与完整CI不得合并，无额度不调用模型或购买额度。
```
