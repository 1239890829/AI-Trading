# 架构与设计汇总（Architecture & Design Summary）

> 当前产品修订见 ../product/product-closure-design.md 与 ../product/feature-closure-audit.md：跨页面主流程与每个小动作都核身份/时点/失败/恢复；业务计算后台拥有，前端只呈现和接收必要用户操作。本文记录既有架构，不代表新设计已实施。
> 当前 AI/语义协处理层以 ../implementation-plan.md v9.5 与 ../ai/jev-integration.md 为准；本文只保留已实施业务架构口径，不复制 Jev 的现役状态或阈值。

> **定位**：跨页面联动 / 资金流 / 热点管道 / 事件模块四份**设计方案已实施部分**的**稳定职责与口径说明**。
> **现役入口不在此**：当前系统契约分别归 `../system/architecture.md`、`../system/api.md`、`../system/deployment.md`、`../system/mcp.md`、`../system/websocket.md`；08-29 的 `../archive/PROJECT-MASTER.md` 仅作历史基线。
> **✅ 三份原文档已删除（2026-09-10）**：本文承载已实施部分。
> **⚠️ 状态不在此（2026-09-14 `GOV-002` 收敛）**：**任何「未做 / 待办 / 进度」断言一律去 `retro-and-gaps.md` §6.0**
> （全仓唯一任务清单）。本文只写**已落地能力与其口径**；正文若出现旧的"未做"字样，一律以 §6.0 为准。
> **为什么收敛**：此前本文把**已闭环项写成待办**（§2 的题材徽标/ watcher 实为 `P1-4`/`P1-5`/`P1-1` ✅、
> §4 的 4 项待办实为 `P0-2`~`P0-5` **全部 ✅**）⇒ 会诱发「文档写未做、实际已完成」的**重复开发**。
> 这类"能力描述失真"是 `doc-health` 查不出的（它只查结构与引用），成因与修法见 [[KB-ENG-85]]。
> **整理**：2026-09-10；**状态收敛**：2026-09-14。

---

## 1. 跨页面联动设计（linkage-design）—— **全部已实施**

| 项 | 状态 |
|---|---|
| T1 题材目录/成分同步 | ✅ |
| 切片 B 详情题材 chips + `?focus` | ✅ |
| T3 题材 K 线（B3 收尾） | ✅ |
| E1+E2 事件最小闭环（规则版） | ✅ |
| 切片 C 预警 → 详情 | ✅ |
| 切片 E 跳转 | ✅（2026-09-03） |

**结论**：联动主体已闭环。原记两项增强项**均无未闭环欠债**，且状态不以本节为准：
- `E4 复盘回写` = 账本 `RSH-017`（**等窗**：文档自注「上线运行积累数据后」）。
- `E3 LLM 增强` = **已闭合**：LLM 通道走 `claude -p` → cc-switch 当前 DeepSeek（2026-09-19 实测 `deepseek-v4-flash`），`events/llm_aux.py` 已落地
  （见 `ai-evolution.md` §7 层 1）；「LLM 增强层待凭据」在 `retro-and-gaps.md` §五 遗留承接表中
  **已明确闭合、无需跟踪**。原行「待凭据支持」是**凭据到位前的旧表述**，已改正（`GOV-002`）。

---

## 2. 资金流三级重构（fund-flow-redesign）—— **已实施**

**体系**：大盘 → 板块/题材 → 个股 三级；打通板块监控 / 选股器 / 自选 / 个股详情 / 预警 / 策略信号。

**P0 已实施（2026-09-07）**：
- `board_flow.py` 服务 + 3 端点（`/market/board-fund-flow*`）
- 落盘 `data/boardflow/daily.json` + `daykline.json`
- fund-tab 三段式（①②③）+ R1 命名统一 + R2 `kline-chart.tsx` 下线

**实施偏差（口径须知）**：
1. 成员/日度沉淀深度取 Top20/kind，保留 60 交易日（控文件体积）
2. 龙头股列**仅名称**（成员净额在抽屉）
3. 5d/10d 用官方字段 `f164`/`f174`（实测 = 今日 `f62` + 近 N−1 完成日 bar，偏差 0.00 亿）；**20 日区间与连续流入走落盘自算**（读路径零外呼）

**后续项（P1/P2）**：题材徽标 / watcher / alert / tech_score v4 流转项 —— **均已闭环或已进观察档，
状态一律见账本 §6.0**（`题材徽标` = `P1-4`/`P1-5` ✅、`watcher` = `P1-1` ✅、
`alert theme_event_burst` = `P2-4`、`tech_score v4` = `P2-1`，后两项随 `RSH-018` 维持观察）。
（**更正 2026-09-11**：其中「助手工具」已于 09-08 P0-3 落地、09-11 扩容至 **26 个只读工具**
并修掉「提示词自我否认」——明细见 [[KB-ENG-49]] 与 `docs/retro-and-gaps.md` §六。）
⚠️ 本行原写「**P1/P2 未实施（保留待办）**」，而清单里前两项**当时已落地** ⇒ 已按实测改正（`GOV-002`）。

---

## 3. 热点消息管道（hotspot-pipeline-design）—— **P0 已实施**

**三段能力对标**：捕获 → 传导 → 映射验证。

**P0 已实施（2026-09-07 午前，commit c84d382）**：
- G1 快讯流：`news/flash.py` 双域 failover → `build_event` 指纹去重入 EventStore；**盘中 15s / 盘外 60s**
- G2 **热点验证环**：`events/verify.py`（实时计算不落库；四态 `confirmed/fermenting/faded/unknown`）——✅ 已落地，账本 `P1-6`

**实施口径差异（已在代码/测试锚定）**：
- 链目标用**官方目录实名**：钾肥→「化肥」、电力→「绿色电力」、电网→「智能电网」、算力→「东数西算(算力)」
- 非农方向 P0 按标题意外词二分（超预期 → −1 / 不及预期 → +1 / 无词 → 显式 0）；**「意外差量化投票」不是待办而是已验证的否证**——10 年 × 116 期回测无稳定 alpha ⇒ 账本 `RSH-021`，**勿重开**

> 原始盘点为 2026-09-07 晨东财接口实测（备域 push2delay），四条标题过 `events/extract.py` 的输出为真实运行结果而非推演。

---

## 4. 事件模块重设计（news-event-module-redesign）—— **主体已落地**

**已落地**：判定状态机（judged / pending → 6h 收敛 neutral / 半衰期 expired）、方向抽取（题材目录匹配 + 方向词典 + 传导链）、**时区统一北京时间**（2026-09-09 修复：此前 published_at 存 UTC 导致展示早 8 小时）、事件 Tab 可点弹窗、`summary` 字段采集与存量回填。

**后续项（原记 4 项「待办」，实测全部已闭环）**：
> ⚠️ **本节 4 项原标「待办」，实际全部已闭环**（2026-09-14 `GOV-002` 实测复核）。
> **本节不再维护状态**——明细见账本 §6.0，此处仅留去向映射：
>
> | 原待办 | 现状 | 账本 |
> |---|---|---|
> | ① 写入盘前简报 `directions` | ✅ 已落地 | `P0-5` |
> | ② 快讯频道扩展 + 多页拉取 | ✅ 已落地 | `P0-2` |
> | ③ 「个股名→题材」映射 | ✅ 已落地 | `P0-3` |
> | ④ 事件正文参与判定 | ✅ 已落地 | `P0-4` |

---

## 原始文档指针

| 原始文档 | 处置 |
|---|---|
| linkage-design.md | 精华并入本文 §1；主体全部实施 → 已归档 |
| fund-flow-redesign.md | 已实施部分入本文 §2；**原件已删除**（后续项 → 账本 §6.0：`P1-1`~`P1-5` ✅ / `P2-1`~`P2-3`） |
| hotspot-pipeline-design.md | 已实施部分入本文 §3；**原件已删除**（后续项 → 账本 §6.0：`P1-6` ✅ / `P1-7`·`P2-11`（等窗）/ `P1-8` ✅ / `P1-9`（否证）/ `P2-4`~`P2-6`） |
| news-event-module-redesign-20260909.md | 已落地部分入本文 §4；**原件已删除**（后续项 → 账本 §6.0：`P0-2`~`P0-5` **全部 ✅**） |
