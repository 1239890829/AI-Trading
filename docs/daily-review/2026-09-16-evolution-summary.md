# 进化总结报告 · 2026-09-16（周三）

交易日确认：✅ `watch-ledger.trade_date = 2026-09-16`（= 今日）。
发送：15:52:32，`message_id = om_x100b6590e2c840a0b14dddbac6da361`，当日仅此一条飞书消息。

---

## 1 采集证据（全部只读）

| 来源 | 端点 | 结果 |
|---|---|---|
| 后端健康 | `GET /api/health` | HTTP 200，`status=degraded` / `is_stale=true`（**盘后假警报**，快照降频所致；判活只看 200） |
| 议程 | `GET /api/agent/agenda` | `id=7 / status=failed`，`error.code=LLMError` |
| 台账 | `GET /api/picks/watch-ledger` | 148 行 / 121 清算 |
| 信号健康 | `GET /api/picks/signal-health` | 12 组 / 49 样本，`status=drift` |
| 实验裁决 | `GET /api/agent/experiments` | `[]`（0 条，连续多日） |
| 数据健康 | `agenda.inputs.data_health` | **数据缺失**（议程 failed ⇒ `inputs={}`） |

### 1.1 议程失败详情（两轮复现）

```
error.code    : LLMError
error.message : claude_cli 调用超时：Command ['<claude-cli>', '-p', '{"review": {...}}']
```

| 轮次 | 起 | 止 | 耗时 | 结果 |
|---|---|---|---|---|
| 自动（调度器） | 15:45:51 | 15:47:53 | 122s | failed |
| 手动 `POST /api/agent/agenda/run` | 15:49:33 | 15:50:54 | 81s | failed |

两轮同一错误码 ⇒ 非偶发抖动，**网关侧超时**。全程只读，未改代码、未改配置。

### 1.2 台账（交易表现）

| 指标 | 今日 | 近值对比 |
|---|---|---|
| 总条目 / 已清算 | 148 / 121 | 09-15: 87 / 69 |
| 胜率（success/(success+fail)） | **86.4%**（76 胜 / 12 负） | 09-15: 61.2% |
| flat | 33 | — |
| 平均盈亏 | **+0.66%**（首次转正） | -0.65% / -0.39% / -0.65% |
| 成功单均值 | +2.07% | 09-15: +1.78% |
| 失败单均值 | -3.91% | 09-15: -3.88% |
| **盈亏比** | **0.53** | 09-11: 0.43 → 09-14: 0.43 → 09-15: 0.46 |
| 最差 / 最优单 | -6.35% / +3.40% | 09-15 最差 -11.02% |
| 未清算 27 条 layer 分布 | `watch_no_entry` 26 / `quiet_starting` 1 | 设计内，非异常 |

### 1.3 信号健康

- 窗口：12 组 / 49 样本，胜率 73.47%，`mean_excess = +1.0224`，今日相位「退潮」（今日组 n=1，全 bad，-2.78）。
- **CUSUM 自指缺陷未修**：`mu0 = 1.0224` 与被评估窗口 `mean_excess` 完全相等，`threshold = 1.5` 远小于日间噪声 ±5~7pp
  ⇒ `drift = true` 为**必然误报**。该问题 09-14 提出，已连续 3 个交易日未修。

### 1.4 数据健康（替代取证，因议程失败）

`agenda.inputs.data_health` 不可得，改用两项历史遗留指标：

- `backend/data/sentiment_metrics.json`：mtime **2026-09-16 15:14**（0.6h 内）→ 曾停更 6 天的隐患已消除。
- `data/ashare.db`(marketdb)：mtime **2026-09-16 15:51** → 同步正常。

### 1.5 自治开关核查（承接 09-15 遗留）

- `backend/.env`：**无显式设置**。
- `backend/app/core/config.py:323`：`agent_autonomy_enabled: bool = True`；`:324`：`agent_code_change_enabled: bool = False`。
- 结论：09-15 记录的「默认值被翻转为 False」**未复现**，今日议程失败与自治开关**无关**，根因是 LLM 网关。

---

## 2 报告原文（已发送）

```
【进化总结 · 2026-09-16】
▍What 今日做了什么
1. 15:45 进化议程生成后连续两次失败（LLMError: claude_cli 调用超时），今日零项进化动作落地，无代码变更。
2. 台账 148 条 / 121 清算：胜率 86.4%（76 胜 12 负）、平均盈亏 +0.66% —— 平均盈亏首次由负转正（前几日 -0.65%/-0.39%/-0.65%）。
3. 信号健康 12 组 / 49 样本，胜率 73.5%、平均超额 +1.02%，今日相位「退潮」；CUSUM 基线 mu0=1.0224 仍等于被评估窗口均值，自指未修。

▍Why 为什么
1. 议程整轮依赖 claude_cli 网关，超时即整体失败且无降级路径，属单点故障。
2. 胜率抬升同时平均盈亏转正，改善非仅来自命中率；但成功 +2.07% / 失败 -3.91% → 盈亏比 0.53（0.43→0.46→0.53），仍是「赢小亏大」。
3. CUSUM 基线取自被评估窗口自身，阈值 1.5 远小于日间噪声 ±5~7pp，drift 告警必然误报。

▍How 怎么做
1. 15:45 轮询至 failed 取证，15:49 手动触发 /api/agent/agenda/run 复现，两次均 error.code=LLMError；全程只读，未改代码。
2. 盈亏比由台账 rows 按 verdict 分组现算（端点仅给汇总胜率与均值）。
3. 数据健康因议程失败缺失，改用替代取证：sentiment_metrics 更新于 15:14（0.6h 内）、marketdb 更新于 15:51，两项均新鲜。

▍明日关注
1. 修 claude_cli 超时：加超时上限/重试或降级为规则议程，否则进化链路持续空转。
2. 盈亏比 0.53 仍 <1：核查止损与跟踪止盈是否让亏损单跑满（今日最差单 -6.35%）。

不构成买卖建议。
```

---

## 3 待办结转（下次先看）

1. **claude_cli 超时**（今日头号）：需加超时上限 / 重试 / 降级为规则议程；否则进化链路每日空转。
2. **CUSUM 独立基线 mu0 + 阈值与 σ/√n 相称**（09-14 提出，连续 3 日未修），修前 drift 不可信。
3. 盈亏比 0.53：连续四日 <1（0.43→0.43→0.46→0.53），方向对但幅度小；查止损/跟踪止盈。
4. 实验 0 条、影子队列 0 持续；`plan_alignment.missing = hmm_regime` 连续 4 日未落地。
5. 台账 flat 占比 33/148（22%）偏高，是否应纳入胜率口径需确认。

---

不构成买卖建议。
