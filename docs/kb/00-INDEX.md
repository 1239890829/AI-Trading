# 知识库总索引（KB Index）

> 定位：**本项目唯一权威知识库**——用户亲授知识、历史教训、系统决策的 canonical 版本。
> 与 docs/INDEX.md 的分工：INDEX 管「文档」；这里管「知识点」。引用知识时必须标注 KB-ID。
> 建库：2026-09-09（用户指令：知识散落多文档无法回忆 → 整合为结构化可检索知识库）。

## 检索方式

- 按类别读：`01-stock-picking.md`（选股）/ `02-trading-lessons.md`（交易教训）/ **工程教训 KB-ENG 分四册**：`03-engineering.md`（**应用与设计层**）/ `09-verification-pitfalls.md`（**验证层**：测试·门禁·CI·防线有效性）/ `10-data-contract-pitfalls.md`（**数据契约层**：写入·去重·传输·时间·质量门）/ `08-tooling-pitfalls.md`（**工具与环境陷阱速查·操作类短条目 KB-ENG-01~15**）/ `04-decisions.md`（系统决策）
- 按关键词：`grep -rn "关键词" docs/kb/`（如 `grep "竞价" docs/kb/` → KB-STOCK-11）
- 按状态：本索引表格「状态」列过滤（⏳ 待落地条目 = 议程 C 类改进项候选池）

## 状态定义

| 符号 | 含义 |
|---|---|
| ✅ | 已落地：有组件/流程承载，注明位置 |
| 🔶 | 试验中：影子运行/实验窗口内 |
| ⏳ | 待落地：已有设计，未实现或未接线 |
| ❌ | 被取代：保留条目防回退，注明被谁取代 |
| 📎 | **示例/参考输入**：题材案例、外部材料等——**不构成规范，不得当判定标准使用**（[[KB-DEC-018]] [[KB-DEC-019]]） |

> **📎 的用法（2026-09-10 新增，KB-DEC-019 反固化条款）**：具体题材/个股案例只允许以「示例」身份存在；
> 其可迁移价值是背后的逻辑，**案例里的排序/强度/"更优"等结论性内容不得当已验证知识使用**。
> 单日、单案例的"吻合"**不构成验证**（同一交易日的多个吻合只算 1 个证据）。

## 复盘框架（06-review-framework.md）

LLM 复盘 SOP：七阶段（定界→事实链→周期定位→六维剖析→规律提炼→KB对照→反例自检/自评闭环）。
任何复盘类任务**必须先读该框架再执行**；框架自身进入自优化循环（演化日志）。

| 版本 | 一句话 | 状态 | 来源日 |
|---|---|---|---|
| v1.0 | 七阶段复盘 SOP + 六维自评闭环（百大案例实践提炼） | ✅ | 2026-09-09 |

## 文档治理（07-doc-curation.md）

docs/ 与 memory 的**文档整理方法论**（v1.6）：六层分类（L0 权威知识→L5 只读历史→L6 参考输入）、归属三问、精华五要素、完成态计划的压缩归档流程、双索引与串联纪律、同类合并/拆分判据、三级淘汰与安全护栏、**长度按层配额**（KB 条目 ≤60 / 框架 ≤500 / 报告 ≤600 / 日志单轮 ≤80）+ **摘要前置**（>150 行须前 40 行含速览与导航，**含层例外**）、试运行与自我淘汰条款。
**体检已脚本化**：`python3 scripts/doc-health.py`（A 未登记 / B 死链 / C 超层配额 + **C-KB 条目超长 + C-KB 扫描覆盖**（后者防"扫描面写窄导致新文件零检查"，[[KB-ENG-60]]）/ D 摘要前置 / E 同类聚集 / **F 代码注释死引用**（B 只扫 `*.md`、F 扫 `app/tests/web/scripts`）/ **F2 代码裸名死引用** / **F3 KB 指针错册**（`见 kb/NN-x.md … KB-ID` 必须同册），有问题 exit 1）——**每个功能点收尾必跑**。
**状态语义铁律（v1.6）**：`✅ 已落地`＝有组件承载；**示例/题材案例一律 `📎`**，两者不得混用（[[KB-DEC-019]]）。

任何"整理文档/整合同类/归档计划/清理冗余文档"类任务**先读该框架**；附录 A/B 为本仓库现状体检与首批整理候选（执行前需逐项确认）。

## 书库编目与收敛标准（11-doc-catalog.md）

`07` 回答「**怎么做**」（分类/归属/配额/淘汰的流程），`11` 回答「**判据是什么**」——两者配套。
2026-09-13 建（用户要求：重新定义「一份好文档」+ 明确 KB 收敛标准 + 覆盖非 `docs/` 面）。

| 内容 | 一句话 |
|---|---|
| §1 **「一份好文档」七条判据** | 可定位 / 单一真相源 / 状态显式 / 主张带失效条件 / 结论可验证 / 待办有出口 / 可维护——**每条都对应一类真实踩过的坑**（§七 已累计 34 处偏差反推） |
| §2 **卡帕西三层模型 ↔ 本仓映射** | L1 原始源（`.workbuddy/` + `archive/`，只读）· L2 编译层（`docs/kb` + `summary` + `CONTEXT.md`，保持最新）· L3 约定层（`kb/07` + 本册 + `AGENTS.md` + `doc-health`） |
| §3 **KB「收敛」标准** | 定义 = **在不损失知识的前提下减少"需要人读的条数"**；四动作（合并/蒸馏/降层/拆册）+ **三底线**（可检索性不降 / 判据不丢 / 证据链不断）+ **触发条件表**（不靠"感觉条目多"）+ 现状基线（139 条 / 3123 行） |
| §4 **非 `docs/` 面治理** | 根目录 / `.workbuddy/memory` / `reports` / `artifacts` / `skills` 的层级归属与处置 |
| §5 **生命周期六阶段** | 新建 → 保持 → 更新 → 收敛 → 归档 → 删除（各阶段的触发与动作） |
| §6 **本册自身待办** | 4 项诚实声明（`docs/INDEX.md` 缺编号列 / 根部 8 份 overview 未归位 / 无主 png / `doc-health` 未覆盖非 docs 面） |

**收敛为何必须做**：条目只增不减 ⇒ 信噪比下降 ⇒ `00-INDEX` 从"可通读"变成"只能 grep" ⇒
**没人再读它** ⇒ 沉淀失效。这与「一上线就红满天的门禁会被整体无视」（[[KB-ENG-58]]）是**同一种失效机制**：**过载 = 被忽略**。

## 维护规则（沉淀机制）

1. **当轮入库**：对话中用户教的新知识 / 拍的决策 / 重大教训 → **同一轮**写入对应 KB 文件（含 ID/来源/状态/关联）并更新本索引；daily log 只留一行指针。
2. **状态流转**：⏳ 落地后改 ✅ 并补「组件 + commit」；被取代的条目改 ❌ 并写「被 KB-XXX 取代」——**永不删除条目**。
3. **冲突处理**：两条知识冲突 → 各自加 ⚠️ 指向对方，不得静默覆盖（矛盾本身是有价值的信息）。
4. **健康检查**：每日议程第八路证据（`evidence.knowledge_base`）自动统计本索引；⏳ 超 14 天自动列入复查；周五元评估做重复/孤儿扫描。
5. **召回链路**（**2026-09-14 起**）：会话开始 → `.workbuddy/memory/MEMORY.md`（**纯索引，一步跳**）→ **`docs/INDEX.md` §0 主题路由**（不知从哪读起时）或 **本索引**（已知 KB-ID / 按关键词）→ 按类别读文件。
   - **读法协议（三级跳，逐级加量）**见 `kb/07` §4.4；**不要**在会话开头把所有相关文档读一遍——那正是"单次读取量"失控的来源。
   - 云端 conversation_search 对本项目召回已实测失败，本地 KB 是唯一可靠来源。

## 选股知识（01-stock-picking.md，KB-STOCK）

> **案例层（KB-STOCK-01~06）是 📎 示例库，不是判定标准**——它们回答「题材可能怎么扩散」，
> 供理解与类比；**可迁移的是"沿产业链纵深扩散"这一逻辑**（由 KB-STOCK-05 / KB-STOCK-19 与扩散模型承载）。
> 具体题材名（厄尔尼诺/PCB/CPO/半导体）**会不断衍生出千万个，本身不进入选股规则**（[[KB-DEC-019]] 反固化条款）。

| ID | 一句话 | 状态 | 来源日 |
|---|---|---|---|
| KB-STOCK-01 | 厄尔尼诺→农业链炒作（化肥→种业→粮食→糖）**〔题材示例〕**；**2026-09-11 实测：其"弹性排序"在 ONI × 申万行业月超额上未获支持**（基础化工 t=−0.20 方向相反）⇒ 只作可解释性材料、必带 `empirical_verdict` | 📎 | 2026-09-09 |
| KB-STOCK-02 | PCB 概念向材料端发散（模块→耦合→材料）**〔题材示例：产业链纵深扩散〕** | 📎 | 2026-09-09 |
| KB-STOCK-03 | CPO 链：光模块→耦合设备→材料端**〔题材示例〕** | 📎 | 2026-09-09 |
| KB-STOCK-04 | 半导体扩散路径**〔题材示例·历史参照〕** | 📎 | 2026-09-09 |
| KB-STOCK-05 | 先炒大票龙头，后炒细分补涨 | 🔶 | 2026-09-09 |
| KB-STOCK-06 | 优秀交易员提前嗅到板块机会（题材形成信号） | 🔶 | 2026-09-09 |
| KB-STOCK-07 | 嗅到≠买入：盘面/量价/资金/指标四重验证后才提示 | ✅ | 2026-09-09 |
| KB-STOCK-08 | 不可能一直空仓（试仓档存在性） | ✅ | 2026-09-09 |
| KB-STOCK-09 | 消息面时效性：新闻→题材映射须进盘前/盘中 | ✅ | 2026-09-09 |
| KB-STOCK-10 | 梯队七角色（空间板/龙头/中军/反包/补涨/跟风/首板） | ✅ | 2026-08-29 |
| KB-STOCK-11 | 竞价分带是龙头续命关键（低开/标准接力/超高开） | ✅ | 2026-09-01 |
| KB-STOCK-12 | 接力溢价一票否决（赚钱效应可否决热度） | ✅ | 2026-08-29 |
| KB-STOCK-13 | 强度四档（领涨/强势/活跃/观察） | ✅ | 2026-08-29 |
| KB-STOCK-14 | 放量破位不出低吸（不接飞刀） | ✅ | 2026-08-30 |
| KB-STOCK-15 | 做T 五指标权重（均价/量价/量能/新高/换手） | ✅ | 2026-08-30 |
| KB-STOCK-16 | 题材唯一归属（一只票只归属一个主题材） | ✅ | 2026-08-29 |
| KB-STOCK-17 | D1 四问预判验证（成立/人气/梯队/时效） | ✅ | 2026-08-30 |
| KB-STOCK-18 | 预判以实测为准（人气数据不迁就印象） | ✅ | 2026-08-30 |
| KB-STOCK-19 | 组合策略框架（稳定核+灵活壳+LLM 验证） | ⏳ | 2026-09-09 |
| KB-STOCK-20 | 风格情绪周期（业绩披露期 vs 空窗期炒小炒差） | 🔶 | 2026-09-09 |
| KB-STOCK-21 | 一字板参与纪律：选对≠能买，开板重评 | ✅ | 2026-09-09 |
| KB-STOCK-22 | 弱转强波浪识别（二浪启动持有） | ✅ | 2026-09-09 |
| KB-STOCK-23 | 趋势转龙头连板成因模型（百大案例六维+规律 a-e） | 🔶 | 2026-09-09 |
| KB-STOCK-24 | 缩量的正确语义：不是没人买，而是没人卖（含主力潜伏四步信号链） | 🔶 | 2026-09-09 |
| KB-STOCK-25 | 题材辨识度记忆效应：消息一来先拉同题材"熟脸"（历史龙头优先异动） | ⏳ | 2026-09-09 |
| KB-STOCK-26 | 消息驱动选股五段流水线（分级→映射→验证→情绪→推送）+ 通知只发四类信号 | ✅ | 2026-09-10 |
| KB-STOCK-27 | 三倍量战法：规则化 + 参数口径 + **实测否决**（近 250 日负期望、胜率 33.5%/35.9% vs 市场 46.8%）+ 形态策略验证五步法 | ✅ 已测否 | 2026-09-10 |
| KB-STOCK-28 | 策略生命周期：海龟失效的三条结构性原因（算法化/趋势衰减/拥挤）+ 登记→验证→监控→处置四环缺口 | ✅ | 2026-09-10 |
| KB-STOCK-29 | 「两点半五步法」规则化 + 实测：原版显著负超额（T+5 −0.51%/年度 2/11）；内含候选B（涨幅3–5%+跌破MA5+大盘涨）11/11 年正超额·**待样本外复验** | ✅ 已测否（原版） | 2026-09-10 |
| KB-STOCK-30 | 候选B 样本外盲测：方向成立（方向一致性 91%）但**分布右偏**（均值 +1.33%、中位 −0.08%、跑赢比例 49.3%）⇒ 观察项，不接入。**09-13 分层续**：正超额集中**小盘<30亿**（T+5 中性 +1.12pp/62.5%，大盘层负）⇒「候选B ∩ 小盘<30亿」强化版观察项（KB-STOCK-36③，事后切分需未来数据验证） | ✅ 已复验+分层 | 2026-09-10 |
| KB-STOCK-31 | 尾险规则（停牌核查红线）的正当性来自**安全不对称性**、不来自均值：红线组均值不显著（t=−0.20）、剔除后回撤反而变差（−21.6%→−28.6%），但停牌=资金锁死不可撤单 ⇒ 硬排除。**方法学：当日封板买不到 ⇒ 必须双口径**（收盘口径把连板溢价算成"收盘价幻觉"，翻转结论）；Y3(50–80%) 分年度 10/11 年是最稳健信号 | ✅ 已校准 | 2026-09-11 |
| KB-STOCK-32 | 量价拆解 10y 复刻：方向效应/倒 U/vratio RankIC −0.0474 过门槛 + placebo 哨兵 0/50；突破确认**双口径终判**——日线近似=反选、盘中量比≥2=正选择（15,301 事件单调），**常量 2.0 保留 ⇒ 执行时点是量能信号语义的一部分**；score_capital 量比改倒 U；五维同源证伪（P2-33 全闭环） | ✅ 已实证 | 2026-09-13 |
| KB-STOCK-33 | TradingView 四个流行指标主张可成交口径检验：EMA200/ADX≥25 **反转**、MACD 零轴增益抹平、三重共振样本过薄——**「强势过滤的表观溢价聚集在买不到的样本里」第三次复现**（KB-STOCK-31 纪律）；ADX/EMA200 不引入、零轴保留、三重共振列观察 | ✅ 已测否 | 2026-09-13 |
| KB-STOCK-34 | 产业链「卡点/稀缺层」排序方法论（serenity-skill 提炼）：**先排稀缺环节、再排公司** + 证据阶梯（弱证据不得单独支撑头部候选）；定位假设生成器，因子化落地须先过 `strategy_verify` 实证（P2-34） | ⏳ 方法论已沉淀 | 2026-09-13 |
| KB-STOCK-35 | 板块异动自主发现地基：全市场快照×官方成分可重建任意历史板块分时（09-11 MLCC/PCB 实测）+ 归因四路只做组装 + 自主发现/跟踪状态机**分权不是替代**；「二次发动=持续」属候选假设待实证（P2-37） | ✅ 一期已落地 | 2026-09-13 |
| KB-STOCK-36 | **条件化审计纪律**：否证结论必须声明「测过的层/未测的层」（无条件均值禁作终态）+ 对照篮子化（单票两向都骗：实测 26pp↔0.91pp / 0.30↔0.97）+ 「测不了≠不存在」域声明；346 常量盘点 → 盲区审计表（P2-38，P0 实锤 2 处统一阈值无板块映射） | ✅ 纪律已立 | 2026-09-13 |

## 仓库追踪（05-repo-tracker.md，agent 分组台账）

| ID | 一句话 | 状态 | 来源日 |
|---|---|---|---|
| KB-REPO-* | agent 分组 10 仓台账 + 筛选经验 + 发现日志（周度发现轮数据源；09-13 起含用户供仓批次） | 🔶 | 2026-09-09 |

## 交易教训（02-trading-lessons.md，KB-TRADE）

| ID | 一句话 | 状态 | 来源日 |
|---|---|---|---|
| KB-TRADE-01 | 会话 current_time 不可信，一律 date 实测 | ✅ | 2026-08-29 |
| KB-TRADE-02 | UTC/北京跨日：日期源必须同源（午夜测试炸） | ✅ | 2026-09-09 |
| KB-TRADE-03 | 内存快照盘后重启为空，端点必须有 parquet/实时兜底 | ✅ | 2026-09-08 |
| KB-TRADE-04 | 同名字段异源异单位，统一走单一数据源 | ✅ | 2026-09-08 |
| KB-TRADE-05 | 三态纪律：unknown 显式、缺失 --、文案单点收口 | ✅ | 2026-08-29 |
| KB-TRADE-06 | 验收以实际渲染为准（agent-browser 文本通道） | ✅ | 2026-08-29 |
| KB-TRADE-07 | 盘点先 grep 再信文档；跨日验日期；跨口径不相加 | ✅ | 2026-08-30 |
| KB-TRADE-08 | 空池=合法行情（composite 空即失败误报跌停池） | ✅ | 2026-09-09 |
| KB-TRADE-09 | 瞬态状态不入异常白名单（connecting 防抖） | ✅ | 2026-09-09 |
| KB-TRADE-10 | 归因误判：情绪面独立题材无共振时 certainty 会高估 | ✅ | 2026-09-09 |
| KB-TRADE-11 | 筛选宁缺毋滥：cert=unknown/低绝不入跟踪池 | ✅ | 2026-09-09 |
| KB-TRADE-12 | 清除类操作用时间切线而非 id 枚举（完备性） | ✅ | 2026-09-09 |
| KB-TRADE-13 | 归因类别×胜率周度统计（改进闭环） | ⏳ | 2026-09-09 |

## 工程教训（03-engineering.md，KB-ENG）

> **文件分工（2026-09-12，按「缺陷发生在哪一层」拆为四册）**：
> `08-tooling-pitfalls.md` = **操作层**（KB-ENG-01~15，每条 3~5 行，怎么用工具）；
> `09-verification-pitfalls.md` = **验证层**（测试 / 门禁 / CI / 环境 / 防线有效性的验证）；
> `10-data-contract-pitfalls.md` = **数据契约层**（写入 / 去重 / 传输 / 时间 / 质量门）；
> `03-engineering.md` = **应用与设计层**（架构与接口设计、口径判据、前端布局、研究方法论）。
> **本表是全序列的唯一登记处，按 tag 检索，不分文件**——查条目不需要知道它在哪册。
> 拆分依据 `07-doc-curation.md` §5.2 条件①（`03-engineering.md` 反复撞 800 行文件级阈值：
> 9 月内两次撞顶后于 09-12 拆出 `08`，同日再拆出 `09`+`10`）。
> 拆分只搬位置、**正文逐字节未改**（拆后以哈希逐条比对原件校验）。

| ID | 一句话 | 状态 | 来源日 |
|---|---|---|---|
| KB-ENG-01 | 同文件多 Edit 并行必丢（last-write-wins） | ✅ | 2026-08-30 |
| KB-ENG-02 | zsh heredoc 陷阱（= 展开 / ${} 替换） | ✅ | 2026-09-08 |
| KB-ENG-03 | 后台 Bash：cwd 不继承；内联 & 被回收 | ✅ | 2026-09-08 |
| KB-ENG-04 | shell grep 假阴性 → 用 Grep 工具 | ✅ | 2026-08-31 |
| KB-ENG-05 | 测试：禁 /tmp 固定路径；exec 切片静默失效；fixture 随契约同步 | ✅ | 2026-08-31 |
| KB-ENG-06 | agent-browser 坑：合成事件不触发 React；input 原生 setter | ✅ | 2026-09-02 |
| KB-ENG-07 | 新表必须 Alembic 迁移；Base 在 models.watchlist | ✅ | 2026-08-30 |
| KB-ENG-08 | SQLite datetime 过滤用对象；get_session_factory 返回 sessionmaker | ✅ | 2026-09-04 |
| KB-ENG-09 | 接口契约变更必须跑端点冒烟 | ✅ | 2026-09-09 |
| KB-ENG-10 | JSX 批量替换漏闭标签；import 插入用 Edit 手工 | ✅ | 2026-09-09 |
| KB-ENG-11 | curl 中文查询串必须 --data-urlencode | ✅ | 2026-09-09 |
| KB-ENG-12 | 前端 dev server 会夜间死亡，晨检 lsof 3000 | ✅ | 2026-09-09 |
| KB-ENG-13 | 过滤后数组是唯一渲染源（空态与列表同源） | ✅ | 2026-09-09 |
| KB-ENG-14 | /tmp 启动脚本会被系统清理，需可重建 | ✅ | 2026-09-08 |
| KB-ENG-15 | memory 日志写入用绝对路径 | ✅ | 2026-09-08 |
| KB-ENG-16 | flex 高度链逐层接力，中间层块级=overflow 全失效 | ✅ | 2026-09-09 |
| KB-ENG-17 | lark-cli `--content` 必须是 JSON；validation≠鉴权失败，勿误重试 | ✅ | 2026-09-09 |
| KB-ENG-18 | ❌ 已被 KB-ENG-20 取代（真因=fileConfig massacre，非级别） | ❌ | 2026-09-09 |
| KB-ENG-19 | 测试写真实仓库路径：_EVOLUTION_DIR 等文件口必须随 DB 一并隔离 | ✅ | 2026-09-09 |
| KB-ENG-20 | 程序化 alembic fileConfig 禁用全部既有 logger（议程零日志主因） | ✅ | 2026-09-09 |
| KB-ENG-21 | 调度器三重防护：补缺失全局 + skip 原因节流日志 + liveness 暴露 | ✅ | 2026-09-09 |
| KB-ENG-22 | 下游过滤依赖的字段必须在**写入侧**落齐（告警快照漏 `name` 静默丢全部） | ✅ | 2026-09-10 |
| KB-ENG-23 | 队列消费先「过滤未处理」再 limit，绝不先 limit 再过滤（否则旧条目永久挤出） | ✅ | 2026-09-10 |
| KB-ENG-24 | 阈值先量噪音比 + 下游预算，别只看触发量（97% 被忽略还挤掉高价值信号） | ✅ | 2026-09-10 |
| KB-ENG-25 | 下游绕道别的列表按 symbol 匹配 = 契约缺失的假解法（真身须在产生层导出） | ✅ | 2026-09-10 |
| KB-ENG-26 | 补全逻辑写在某个路由里 = 同组件被两数据源喂出两种样子（多份即漂移） | ✅ | 2026-09-10 |
| KB-ENG-27 | 判断两组件该不该合并：先看输入契约能否归一，再看渲染分支数（互补 ≠ 重复；合并卡片 ≠ 合并分区） | ✅ | 2026-09-10 |
| KB-ENG-28 | 用户说「没有 X」先分三类：数据没产生 / 有数据没标签 / 标签语义漂移 | ✅ | 2026-09-10 |
| KB-ENG-29 | 全局门槛跨模块复用前先验量纲：回放分（二维重建）≠ 线上分（六维综合） | ✅ | 2026-09-10 |
| KB-ENG-30 | 名额类常量要区分「上限（capacity）」与「目标（target）」：只该有上限 | ✅ | 2026-09-10 |
| KB-ENG-31 | 多来源拼装的行必须按**唯一键**去重；「撞约束」的兜底不能把「并发重复」与「新行非法」合并成一类 | ✅ | 2026-09-10 |
| KB-ENG-32 | 档位标签（level）是**计数产物**，不能当语义判据用（细则另写判据 + 结论算在产生点） | ✅ | 2026-09-10 |
| KB-ENG-33 | 调度链一端传错类型 = 静默数周的数据陈旧（异常被 except 收成日志；须有观测调度自身的手段） | ✅ | 2026-09-10 |
| KB-ENG-34 | `describe()` 型接口先确认它描述的是「谁」的位置，再拿来当「今天」 | ✅ | 2026-09-10 |
| KB-ENG-35 | 运行时覆盖：签名默认值不可绑定常量；`None` 已有语义时上哨兵；meta 写生效值 | ✅ | 2026-09-10 |
| KB-ENG-36 | 归档不是摘要：可执行清单必须整表搬或留原文指针（压成概括 = 丢失 N 个待办） | ✅ | 2026-09-10 |
| KB-ENG-37 | HTTP 200 ≠ 契约满足：`.json().catch(()=>({}))` 把「响应非 Envelope」静默变成 undefined；`res.ok` 只证明传输成功 | ✅ | 2026-09-10 |
| KB-ENG-38 | 「滚不动」先查滚动归属：`shrink-0` + 祖先 `overflow-hidden` = 静默裁掉；诊断读几何（scrollHeight/clientHeight/overflowY 链） | ✅ | 2026-09-10 |
| KB-ENG-39 | 战法核验器（`app/research/strategy_verify.py`）+ 四条方法论教训：t值≠效应量 / 市场中性须按日对齐 / 均值要配中位与跑赢比例 / 空分组须显式 0 | ✅ | 2026-09-10 |
| KB-ENG-40 | 代码里的「示例固化」：**"词→类目"是索引可枚举，"类目→强弱"是结论须验证**；处置三选一不得悬空；代码固化内容须与 KB 状态一致 | ✅ | 2026-09-10 |
| KB-ENG-41 | 登记层三个漂移点：**注册表一物二用**（可创建集合 = handler 注册表本身）／**待办必须有出口**（queued=等待执行 vs needs_confirm=等待处置）／**枚举里存在 ≠ 已实现**（纸面状态）；登记类两范式 record_mutation / record_escalation | ✅ | 2026-09-10 |
| KB-ENG-42 | **模块有测试 ≠ 接线有测试**：完成度 = 消费方（谁调用/何时/给谁看），库级测试只证明「算得对」；新模块验收必须含一条真实调用链 + 调用方契约断言 | ✅ | 2026-09-11 |
| KB-ENG-43 | 用「盘后批处理」替代「盘中实时采集」的四个前提：**前缀稳定 / 幂等标记（失败与无数据都不写）/ 来源唯一不回落（台账空即跳过）/ 跳过原因具名**；口径干净 > 覆盖率高 | ✅ | 2026-09-11 |
| KB-ENG-44 | 实证核验脚本必须 **`import` 生产模块**（口径单点收口，核验的才是线上规则）＋ **常设「自检行」**（分组计数 + \|Δ\| 中位数，专抓「全判同一档」的静默失效）；只跑相关性不够，须直接调 `evaluate()` 替线上规则走一遍；分年度一致性是廉价稳健性检验；**核验的价值在敢删**（美债 10Y 实测否决 → 权重置 0） | ✅ | 2026-09-11 |
| KB-ENG-45 | 质量门的「主键」必须与数据**真实语义**同源：**同一除权日可有多个合法事件**（派现+送转同日），只按 `(代码, 日期)` 分组会**恒定误报**——一个每天都红的门等于没有门（告警疲劳 + 掩盖真故障）。正解 = **全字段自然键**放行合法多事件、仍捕获真重复，**且**入库侧 `SELECT DISTINCT` 根治（只改检查 = 静默掉真问题）；重复行对复权窗口乘积会被**重复计数**（实测过复权 28.6%），去重前先算数值影响 | ✅ | 2026-09-11 |
| KB-ENG-46 | 被 `except` 吞掉的「守卫」是**死代码**：`_calendar_days_ms` 调 `async def trading_days(provider,…)` **缺 provider 且未 await** ⇒ 必然 TypeError ⇒ 被吞 ⇒ 滞后门槛**从未生效**（日志看着完全正常）。防线三条：测试**直接调它**断言非空且语义正确（不是"不抛异常"）／源码级反漂移断言／修完要**外部可观测证据**（日志真的打出那个量）。判据：凡守卫类代码先问**「它失败时会怎样」** | ✅ | 2026-09-11 |
| KB-ENG-47 | **门禁工具本身也会假阴性**：`pyproject.toml` 已含 `-q` 而命令行再加 `-q` ⇒ `-qq` 抑制统计行（"N passed" 从不出现）；`.pytest_cache` 的 `lastfailed`/`nodeids` **跨轮累积且不清理消失的 nodeid** ⇒ 缓存内容 ≠ 本次结果。纪律：判失败只认**退出码 + 进度字符 `F`/`E`**；判 nodeid 是否存在**必须 grep 源码**。判据：受限/陈旧造成的假阴性不能当事实 | ✅ | 2026-09-11 |
| KB-ENG-48 | 口径/披露类信息必须放在**所有提前 `return` 之外**：空池那句 `if (items.length===0) return` 会把整段陈旧披露吞掉 ⇒「今日无票」被静默升级成「基于 6 天前数据的无票」。披露描述的是**整块数据的可信度**，与"有没有内容"正交 ⇒ 顺序恒为 `披露 → {空 ? 空态 : 内容}`。验收必须显式覆盖**「空 ∩ 异常」交叉格子**（只测两个角落 = 没测）。<br>**续（2026-09-14，用户报障「两市成交额怎么没出来了」）**：同一条纪律在**字段级**的形态——展示层写 `{v ? render(v) : "--"}`，把 `null` 的两种成因（**上游尚未就绪** / **真的没有数据**）压成同一个 `--` ⇒ 用户**无从分辨"在加载"还是"坏了"**，是用户来问才发现（界面自身不提供分辨依据）。⇒ **`--` 只应表示明确判定为缺失**，"尚未就绪"必须另有文案（「加载中…」）；**成因必须随值一起传递**——本仓早有 S2-1 `Freshness`（`{state, reason}`）**该复用却没复用** | ✅ | 2026-09-14 |
| KB-ENG-49 | **「关于自身能力的事实断言」必须单一真相源 + 双向守卫**（AI 认知漂移三类失效点）：①提示词自我否认（无工具时代文案接入工具后没改 ⇒ 与工具清单直接冲突，模型信了"你没有"那句）②工具覆盖缺口（端点早有、助手没登记，17→26）③系统地图漂移（仍指向已 302 下线的 /picks /research）。纪律：能力清单只写一遍、其余派生；守卫测试双向对齐（含**关掉工具时必须恢复诚实的"没有数据"**，否则翻车成自我夸大）；穷举不了就把「没调工具却声称没有数据」记成 warning ⇒ 日志即自动产出的缺口清单（调过工具再说没有 = 真取不到，不算）。**验收时立刻复现同类第 4 例**（"没有新闻数据源"）⇒ 光修措辞不够，清单漏项照样触发 | ✅ | 2026-09-11 |
| KB-ENG-50 | **时间一律先转 epoch 再比大小**——不同格式/时区的字符串比较是静默错判。实例：已读水位被写成 `toISOString()`（UTC 带 T）却与后端 `ts`（北京 naive 带空格）字面比较 ⇒ `' '(0x20) < 'T'(0x54)` 使**当天条目恒判已读**，换格式/跨日又整天一起计入 ⇒ 用户看到"计数在旧累积上累加"。纪律：跨源时间先归一为 epoch；同一语义只许一种写入格式（两个写法必漂移）；北京 naive **不能直接 `Date.parse`**（会按本地时区解释）⇒ `Date.UTC(y,m-1,d,H-8)`；解析失败返回 **null 不返回 0**；迁移旧值顺带修正历史错值。**验收位**：水位设 12:30 → 徽标须恰为之后条数（实测 1），不是 66 也不是 0。**隐含前提**：红点需求 = "打开 ≠ 已读"（原实现打开即全部已读，红点会一闪即逝） | ✅ | 2026-09-11 |
| KB-ENG-51 | 消 N+1 的「批量替换单查」**三个等价前提**：①**定序**（单查 `ORDER BY symbol` vs 批量无排序 = DB 扫描序 ⇒ 有 `[:200]` 截断时取到的是**另外 200 只**，数值看着全对、集合却不同且随插入序漂移；排序必须由批量实现自带）②**截断语义**（1 次全量查询换 N 次小查询，用内存/耗时换查询数，须实测净收益）③**缓存键不许依赖查库结果**（key 依赖"有成分的题材集合"⇒ 必须先查库才能算 key ⇒ 缓存判断被结构性推后，命中白付全部查询；key 只能依赖请求参数）。验收：`before_cursor_execute` 计数真实查询数 + 旧新两口径**逐元素含顺序**断言 + 线上冷热响应体逐字节比对。实测 391 次/541ms → 2 次/142ms，热路径 2.7ms | ✅ | 2026-09-11 |
| KB-ENG-52 | 三条"看着无关"的静默陷阱：①**JSDoc 里 `**43/83/87/88**/92` 会提前闭合块注释**（`**/` 含 `*/`）⇒ 症状是打包器 transform 报 "Try inserting a semicolon here"、测试显示 `Tests no tests` 像"测试全丢了"；注释里别用 `**` 包含斜杠的列表 ②**测试不许依赖「表恰好还没建」这类副作用**，否则通过与否取决于执行顺序（实例：`_sf` 用真实 sessionmaker + 只 patch 全局工厂 ⇒ 只拦到一路；单跑 2 条降级 / 全量 1 条）⇒ **「单跑绿、全量红」先查测试对共享状态的隐式依赖**，修法是每条路径显式注入失败并做混序复跑 ③**pyflakes 不认 `# noqa: F401`**（那是 flake8 语法）⇒ "导入即副作用"会被报 unused；改法：顺手断言该名字（如 `assert RealTrade.__tablename__ == "real_trade"`） | ✅ | 2026-09-11 |
| KB-ENG-53 | 测试与前端环境的六条**静默失败**（共同形态 = 无错误、无告警、结果看着正常）：①pytest 必带 `--basetemp`，否则沙箱拒建默认临时目录 ⇒ 几十个 **E** 而非 F，**E 成簇在开头先怀疑环境** ②SQLite datetime 过滤必须传 datetime 对象（库里是空格格式，与 `…T00:00:00` 字面比较**恒 False** ⇒ 防线静默失效，统一走 `_utc_cutoff_today()`）③滚动加载哨兵必须挂在**滚动容器内部**（放外面则 `getBoundingClientRect` 恒在视口内 ⇒ 首帧补载后**永久卡第 2 页**）④列表项 memo 化缺 `useCallback` 稳定回调或 `useMemo` 稳引用任一即全失效 ⑤复用既有抽象前先读依赖语义（`usePollingFetch` 只在 `intervalMs` 变时重跑 ⇒ 参数会变的取数必须传第三参 key）⑥扫「未引用导出」必须把测试纳入扫描，否则删掉只被测试引用的导出 | ✅ | 2026-09-11 |
| KB-ENG-54 | 加了一层「守卫 / 上界」后**真正的风险是没覆盖到的那些路径**：①S1-4 涨跌停守卫只挂在 `place_order`、`match_pending`（第二条成交路径）裸奔 ⇒ 股价冲上涨停后挂单仍按现价成交；**同族写法** `if quote.limit_up_price and ...` 在缺值时整体短路 = 把「判不了」当「不需要判」②S2-3 请求预算给 `deadline` **加了默认值 `None`**、4 处调用点漏改 1 处 ⇒ 该路径整条绕过预算（静默跑满 30s）。纪律：收尾必答三问——grep 出**全部**调用点 / 测试钉住**最常走的那条** / 保护触发必须有**可见出口**；**修法是让漏传在开发期报错（必填参数），不是补这一处** | ✅ | 2026-09-11 |
| KB-ENG-55 | **判据的口径比判据本身更要命**：照文档写对了条款名与阈值、却取错口径 ⇒ 在真实分布下**近似恒真**，闸门形同虚设且无报错。实例：`gate_verdict` 用**原始**中位/胜率（+3.33%/68.1%）当准入判据 ⇒ 候选B 被判 pass；换**市场中性**口径是 −0.08%/49.3% ⇒ 应为 observe。**上涨市里原始胜率天然 >50%**。同族：`PROMO_FLOOR=30%` 落在分布之外。修法：只接受中性口径，拿不到就记 `unchecked` **跳过**，绝不换口径顶替（没验 ≠ 验过）。纪律三问：归一化到什么基准／在历史分布里是否恒真／拿不到时怎么办。判据：**从未被触发过的判据先怀疑它恒真**。连带：结论只写数字不写口径 ⇒ 重跑必然对不上 | ✅ | 2026-09-11 |
| KB-ENG-56 | **真实运行日驱动的断言只在特定日历日变红 ⇒「一周 5/7 是绿的」等于没写守卫**。实例（2026-09-12 周六跑全量报 2 failed，根因同源）：①`assert 日历末元素 >= 今天`（日历只装交易日，末元素必为周五）②`assert real_days[-1] >= 今天` 且受控时钟取真实今天 ⇒ **每个周末与长假必挂**。**产品口径是对的，断言写宽了**：把"日历覆盖交易日"写成了"日历覆盖今天"，且把正交的"运行日"当成"测试夹具"。修法两条（不碰产品逻辑）：①基准日做成**可注入参数**（`today=None` 才取 `now()`，范式照抄既有的 `trading_day_lag(latest, asof)` / `freshness(asof=)`）——附带修掉"测试与实现各取一次 `now()`"的**跨零点竞态**；②夹具锚到**日历里真实存在的交易日**（`real_days[-1]`）并断言选它的理由。必配定点回归：周六→周五／**周日→同样周五**／工作日节假日**刻意**被补上（若要改必须连 `freshness` 一起改）／**补负向用例**（"非交易日必须不触发"此前无人守）。**反哺**：[[KB-ENG-46]] 的处方「断言末元素等于今天」**就是缺陷来源**，已就地订正——错误处方会被一字不差地抄进测试 | ✅ | 2026-09-12 |
| KB-ENG-57 | **CI 环境差异三件套**（时区=进程本地≠北京口径／gitignored 大文件缺位／测试执行顺序）：依赖装不上的 job = 测试从未被 CI 验证过。显示与日期归属显式北京（BJ_TZ 族）；大文件缺位必须有显式降级态（不 500、不空池冒充）；复现通道 `TZ=UTC pytest <files>` | ✅ | 2026-09-12 |
| KB-ENG-58 | **口径类常量/档位改动三步**：①**先量影响面**（看似边角的常量实测覆盖近半样本；用生产函数跑满既有样本，不自造近似）②**把「常量项」边界推导到精确**（同相位同标志取值相同 ⇒ 只翻转跨组倾斜、不改组内次序；「会改变排序结果」是不可判的过宽表述）③**档位/阈值也是口径的一部分**（只 import 判定函数、没对齐 `resolve_bands()` 校准层 ⇒ 同一函数同一输入仍算出失真结果）。配套：改后留**来源同一性 + 源码级**断言（只断言常量存在 = 假守卫）、阻塞点具名、结论给**双口径** | ✅ | 2026-09-12 |
| KB-ENG-59 | **批量改写必须区分「注释」与「运行期字符串」，且改完必须重跑全量门禁**。事故：清理注释死引用的正则**同时命中运行期字符串**（`_NFP_BASIS` 会渲给用户看），提交信息却写「无行为变更」且**跳过 pytest** ⇒ 1 项断言静默变红。判据：①改写后**审计命中行是否为代码行**（不以 `#`/`//`/`*` 开头即可疑），别信「正则只会碰注释」②「无行为变更」要有全绿证据，**声称注释级改动更不能跳过测试**③用户可见字符串里的文档指针，正解是**断言其可解析**（正则取出路径再 `exists()`），不是写死新路径 | ✅ | 2026-09-12 |
| KB-ENG-60 | **门禁的「扫描面」也是口径**：写窄的 glob 会让**新对象根本不进检查**，而输出与通过完全一样（全绿）。实例：`doc-health` 用 `glob("0[1-9]*.md")` 只覆盖 01~09，新建 `10-*.md` 实测零检查。同族：B 只扫 `*.md`（漏代码指针 → 补 F）/ F 只认显式 `docs/` 路径（漏裸名 → 补 F2）。修法：①扫描面收成**一个入口函数**②配**覆盖守卫**（没被任何检查覆盖即 FAIL，"新增即自动纳入"不靠人记得改）③门禁触发**先量假阳性再定档**（F3 宽版 23 命中/0 真阳性 ⇒ 否决，留窄版 1/0）。判据：**「没进检查」与「检查通过」不可区分**；新增对象前先问「它会自动被扫到吗」。配套：**拆分/搬家用哈希逐条比对原件**（只搬不重写才使得核对可执行） | ✅ | 2026-09-12 |
| KB-ENG-61 | **「长期用户状态」不能只存浏览器本地存储**（按 **origin** 命名空间：实测 localhost 侧点过「全部已读」→ 切 127.0.0.1 徽标立刻回到 **65**；换端口/profile/清站点数据同理）⇒ 前端存储只能当**首帧缓存**，权威副本落服务端。同源第二坑：**解析路径不许偷偷依赖浏览器时区**——`parseTs` 正则只收 1–3 位小数，而 alert 的 `ts` 带 **6 位微秒** ⇒ 静默落到被 [[KB-ENG-50]] 明令禁止的 `Date.parse` 兜底（本机 delta=0 只因恰好是 `Asia/Shanghai`）。修法：服务端权威 + **前后端同一套单调合并**（水位取大 / 清除水位取大 / id 取并集；覆盖式双向都会让已读变回未读，合并在服务端还能消解并发 PUT）+ hydration 未完成不回写；正则收 1–9 位小数并截断到毫秒。判据：**徽标等于 payload 总数 = 状态缺失**（≠"多算几条"） | ✅ | 2026-09-12 |
| KB-ENG-63 | **测试「间接」写真实数据文件**：写入口不在被测对象的调用栈里 ⇒ 覆盖率再高也照不出（该模块在 tests 下零引用）。事故：`leader_archive.json` 被 **MockProvider 覆盖**（`symbols_seen=6`=mock 的 `UNIVERSE[:6]`、`days_with_pool=30`=mock 对任何日历日都给池；⚠️ `themes={}` **不是**判据——09-11 真实构建同样空题材）、`cognition_gaps.jsonl` 被追加 39 行**全是同一条测试问题**。触发器 = **扫荡式测试**（openapi 遍历 **132 个 GET 端点**，`/api/events/impact`·`/api/picks/leader-archive` → `get_archive()` 落盘）。**判据：内容取证 > mtime 推断，且只认与数据源实现强绑定的量**；影响面用 `find backend/data -type f -newermt <运行时刻>` 实测（⚠️ BSD `stat -f "%m"` **不是** epoch 秒），对照"临时停用隔离跑全量 ⇒ 恰好 2 个文件被写"。修法：**conftest 单点改指沙箱**（沿用 `REPORT_DIR` 先例；**不做无差别重定向**，同类常量有的会被读）。守卫三层（AST 清点登记 / 注入校验 / 功能回归）+ **⚠️ 新增守卫必须做「注入验证」**：让被守的东西失效，确认它变红——首版守卫因 `parents[]` 层级差（`tests/` 是 `parents[1]`、`app/` 模块是 `parents[2]`）**取成了仓库根，全绿而根本没在守**。副产品：`leader_archive.json` 补齐 `.gitignore`（运行时状态文件，与 `trade_calendar.json` 同族，`git rm --cached` 但保留磁盘文件） | ✅ | 2026-09-12 |
| KB-ENG-62 | **安装型自动化（launchd / cron）的「装了 ≠ 在跑」**：`com.ashare.review`（16:30 独立盘后复盘）自 09-09 起**已加载、触发 4 次、全部失败、零产出**，而文档一直写作已落地。根因 = 项目在 `~/Desktop`（macOS **TCC 保护目录**，`drwx------+`），launchd 进程无授权 ⇒ `Operation not permitted`（**EPERM**，非 EACCES）⇒ **exit 126**。三条伪证据：「有执行位」「plist 在 LaunchAgents」「日志里有成功字样」（那是手动跑的那次）。**验活三问**：① `launchctl print gui/$UID/<label>` 读 **`runs` + `last exit code`**（`state=not running` 不能推出"跑过且成功"）② ⚠️ **别用 `launchctl list \| grep` 判"未加载"**（本轮据此误判，job 实为已加载）③ 读**产出目录**最新文件日期。**达标判据 = `runs` 递增 + `last exit code = 0` + 产出目录有新文件** 三者齐备。退出码：`126` 不可执行 / `127` 找不到（PATH）/ `78` 配置错。**零成本直证**：`launchctl kickstart -k` 触发后三项联读——退出码变但日志 mtime 不变 ⇒ **脚本零执行**（权限层）；日志有新增 ⇒ 失败在更深处。**同类面**：能进 `SchedulerRegistry`（`GET /api/system/schedulers`）就不要另起独立机制。**处置（2026-09-12 裁定 B）**：独立通道已拆除（bootout + 安装位 plist 与 `run_review.sh` 等四文件入回收站），**能力由既有 `review-scheduler`（交易日 15:30）承担，未新增调度器**——实测其 09-02~09-11 连续 9 日准点产出 | ✅ | 2026-09-12 |
| KB-ENG-64 | **数值呈现必须按「量的语义」分档**：收益率**带符号**、比率类（回撤/波动率/胜率/盈亏比）**不带**。事故：二者共用一个 `_pct()` 一律加 `+` ⇒ `最大回撤 +16.90%` 读起来是「涨了 16.9%」，**方向恰好反了**；连带胜率显示 `+12.50%`、浮点 `8.461538` 未截断。**为什么没被挡住**：断言写的是 `"最大回撤" in out` —— **子串存在性断言对「值的方向」完全不敏感**，桩数据 + 单测全绿；**真实数据跑一遍才看见**（「验收以实际渲染为准」再次应验：桩数据证明"跑得通"，不证明"读得对"）。修法：按语义分档格式化 + 每档配**方向性断言**（`assert "最大回撤 +" not in out`）并做注入验证。判据：**写数值呈现前先问「这个量有方向吗」；存在性断言 ≠ 正确性断言** | ✅ | 2026-09-12 |
| KB-ENG-65 | **三种「假绿守卫」形态**：①**桩缺方法不是守卫**——批量化后桩缺 `member_symbols_bulk`，`AttributeError` 被 `except Exception` 吞掉 ⇒ 把实现改回旧的逐只 N+1 **照样全绿**（与 `_Store.directions_of` 同坑的变体，隔天复现 ⇒ 是模式非偶发）⇒ 守卫要把**调用计数放进桩**，断言「批量恰一次 + 单查恰零次」**且另有一条"未走异常兜底"**；②**A/B 对照钉不住"两路共用的判据本身失效"**——把共用的 `override_still_active` 改成恒 True，`bulk == single` 照旧成立（两路一起错）⇒ 必须另配**单路绝对结果断言**；③**注入必须让行为可观测地错**——只加 `# INJECTED` 注释没动代码 ≠ 注入；验收 = **精确变红**（只有目标用例失败）+ 复原后 `INJECTED` 计数为 0。失效条件：无桩位 / 无两条等价实现 / 共享可变状态致"多红" | ✅ | 2026-09-12 |
| KB-ENG-66 | **源码级守卫必须区分「调用」与「提到调用的文字」**：给 `test_event_loop_no_block` 扩面时守卫**当场假红**，命中的是**我写在 docstring 里解释这条守卫的那句话**（纯文本扫描把"提及"当"调用"），且失败信息断言「存在未被 `to_thread` 包裹的调用」**与事实不符**——**误报的失败信息比不报更糟**：它会引导后人去删掉那段正确的说明文字。修法三约束（缺一不可）：①用 `tokenize` 而非正则把注释/字符串替换成**等长空白**（正则处理不了 `#` 在字符串里、以及三引号跨行）②**行号必须守恒**——掩码后文本长度会变 ⇒ **行列号在掩码文本上算、回报时取原文那一行**（两套坐标分工）③**"是否已包"的前缀判断也要在骨架上做**，否则字符串里的 `to_thread(` 被当成"已包" ⇒ **漏报**（比误报更危险）；正向 needle 用原文、反向断言只在骨架上做。**第四条假绿形态**（补 [[KB-ENG-65]]）：自证测试 `test_to_thread_inside_a_string_does_not_count_as_wrapping` 首版把字符串放在**另一行**，而前缀比较只看**同行** ⇒ **掩不掩码都通过**（等于没测到）⇒ **「注入了却没变红」的排查顺序：先怀疑测试没覆盖到那条路径，再怀疑守卫不够狠**。失效条件：语法不完整自动退回纯文本扫描（**刻意选严**，严 > 漏）；被守对象无可用 tokenizer 时只能退回正则 | ✅ | 2026-09-12 |
| KB-ENG-68 | **「文档能力失真」先分「可判子集 / 语义不可配对」两类**：四个原型里**两个宽口径实测否决**——v1 句级共现 66+40 命中、真阳性≈0（把"锚点"与"断言"**错误配对**：一句里两个锚点真值相反；账本占 47/66，而它的职责就是叙述"曾未做 → 已完成"）；v2 收窄到 2+3 仍近乎全假，**败因是根本性的**——断言的宾语常是**中文子能力**（「禁止交易名单」），而可机械核验的只有它**所在的文件** ⇒ **语义不可配对，不是调参能解决的** ⇒ **判定"文档对某能力的描述是否过时"不可自动对账，不设门禁**；只对**判据零歧义**的子集设门（点名的代码路径还在不在：459 锚点 → 3 处，＋围栏树行 → 4 处，**全真、0 误报**）。三条可迁移纪律：①**先量噪声再定档**是为了判定这条路走不走得通，噪声≈全假时正确处置是**换口径或放弃**、不是继续调 ②**宽口径的否决必须留痕**，否则下一轮有人重新发明同一个宽口径 ③**召回与精确度不对称**——修掉守卫**自身**缺陷（占位名正则吞掉所有 `app/...` 锚点 ⇒ 行内覆盖等于完全失效）后召回恢复真实、又冒出 3 条新命中 ⇒ **"修好后新命中出现"是正常顺序，不是误报证据**。另：**排除规则按"内容性质"实现、不要按"文件身份"实现**（`PROJECT-MASTER.md` 是**混合体裁**：权威结构树必须判、「近期路线」changelog 不该判 ⇒ 文件级代理失效，改为**行级**） | ✅ | 2026-09-12 |
| KB-ENG-67 | **「同步」≠「该搬去线程」**：同一次审计里 `picks_pipeline` 的 8 处同步 IO **搬进 `asyncio.to_thread`**，而 watcher `dispatch_alert` 的 `append_alert`（**读-改-写整个 JSON 简报文件**）**判定不搬**——同步执行时"读→改→写"之间**没有 `await`** ⇒ 对事件循环而言是**一次原子操作**；丢进线程池反而**制造并发**（两条 alert 交错完成读改写 ⇒ **后写者覆盖前写者，丢提醒**）。**"搬线程"在这里是把一个安全操作改成不安全操作。** 判据三项一起看（只看第一项会做出错的改动）：①阻塞时长 ②触发时是否在交易时段（连累 QuoteHub 秒级推送）③**搬走是否引入新风险**。搬前必查两件事：被搬函数**自己新建并使用** session/连接（本仓 `_sf()`/`_db()` 均 per-call）——**持有长生命周期句柄的函数不能整体搬**／该函数**没有已经 `to_thread`**（worker 线程里再开线程是纯浪费）。**副产物：冗余常与阻塞同源**——`store.list_events` 在管线里实测跑**三遍**（候选池题材反查 / regime 的 `ev_texts` / 消息命中索引）⇒ 既白读两遍又三度阻塞，收敛成**取数单点**（与 `limit_up_pool` 的 P2-4 范式同型）；被收敛掉的那段随之变成**纯内存函数**（零 IO，反而**不该**再包 `to_thread`） | ✅ | 2026-09-12 |
| KB-ENG-69 | **「兜底分支必须无条件推进」**：解析/分派循环里**两个各自正确的分支会组合出「零消费死循环」**。事故：自研 Markdown 渲染器的块级循环，表格分支要求「下一行是分隔行」（GFM：无分隔行不成表），兜底段落分支的延续条件却带 `!startsWith("|")` ⇒ **以 `|` 开头却不构成表格的行**两边都不接 ⇒ `buf` 空、`i` 不推进 ⇒ 同步死循环、整页卡死（用户报告「进知识库整个卡住」）。**单测/`tsc`/eslint 全绿照不出**（没有测试喂过这类输入）⇒ 只有端到端打开页面能照出。通例：①兜底分支必须无条件先消费一行（`do...while`），推进要**结构上显然**而非"推导可知"②分派条件与分支体条件互为否定式时，必查「被两边都拒绝」的空档集合③解析不了的降级是**退化为文本**，绝不能是**不推进**④**检测死循环的脚本自身循环也要能退出**（本轮验证脚本第一版忘 `break`，把自己挂死在 137，形态与它要抓的缺陷相同）。触发面系统性：`docs/` 4 份 / 49 行用**空行给同表分组**（在任何严格 GFM 渲染器下同样打碎），且 `kb/00-INDEX.md` 正是面板默认打开件 ⇒ 一进必中。修法两条缺一不可：文档改合法 GFM + 渲染器 `do...while` 兜底。**边界**：兜底只保证不死循环，不成表的行退化为带管道符文本（GFM 正解）；想让它成表须改**文档**而非放宽渲染器（放宽会把相邻两张表吞成一张）。注入验证已知形态：改回 `while` 后回归测试是**挂死**而非变红（同步死循环打断不了），刻意接受 | ✅ | 2026-09-12 |
| KB-ENG-70 | **「本地绿 / CI 红」多半是「判定面 ≠ CI 检出内容」**：J 项判"文件是否存在"用 `os.walk` 现算，而 CI 只检出 git 跟踪文件 ⇒ 本地多出的文件都能"救活"死引用（实测 `os.walk` **10409** vs `git ls-files` **999**，**实际遮盖 1 处**）。**与本仓删除纪律互相削弱**：处置一律进 `.workbuddy/trash/`（gitignored，禁 `rm`）⇒ **越守纪律门禁越假绿**、`test_real_repo_has_no_dead_doc_anchor` 在本地是瞎的。另两条：真阳性属"已处置物"须显式登记 `ANCHOR_ALLOW`；汇总行写死常驻局限会被读成失败原因（实测致排查跑偏）。附：CI 日志取法（`curl -sL` 绕 401、`head_sha` 必须完整 SHA，短 SHA 静默返空）＋**"注入串是另一份名单的子串"致守卫被误判无效** | ✅ | 2026-09-12 |
| KB-ENG-71 | **性能实测的「标签」必须与「被测对象」逐行对账**：错配的标签不报错，却产出**看起来精确的数字**，直接翻转"改不改代码"的结论。实例：基准表标签写 `/api/alerts/events AlertRepository`、lambda 调的却是 `EventStore` ⇒ 记成 8.5ms 并动手包了 `to_thread`；复核实测 `AlertRepository.list_events(50)` 仅 **0.36ms**（差 24 倍）⇒ **两处包裹被回退**。风险面：`X.list_events` 这类**同名方法跨类**（`store.`/`repo.`/`svc.`）肉眼极易放过。处方：标签由 lambda 派生 / 按对象而非端点分组；**每个数字写进注释前再单独测一次该对象**；阈值两侧各留对照行。判据：**"这个数是谁的"与"这个数是多少"同等重要** | ✅ | 2026-09-12 |
| KB-ENG-72 | **源码级守卫的匹配面**：①只写方法名会**误伤同名方法**（`fn="list_events"` 命中 `AlertRepository.list_events` ⇒ `tools.py` 假红，差点去包一个 0.36ms 调用）⇒ 判据**带接收者**（`store.list_events`）；关键性质：`to_thread` 传**函数引用**不含 `(` ⇒ 该字面量只命中裸调用、天然不自match。②不区分 async 上下文会**误伤同步 helper**（`notifications._alert_items` 体内调用合法，违规在调用它的 async 端点，是另一层）⇒ 反向断言加 `async_only`：`ast` 取 async 函数体行号集合、**减去其中嵌套的同步 def**，语法不完整返回 `None` 退回不过滤（严>漏）。**"该不该包"是调用点的属性、不是函数的属性**。注入实测：`events.py` 一处 ⇒ 5 红（5 条登记项共用文件级反向断言，非失灵）；`notifications.py` 一处 ⇒ 精确 1 红 | ✅ | 2026-09-12 |
| KB-ENG-73 | **死代码判定的盲区：装饰器注册对「名字计数」天然不可见**——`_perf_middleware` 被 `@app.middleware("http")` 注册（装饰器行不含函数名），扫描器与人工 spot-check 均判为「零引用可删」。**「名字零引用」≠「无调用方」**；排除清单要按**语义等价类**穷举（路由/中间件/fixture/校验器/事件监听），不是照已知那一种打补丁；**候选 ≠ 死代码**（28 个候选里 2 个实为"该接线"）。自证 13 项随 `scripts/deadcode_scan.py` 入库 | ✅ | 2026-09-13 |
| KB-ENG-74 | **守卫的「驱动形态」决定敏感度**：测试框架/宿主的**外部取消会顺手跑掉同步清理**，从而掩盖「必须**自行**收尾」的缺陷。实例：R24 旧实现 `await writer_task` 下，`TestClient` 形态的 `test_client_disconnect_...` **仍然全绿**——因为 `WebSocketTestSession.__enter__` 依 LIFO 执行 `close(1000)` → `cs.cancel()` → `fut.result()`，`cs.cancel()` 的取消只在下个挂起点抛异常，`finally` 里**同步**的 `hub.unsubscribe` 照常执行 ⇒ 新旧实现无法区分。修法：**两种驱动形态各司其职**——`TestClient` 验端到端契约、**直接驱动路由协程**（`asyncio.wait_for`）才钉得住"自行收尾"。同轮第二发现：收尾含 `await` ⇒ **全部**走会话退出的用例抛 `CancelledError`（清理被整段跳过，生产侧 = 订阅实际未注销）⇒ 固化「收尾零 await」（`gather().add_done_callback` + 同步 unsubscribe）。写守卫前先问「这条用例是被什么驱动结束的」 | ✅ | 2026-09-14 |
| KB-ENG-75 | **门禁必须整仓跑**：局部 `eslint <file>` 通过 ≠ 门禁通过（与 [[KB-ENG-60]] 扫描面同源，此处是**文件面**）。实例：R19 改完只跑了相关测试与 `tsc`，收尾 `npx eslint .` 报 1 error —— `react-hooks/refs` 禁止**渲染期读写 `ref.current`**（React 官方：渲染必须纯）。**lint 报错是设计信号**：正解是把写入挪到**发请求那个 effect 的体内首行**（依赖含 `orderSig` ⇒ 草稿一改即重跑；而请求最少等 300ms 去抖 + 一个往返 ⇒ **任何回包都晚于本次 effect 的同步段**，无窗口），不是 `eslint-disable`。挪完必须复跑该模块的注入验证（R19 `4/4` 仍精确变红） | ✅ | 2026-09-14 |
| KB-ENG-77 | **注入验证的三条硬约束**：①**等宽注入**——注入面必须与语义面等宽，I2 首版只改 `qs[5]-qs[1]`→`qs[5]` 却留下 `if 1 in qs and 5 in qs` 前置过滤 ⇒ 预测集与实测集不等（连前置条件一起注入）②**注入必须先过 `compile()`**——字符串替换可能留下空 `try:` ⇒ 整模块 `SyntaxError` ⇒ **红是假的**（全红看着"守卫极强"）③**崩溃安全三层**——**SIGTERM 默认处置不执行 `finally`**，实测脚本被打断后 `evaluate.py` 被**留在注入态**，发现方式是**复原后按锚点 grep 逐条复验**（脚本自报的"复原复测"根本没跑到）⇒ 启动自愈（残留备份先还原）+ 信号/atexit 还原 + 备份文件即真相源 + 每次注入后立即还原。判据：**变红集合精确等于预测集合**；取失败集用 `--junitxml`（不解析统计行） | ✅ | 2026-09-14 |
| KB-ENG-78 | **统计量的「量纲」必须进字段名与报告标签**：`fwd = close_{T+h}/close_{T+1} − 1` 是**期间收益**（持有 **h−1** 日），却被命名 `q_avg_daily` 并 `×243`。实测：某因子 19 日期间多空 **−4.31%** 报成 **−1048% 年化**（修正 ≈ **−55%**，高估 **19 倍**）；审查用例 2% ⇒ 旧报 486%、正确 ≈25.58%。**危害面**：该量参与分层准入（`LS_ANN_MIN=3%`）⇒ 量纲错直接改准入结论，而**阈值字面完全正确**。第二独立缺陷：`yearly_ls` 只取同年 Q5、不减 Q1（含 beta 的绝对收益当多空收益）。修法五条：**一重量一名**（`q_avg_period`/`q_avg_daily_approx`/`q_avg_ann_simple_pct` + `annualization=simple_linear` 标签）／**可实现性三态**（重叠建仓 ⇒ 缩放值不可实现 ⇒ `implementable_annual_return_pct` 恒 None，不得冒充）／年度 LS = **同年 Q5−Q1**（缺组不产出）／**口径参数必传**（`horizon` + `h−1≥1` 守卫）／**口径变更留痕**（`algo_version` + `verdict_prev` + `recheck=pending`，修复不静默把通过改失败）。测试形态：按 horizon 参数化 + **绝对断言**还原缩放系数（断言与误口径之比恰 = h−1）+ **命名守卫**（`"q_avg_daily" not in q`）+ 容差**按量化步长**取（2 位小数 ⇒ `abs=0.01`，否则 h=20 假红）。判据：**写数值字段前先把它周期的分母写进名字** | ✅ | 2026-09-14 |
| KB-ENG-79 | **`ntile()` 没有 tie-break ⇒ 分层归属与准入门槛不可复现**（连跑 3 次给 3 个结论）。同合成仓、同代码、基线 `dc2cc35` 实测：`imax20` = **PASS/CONDITIONAL/PASS**、`sump20` = CONDITIONAL/PASS/PASS。**定位法**（别停在"不稳定"）：逐层二分到具体字段——`ic_mean` 三次**逐字相同**（−0.8672）而 `q_avg_daily` 每次不同 ⇒ 差异**只在分位组归属**。根因：`ntile(5) OVER (PARTITION BY date_ms ORDER BY f)` **排序键不唯一**，因子值大量并列时（整数索引、计数值）分位切点落在并列值中间 ⇒ 谁进哪组由执行顺序决定 ⇒ 多空差与 `monotonic_pairs` 抖动 ⇒ 门槛「≥3/4 单调对」随之抖动。**为何长期没被发现**：评估离线、无"两次结果必须相同"的守卫；`report.py` 只消费稳的 `ic_mean/icir` ⇒ 运行期消费方看不见。修法：排序键补**唯一 tie-break**（`ORDER BY f, thscode`）——并列怎么分本是任意的，但**必须确定**。判据：**凡分层/分位/分桶 SQL 先问「排序键唯一吗」，验收 = 同输入连跑 ≥3 次结论逐字相等**（写成守卫）。状态：**已定性待拍板，且优先级已升格**（同一天第二次咬人：它**污染了 R17 的验收归因**——`recheck` 里唯一的翻转 `imax20` 触发量 `monotonic_pairs` 与年化公式无关却在两次跑间 3↔2 抖动 ⇒ **凡以「结论变化」为验收判据的改动，底层指标须先可复现**；详见 `09-verification-pitfalls.md` 该条第二个实例），见 `retro-and-gaps.md` §六 | ⏳ 待拍板·阻塞验收 | 2026-09-14 |
| KB-ENG-80 | **验证判据自身的失真会制造假缺口**（与 [[KB-ENG-04]] 同族、更隐蔽：判据是**专为本次结论写的一次性脚本** ⇒ 无他者复核，写判据与下结论是同一人 ⇒ **自造判据 = 自造的假阴性**）。实例：md↔html 无损比对脚本在 **md 侧**用了 HTML 去标签 `strip('<[^>]*>')`，而 **md 是纯文本、其中 `<` 是内容**，一次 strip 吃到下一个 `>` 的大段正文 ⇒ **md 侧凭空变短** ⇒ 反被读成「HTML 丢失 2 处」；修正版又误删**字面量** `*` `|` `\`（`NEXT_PUBLIC_*` / `tests/*.py` / `12 * 3600` / `is_*` / `grep "\|"` / `"\|err\|"`）⇒ **16 处**假缺口。**复核撤销**：渲染器 `md-report-html.py::_inline()` **首行即 `html.escape()`** ⇒ 裸 `<` → `&lt;`，**本无此缺陷**（逐条目实测 **1191/1191 全绿**）。三条判据：①**被比对象是什么格式就用该格式的解析器**（md 侧不得去 HTML 标签、html 侧须先 `unescape`）②**归一化只去真正的标记，字面量原样保留**③**判据必须能变红**（[[KB-ENG-65]]）——本轮更硬一层：**判据还会反过来诱发"修"正确代码**。修法：固化 `.workbuddy/tools/md-html-parity.py`（逐条目命中 + 结构计数 + 时间戳三项，打印第一个断裂点，exit 1；三组注入精确判红），**取代**原先「数 `<li>`/`<table>` 个数」的口径。**（其续篇已独立成条 → [[KB-ENG-81]]：复核一个函数 ≠ 复核一个模块）** | ✅ | 2026-09-14 |
| KB-ENG-81 | **复核一个函数 ≠ 复核一个模块；同一条错误判据用在两侧 = 双向自洽的假绿**（[[KB-ENG-80]] 续篇，因单条 ≤60 行而独立成条）。①「渲染器无此缺陷」的撤销**只对 `_inline()` 成立**，却被推广成整体结论；真缺陷在隔壁 `_split_row()`（表格切分用裸 `split("|")`、不识别转义竖线 `\|`）⇒ `boolean \| null` 被切成两格、该行 **4 个 `<td>` vs 表头 3 个 `<th>`**、`<code>` 未闭合 + 反引号泄露到页面；**靠全量对账 `371/373` 的真阳性才抓到**。② GFM 口径**两轮才对**：**表格单元格内 `\|` 一律还原为 `|`**（GFM 在行内解析前先还原，实测 GitHub 行为），**非表格正文只还原代码段之外的**⇒ 落地 `_cell_inline()` / `_split_cells()` **两侧逐字同口径**。③ 判据盲区：结构计数原只数 `<table>` 个数、**不校验列数**，而“两侧用同一条错误判据”会让逐条比对**双向自洽地通过** ⇒ **新增第 3 项：每表数据行 `<td>` 数须等于表头 `<th>` 数**（注入回退 ⇒ ② 独立判红并直指「表#7：表头 3 列 vs 数据行 4 列」）。三条可迁移判据：①**复核一个函数 ≠ 复核一个模块**（说“X 无此缺陷”前先枚举 X 的**所有**同类入口）②两侧同判据 ⇒ **必须另有一条独立于逐字比对的结构一致性检查** ③修复按被仿对象的**真实规范**（GFM/CommonMark），别凭直觉设计“更安全的”中间口径 | ✅ | 2026-09-14 |
| KB-ENG-82 | **数据路径必须「读写同源」且锚定到文件位置**（2026-09-14 数据目录盘点实测）。**①读写分叉（生产已生效、全静默）**：同一条逻辑路径被两个模块各自定义且锚定方式不同——写侧 `heat_history.HEAT_DIR = REPO_ROOT/"data"/…`（**绝对** ⇒ 仓库根），读侧 `distinctiveness.SKYROCKET_PATH = Path("data/picks/heat/skyrocket.jsonl")`（**裸相对** ⇒ 按进程 CWD）；标准启动（CWD=`backend/`）下指向**不同文件**，而读侧缺文件时 `load_skyrocket_hits` **静默返回 `{}`** ⇒ 权重 20/100 的 `W_SPIKE` 分项**恒 0 且无任何降级标记**。**实测**：写侧 83 个 symbol / 100 次入榜，默认路径命中 **0**，显式指向写侧文件则 **83 / 100**。**②判据按“名字”全局汇总 ⇒ 同名互相豁免**：新守卫把“是否锚定”收敛成全仓名字集合，于是 `SKYROCKET_PATH` 在 `heat_history` 锚定过就把 `distinctiveness` 里**另一份同名未锚定定义一并放行**——**是注入验证把它暴露的**（改回缺陷形态守卫仍全绿，[[KB-ENG-72]] 标准形态）。判据：①**锚定文件位置**（`Path(__file__).resolve().parents[N]/"data"/…`），`Path("data/…")` 的基准是“谁启动的、在哪个目录”，同一代码在 uvicorn/pytest/容器/systemd `WorkingDirectory` 下指向不同文件且两侧都不报错 ②**一条路径只定义一次**，读侧**调用时**取写侧属性，patch 写侧一处即同时移动两端 ③**判定“派生/锚定”必须逐定义**（键是 `<文件>:<常量名>`，不是 `<常量名>`）④**缺数据必须显式披露**（空 `{}` 至少两种成因；范式 `lhb_archived_days`/`marketdb_note`）⑤**死常量是负债**（`distinctiveness.LHB_DIR` 重复定义且无人引用 = 改一处漏一处的温床）| ✅ | 2026-09-14 |
| KB-ENG-83 | **上游故障信号要「结构化」并穿透所有中间转换层；退避按故障性质分档**（2026-09-14，起因即 [[KB-ENG-48]] 续篇的上游成因）。**三重失效点**：①**信号没有结构**——`sina_market` 原只抛 `ProviderError("sina stock count HTTP 456")`，把 456 这个明确的限流码塞进字符串，调用方只能匹配文本 ⇒ 它退化成与"源坏了"同类，共用同一套指数退避（120s → 240s → 300s cap），**实测那两轮都仍在限流窗口内**（13:31:59 / 13:33:59 失败，13:38:04 才成功；而**同一时刻命令行单发 curl 是 200** ⇒ 判的是请求特征/瞬时并发，不是整 IP 封禁）。②**刚建好的类型会在中间层被抹平**——分页抓取走 `asyncio.gather(..., return_exceptions=True)`，其后的循环对每个异常统一 `raise ProviderError(f"sina page fetch failed: {item}")` ⇒ 限流类型到此消失、`run()` 的限流分支**永远命中不了**；**不专门为这一步写守卫的话，代码"看起来已经修了"**。③**`except` 顺序**——子类必须排在 `except Exception` 之前，否则被父类兜住、判据永不生效，而**所有行为用例仍然全绿**（它只是退化成普通失败）⇒ 这类判据只能**静态钉**（AST 读 `Try.handlers` 顺序）。**五条判据**：①故障码里有明确语义的（429 / 456 / 503+Retry-After）**一律单独成类**（消息文案会改、类型不会）②结构化之后**逐个中间层确认它还在不在**（`gather` / 线程池 / 重试装饰器都是"统一转换"高危处）③退避按故障性质分档并**显式写优先级**（限流冷却**渐进式 240 → 480 → 900s 封顶**、**优先于**休市降频 240s——后者是按"上游正常、只是数据静止"设计的，限流期用它＝继续按正常节奏敲上游）④判据抽**纯函数**才可测（`_next_delay(live)`；原先是 `while True` 里的内联分支，只能靠"跑一整天看日志"验证）⑤⚠️**退避档位不是「越大越安全」**——初稿按"防封禁"直觉给**固定 600s**，**被实测推翻**：本环境限流是**间歇**的（观测窗口内失败 5 / 成功 5 交替），**首次失败后 245s 内必恢复**（13:33:59✗→13:38:04✓），固定 600s 会用恢复速度换来**虚假**的封禁保护，把陈旧度 ~4 分钟**恶化到 10 分钟** ⇒ **幅度必须由实测恢复窗口定**，且**样本量必须写出**——⚠️ **本条自身即以 n=2 写过「245s 内必恢复」，当日即被反例推翻**（13:56:59 末次成功之后连续失败，**至 14:14:43 实测 `age` 1064s、`fails=3` 仍在用旧数据**；重试间隔实测 **240s→480s→（900s 待发）** 与渐进档位逐一吻合 ⇒ 反证结构化路径已在生产生效）；🔻 **该反例还暴露本档位固有代价：恢复探测延迟最坏 900s**（旧口径 cap 处仅 300s）⇒ 首档与封顶**都只据 n=2 定，须更长观测后重估**，账本 §6.6 行 23 已降为 **🔶 观察 / 待重估** | ✅ | 2026-09-14 |
| KB-ENG-84 | **「时间新鲜」≠「结论可用」：口径版本是与时间正交的第二维度；复核清单 ⊋ 翻转清单**（2026-09-14，`GOV-001` 落地时**实测**照出）。**事故**：磁盘因子报告产出于 `2026-09-07`、**连 `algo_version` 字段都没有**，而代码口径已是 `2026-09-14.ic-maturity-v3`；但 `freshness()` 因 `age_days=7 < 40` 判 `stale=false`、`ic_evidence()` 同样 `stale=false` ⇒ **旧口径的 PASS 因子以「不陈旧」身份继续送进进化议程**（正是报告 F5 要防的「旧报告继续被当调参依据」）。**根因**：新鲜度只有一个维度（时间），而「这份结论还能不能用」是两个**正交**维度——时间问「多久没更新」，口径问「用哪一版算法算的」（算法一改，数值前提就变了，**哪怕它昨天才跑**）。**四条纪律**：①两个维度**并列成两个字段**，不许合成一个布尔（否则既有消费方的「该重跑了」判定会被口径变更误触发）②口径维度要**三态**：`True` 同源 / `False` 已变更 / **`None` 未声明=未判定**；`None` **不得**塌缩成 `False`、**也不得**当"没问题"放过——实测那份 09-07 报告正是 `None`，按"未声明=未变更"处理它就永远复核不到 ③**复核清单 ⊋ 翻转清单**：只列"结论翻转过"的是错的，**未翻转 ≠ 不受影响**（旧数是旧口径算的，换口径就该复核，哪怕三态停在原档）；`recheck` 管"改动是否静默把通过改失败"、`review_required` 管"哪些历史结论要重看"，职责不重叠 ④**"版本化/留存/归档"类承诺必须有守卫钉住它真产生了文件**——制度 §7.2 早写「报告按 generated_at 版本化留存」而落盘长期是单文件原地覆盖 ⇒ 历史结论只剩上一次，**"列出需复核的历史结论"连数据面都不存在**（[[KB-ENG-72]] 同族）。**可迁移判据**：判断某产物能否继续用之前，先问「**它是由哪一版口径产出的？**」——凡"算出来的结论"（因子 IC / 评估报告 / 回测基线 / 门槛校准）都与产生它的算法版本绑定 | ✅ | 2026-09-14 |
| KB-ENG-85 | **「文档能力描述失真」是 `doc-health` 查不出的第 N 类失真；状态只能有一个权威出口**（2026-09-14 `GOV-002`）。**事故**：`doc-health` **全绿**而这些句子全错——`summary/architecture-design.md` §2 写「**P1/P2 未实施（保留待办）**：题材徽标 / watcher」（实为 `P1-4`/`P1-5` ✅、`P1-1` ✅）、§4 写「**待办（⏳ 保留）**」4 项（实为 `P0-2`~`P0-5` **全部 ✅**）、`summary/stock-strategy.md` 行 40「未落地项：止盈 tracker」而**同一文件**行 56/文末又写「✅ 已完成」（**同文档自相矛盾**）。**根因**：A–N 各项扫的是**结构与引用**（未登记/死链/超长/摘要前置/锚点/表格分隔行/任务指针/体积），**没有一个**会因为「把已完成的说成未做」变红——**判据扫形式，失真在语义**。**危害不是"不好看"，是诱发重复开发**（照文档做一件已做完的事、验收时把已闭环项当欠债重排）。**四条纪律**：①**状态只能有一个权威出口**——`summary` 层不是禁谈状态（"已落地"也是状态），而是**谈状态必须带指向唯一清单 §6.0 的指针**②**豁免按行不按段**（改失真必须能引原话溯源，但段级豁免会让"一段里既贴原话又照抄一遍"蒙混）③**同文档自相矛盾是最强检出信号**（不需外部信息，正文"未落地" vs 文末"✅ 已完成" ⇒ 必有一错；校验成本最低）④**守卫扫描面刻意收窄 ≠ 其它层干净**（只扫 `summary/`，而 `api.md`「LLM 增强层未接入」同样中招）。**可迁移**：写「X 未做」前**先 grep X 的实现**——「未做」与「已完成」**同等需要证据**。**过程留痕**：守卫否定用例写了**一份虚构的 summary 文件名**当假路径 ⇒ 被 `doc-health` **F 项**判红（**判定面越宽越容易被别的守卫咬 = 两层门禁确实互补**）；**同族第三次**：照抄该假路径进文档 ⇒ 被 **B 项（死链）**当真引用判红 ⇒ **叙述缺陷勿写可解析路径字面量**（门禁不区分「引用」与「提及」）；并顺手发现上轮自造格式失真——追加索引行**漏换行**把新行粘到旧行尾、吞掉 `| ✅ | 日期 |` 两列（1522→2526 字符、列数 4→8），⚠️ **K 项只校验分隔行形态、不校验行列数** ⇒ **该形态无门禁覆盖，长行写入后须按字符数抽查** | ✅ | 2026-09-14 |

## 系统决策（04-decisions.md，KB-DEC）

| ID | 一句话 | 状态 | 来源日 |
|---|---|---|---|
| KB-DEC-001 | 飞书推送矩阵四类，每日仅一条进化总结报告 | ✅ | 2026-09-08 |
| KB-DEC-002 | 告警判读即终态（无人工确认环节） | ✅ | 2026-09-08 |
| KB-DEC-003 | 进化议程 A/B/C 三类自动执行 + 红线清单 | ✅ | 2026-09-08 |
| KB-DEC-004 | A 类参数影子运行→剧变检测→转正/拒绝 | ✅ | 2026-09-08 |
| KB-DEC-005 | 改动前强制建 mutation 台账（三入口） | ✅ | 2026-09-08 |
| KB-DEC-006 | 策略改动回放对比门禁（口径诚实注记） | ✅ | 2026-09-08 |
| KB-DEC-007 | L3 硬禁止清单（下单/推送/凭据/删数据代码层屏蔽） | ✅ | 2026-09-08 |
| KB-DEC-008 | 盘中跟踪量化门槛（⚠️ 被 KB-DEC-011 部分修订：移除涨停准入） | ✅ | 2026-09-09 |
| KB-DEC-009 | 台账首见唯一（trade_date+symbol）+ 清算幂等可重跑 | ✅ | 2026-09-09 |
| KB-DEC-010 | 知识库机制：当轮入库 + 议程第八路消费（本条） | ✅ | 2026-09-09 |
| KB-DEC-011 | 临板预警制：涨停前识别才准入，封板后一律不入册 | ✅ | 2026-09-09 |
| KB-DEC-012 | 仓位引擎+自动离场（阶段仓位/角色权重/封板持有/移动止盈） | ✅ | 2026-09-09 |
| KB-DEC-013 | 盘前名单名额由质量决定：入选门槛 MIN_PICK_SCORE=50，MAX_PICKS 降为容量上限（0~5 只） | ✅ | 2026-09-10 |
| KB-DEC-014 | 空仓闸门分档：相位级信号（退潮/冰点）或多信号叠加才撤买入区间，单条量化擦线只提示 | ✅ | 2026-09-10 |
| KB-DEC-015 | 闸门量化阈值改历史分位口径（p10/p90），绝对经验值降为兜底 + 写明所用口径 | ✅ | 2026-09-10 |
| KB-DEC-016 | 控制台留痕合一：议程→只读任务视图（视图映射，不复制数据/不新建表） | ✅ | 2026-09-10 |
| KB-DEC-017 | 参数白名单纳入口径（风控/资金类永久排除）+ 回滚归因封闭集合 + 存活率三数分工 | ✅ | 2026-09-10 |
| KB-DEC-018 | 示例 ≠ 规范 + 「方法论 / 经验 / 思路」三类不可混淆（经验须本地验证、思路是线索非结论；禁外推当结论） | ✅ | 2026-09-10 |
| KB-DEC-019 | 策略/战法**准入—监控—退役闸门 + 反固化条款**：示例只作示例（📎）；准入门槛五条；禁止围绕单一标的固化；多次失效即淘汰；复盘新规律经验证可纳入 | ✅ | 2026-09-10 |
| KB-DEC-020 | 告警 **escalate = 人工处置待办**（needs_confirm + `resolve` 接口，确定性 id 幂等），系统只登记不自动执行；与 KB-DEC-002「判读即终态」的边界：**"读没读"不问，"办没办"要问** | ✅ | 2026-09-10 |
| KB-DEC-021 | 常驻任务清单**必须是一份声明**（S2-2）：启动清单与停机收割清单两张手写表必然漂移（漏收割 ⇒ lifespan 永不结束 ⇒ 全量 pytest 整场挂死）。`SchedulerRegistry` 一份 `add()/add_periodic()` 同时驱动启动/心跳/重启/收割 + `GET /api/system/schedulers` + 死亡进哨兵（文案只含稳定值）。三条细节：**开关未登记要启动期 fail-fast**（否则测试环境真跑生产调度）/ 注册表**模块级不得 import settings**（否则 conftest 的测试设置全失效）/ heartbeat 要诚实（外部驱动的循环标 `"external"`、`last_tick` 恒 null，不假装有数据） | ✅ | 2026-09-11 |
| KB-DEC-022 | agent 域事件时间存储统一为**北京 naive**（方案 A）：7 表 14 列 699 值一次性 +8h 迁移 + 写入侧翻转 + 帮手改名对齐口径；一次性迁移三重防呆（marker/UTC 断言/dry-run 默认），回读校验用**精确等值**不用启发式（陈旧列必误报）；写码与写库同窗口切换 | ✅ | 2026-09-12 |
| KB-DEC-023 | **外部看盘工具生态评估（2026-09-12）**：全仓 `tradingview` 0 命中 ⇒ 系统未用 TradingView，只依赖其**开源**图表库 `lightweight-charts@4.2.3`。**不引入 `tradingview-mcp` 本体**——三条独立理由（①需桌面端 + 付费订阅 + CDP 打未公开接口 ⇒ 无时间戳/质量档，不满足 `Freshness` 契约；②自述禁用「据其数据做自动决策」+ ToS 限制自动化采集 ⇒ 触碰红线 2/3；③定位是「人工看盘助手」，12 类能力与本系统大面积重叠且它不下单）。**图表库 v5 升级缓议**（消费方 4 文件/1693 行、`addXxxSeries`≈20 处 + `setMarkers` 1 处；收益为视觉/打包，代价是重置 v4 上的闪烁与主题不重建等稳定性资产）⇒ 触发条件=出现「真多窗格」硬需求。**正路 = 把本系统自身能力包成 MCP**（即既有 P2-10/P2-13，不另立项） | ✅ | 2026-09-12 |
