# 工程教训（KB-ENG）· 应用与设计层

> 编码 / 测试 / 工具层面的坑。整合自 MEMORY.md「工具教训」与 2026-08-29 ~ 09-09 逐日日志。
> 发现遗漏按 00-INDEX 维护规则补录；与 KB-TRADE 的边界：影响代码正确性的归这里，影响盘面判断的归 02。

> **KB-ENG 分册（2026-09-12 起；`00-INDEX.md` 为全序列唯一登记处，按 tag 检索不分文件）**
> - `08-tooling-pitfalls.md` —— 工具 / 环境**操作**陷阱速查（KB-ENG-01~15，每条 3~5 行）
> - `09-verification-pitfalls.md` —— **验证层**陷阱（测试 / 门禁 / CI / 环境 / 防线有效性的验证）
> - `10-data-contract-pitfalls.md` —— **数据契约**陷阱（写入 / 去重 / 传输 / 时间 / 质量门）
> - **本文件** —— 其余系统性工程教训（架构与接口设计、口径判据、前端布局、研究方法论）
>
> 拆分依据 `07-doc-curation.md` §5.2 条件①（文件级超 800 行且主题可分离）；
> 条目级硬门（单条 ≤60 行）由 `scripts/doc-health.py` 保证。

### KB-ENG-16 flex 高度链：中间层块级元素会让 overflow-y-auto 全部失效
- **现象**（两次踩坑，用户明令不再犯）：面板内容超高时页面无法滚动——overflow-auto 容器拿到的
  是「内容高」而非「受限高」，滚动永不触发。
- **根因**：flex 高度链在中间层断掉。FadeSwap 等 tab wrapper 若是**块级元素**（未设
  flex flex-col），其子面板的 flex-1/min-h-0 不产生约束高度 → 内部 overflow-y-auto 失效。
- **正确做法**：高度链每一层都要接力——wrapper `flex min-h-0 flex-1 flex flex-col overflow-hidden`，
  面板根 `h-full min-h-0` + 自管滚动（overflow-y-auto 或内部分栏各自滚）。
  **新建任何「左树右详情/上表下详情」面板时，先把高度链从头到尾写对再写内容。**
- **自查命令**：浏览器 eval `pane.scrollHeight > pane.clientHeight && pane.scrollTop 可设置`。

### KB-ENG-17 lark-cli 发消息：`--content` 必须是 JSON，且别把 validation 误判成身份失败
- **用法**（2026-09-09 实测）：`lark-cli im +messages-send --user-id <open_id> --as bot --msg-type text --content '{"text":"..."}'`，
  或用 `--text '...'`。**直接传纯文本会报 `--content is not valid JSON`**。
- **防误判**：该错误是 `type=validation / subtype=invalid_argument`，**不是身份/权限失败**。
  此时若按"--as bot 失败就重试 --as user"的兜底逻辑去重试，会**多发一条重复推送**（议程类推送红线：一天仅一条）。
  正确做法：先把文本用 `python -c "json.dumps({'text': txt})"` 落文件，再 `--content "$(cat file)"`。
- **通用教训**：「失败后换身份重试」这类兜底，必须先区分**参数校验失败**与**鉴权失败**，否则兜底动作本身会破坏幂等。

### KB-ENG-18 ❌（结论已被取代 → KB-ENG-20）「日志里没有」≠ 没发生：先看 logger 级别
- **原结论作废**：当日归因为「app logger 级别是 WARNING，INFO 不落盘」——实测 root level 是 INFO。
- **真因见 KB-ENG-20**：启动时程序化 alembic 迁移的 `fileConfig(disable_existing_loggers=True)`
  把迁移前创建的 logger（含 evolution、uvicorn 自身）**整体 disabled**，与级别无关。
- **仍然成立的教训**：判断后台任务存活走落库/端点侧证据（本次：agent_agenda 表 + API），
  不要拿"日志里没有"当证据——日志通道本身可能已断。

### KB-ENG-20 程序化 alembic 迁移的 fileConfig 会 massacre 全部既有 logger（议程静默事故主因之二）
- **事故**（2026-09-09）：15:45 进化议程静默未生成。直接死因是 `_LAST_SHADOW_DATE` NameError
  （每 tick 崩溃，见下）；**但零日志可取证**的原因是 `migrations/env.py:35`
  `fileConfig(config.config_file_name)`——默认 `disable_existing_loggers=True`，每次启动把
  迁移前创建的所有 logger **整体 disabled**：uvicorn 自身（"Application startup complete" 消失）、
  main、以及随 routes 在 import 期加载的 `app.services.evolution`。log.exception 照样被吞。
- **判定特征**：日志里只出现**迁移之后才懒加载**的模块（watcher/fund_flow/review_intraday）
  的告警、且格式是 alembic.ini 的 `%(.5slevelname) [name]`；迁移前创建的 logger 零输出。
- **修复**：①程序化迁移 `cfg.attributes["configure_logger"]=False`（env.py 跳过 fileConfig）；
  ②CLI 直跑保留但 `fileConfig(..., disable_existing_loggers=False)`。
- **通用教训**：库代码里任何 `fileConfig/dictConfig` 都可能 massacre 宿主日志配置；
  程序化调用第三方 CLI 入口时，先查它有没有"顺手重配日志"的副作用。
  另：**"某模块日志消失" ≠ "该模块没跑"**——本次调度器明明在每 tick 崩溃，日志却一片祥和，
  逼着人往"任务没起/守卫没过"的错误方向排查。

### KB-ENG-21 议程调度器三重防护（2026-09-09 事故收口）：缺失全局、可观测、liveness 面
- **直接死因**：`evolution_scheduler` 用 `global _LAST_SHADOW_DATE` 声明并读写，但模块级从未定义
  （4f24462 影子批次引入）→ NameError 每 tick 崩溃且发生在议程块**之前** → 议程永不生成、无任何日志。
- **修复与防护**（evolution.py）：
  ① 模块级补 `_LAST_SHADOW_DATE: str = ""`；
  ② 观测：scheduler started / 窗口触发 / 议程完成全部 `log.warning`（INFO 不可信，见 KB-ENG-20）；
     跳过原因按 (日期, 桶) 节流每日一条（`_log_skip_once`），不刷屏；
  ③ liveness 面：每 tick 刷 `_SCHED_LAST_TICK`，`scheduler_status()` 经 `GET /api/agent/agenda`
     的 `meta.scheduler_status` 暴露（last_tick_at / last_tick_age_sec）——**data=null 时先看调度器死活，
     age > 120s 即停摆**；15:45 推送 automation 与哨兵据此区分"调度死了"与"真没有"。
- **回归测试**：`test_scheduler_fires_when_clock_crosses_window`（受控时钟驱动真实循环跨 15:45 窗口）
  + `_MIN_TICK_INTERVAL_SEC` 可注入（生产恒 60s，行为不变）。
- **教训**：`global X` 声明的名字必须模块级有定义——静态检查抓不到（pyflakes 不报），
  只有"驱动真实循环"的测试能抓；调度类代码必须有可观测的 skip 原因与 liveness 暴露。

### KB-ENG-24 告警/信号阈值先量「噪音比 + 下游预算」，别只看触发量
- **现象（2026-09-10，P1-16）**：`flow_surge` 阈值 0.3 亿，日均产出量看着不大，
  但实测库内 151 条事件的判读中 **102/105 = 97.1% 被 LLM 判读层 ignore**（理由全为
  "冷却窗口内已提醒"）；触发值中位数仅 0.92 亿，0.3 亿档贡献了 51.7% 的事件。
  更严重的是**挤占判读预算**——`triage_pending` 每轮上限 20 条，噪音把窗口占满，
  导致 75 条事件**从未被判读**，其中包含 `falsify`（方向证伪）这类最高价值信号 18 条。
- **正确做法**：①先算**噪音比**（被下游忽略的比例）而不是触发量；②再看**下游预算**
  （LLM 判读是有限资源，噪音排挤的是高价值信号，损失不对称）；③改阈值同时加
  **每拍 Top-N 上限**，未入选的**排队而非丢弃**（`FLOW_ALERT_PER_BEAT=5`）；
  ④阈值配置化 + 非法/非正回退记日志，**不静默改口径**。
- **推广**：所有"触发即产生下游处理成本"的阈值（告警、候选池、采样率）都适用——
  衡量的不是"产出了多少"，而是"下游真正消费了多少、被挤掉了什么"。

### KB-ENG-27 判断「两个组件该不该合并」：先看输入契约能否归一，再看渲染分支数
- **现象（2026-09-10，用户："这两张卡片字段都是不一样的，共用时应该互补，合起来才是完整的"）**：
  `PickCard`（盘前名单）与 `WatchCard`（盘中名单）长期并行。`card-shell.tsx` 里还留着
  一条**写死的错误结论**："只抽壳、不抽内容——两卡分节语义差异大，强行合并会把条件
  渲染做成 5 处分支，可读性反而崩"。该结论把一个**输入契约问题**误判成了**渲染复杂度
  问题**，于是合并一直被劝退。
- **正解**：两卡的字段是**互补**而非重复（盘前有六维评分/估值/买入区间/失效条件，
  盘中有辨识度·确定性判定/涨停原因/T 档/题材强度档）。合并通路不是「在渲染层堆分支」，
  而是**在适配器里归一输入**：`fromDailyPick(DailyPickItem)` / `fromIntradayStock(...)`
  → 中间模型 `TradingCard`，渲染层只认一种形状、每节「有则渲染，无则整节不出现」。
- **两条硬纪律**：
  ①**异名同义字段必须在适配器单点归一**（本例 `role↔echelon_role`、`stage↔theme_stage`、
  `stop_ref↔stop_loss`、`exit_plan↔exit_discipline`）——下游若再写
  `item.role ?? item.echelon_role`，等于把归一逻辑又散回渲染层，等于没合并。
  ②**来源缺的维度不补、也不留白**：整节不渲染 + 卡底一句**口径注记**说明"为什么没有"
  （"盘中实时口径…不含收盘六维评分与买入区间"）。渲染占位「—」是把"该来源本就没有
  这个维度"伪装成"有数据但没取到"。
- **副作用要一并清**：合并后 `exit_plan`（未定型 dict）取值可能一项都没有 ⇒ 归一为
  `null` 而非 `trailing_pct: 0`（渲染出"回落 0%"是臆造数字）；测试要覆盖"整节不渲染
  + 口径注记出现"，而不是覆盖"渲染了占位"。
- **推广**：评估"重复实现要不要合并"时，先问**输入能否归一到单一契约**。能归一 ⇒
  渲染分支数是伪问题（各节只是可选）；不能归一 ⇒ 才轮到讨论拆分。**判定依据要写进代码
  注释，否则下一个人会继续引用那条过时结论。**
- **补充（同日，用户第二轮反馈）**：**合并卡片 ≠ 合并分区**。字段统一后用户仍要求
  "盘中跟踪和盘前选择放两个容器、两条瀑布流、盘中有上面" —— 因为**来源节奏不同**
  （盘中=当日实时动态名单，盘前=收盘定次日持久组合）。教训：组件复用解决的是
  "同一件事别写两遍"，**不解决"两件不同节奏的事别混在一个视图里"**；两者是正交的。

### KB-ENG-28 用户说「没有 X」时，先分清三种成因（数据没产生 / 有数据没标签 / 标签语义漂移）
- **现象（2026-09-10，用户："盘前选择怎么没有入选原因呢"）**：实测 `GET /api/picks/today`
  5/5 只票的 `bases` 七维齐全（情绪/消息/技术/基本/资金/梯队/筹码），依据行一直在渲染
  ——**数据一条没丢**。真正缺的是：盘中卡有一行标签「入选」，盘前卡只有七行维度行、
  **没有标题** ⇒ 用户扫过去判定"这卡没有入选原因"。
- **三分诊断（排查顺序）**：①**数据没产生**（真缺口，改产生层，见 KB-ENG-25）；
  ②**有数据但没标签**（本次；改展示层，最小改动即给分节加标题、两侧口径一致）；
  ③**标签语义漂移**（数据在但挂错名，如 `reason` 曾标成「依据」，见 KB-ENG-26 同轮修复）。
  **不先分清就动手，很容易去改后端补一个本来就在的字段**（浪费 + 引入重复契约）。
- **验证手法**：先 `curl` 原始载荷确认字段在不在（**不要靠看界面猜**），再逐卡枚举
  渲染文本比对。本次靠 `grep`/Python 统计「入选原因」标题数 = 卡片数（3 盘中 + 5 盘前 = 8）
  一次对齐。
- **推广**：UI 上的"缺失"报告一律先落到这三类之一再动手；**"看起来没有"与"真的没有"
  是两种缺陷，修复面完全不同**。

### KB-ENG-29 全局门槛复用前先验量纲：回放分 ≠ 线上分
- **现象（2026-09-10）**：给线上组合加「入选门槛 MIN_PICK_SCORE=50」后，
  `tests/test_picks_replay.py` 三条用例立刻失败——回放的候选分是 30~80 的合成阶梯，
  被绝对门槛成片切掉。
- **根因**：跨日回放只能重建**梯队+技术两维**（消息/情绪/基本面/资金依赖当前快照，
  见 `app/picks/replay.py` docstring），其绝对量级与线上六维综合分**不同量纲**。
  把线上标定的绝对门槛套到二维重建分上属**量纲错误**，会把回放整体压成空组合，
  且失败方式隐蔽（表现为"换手率变好"，看着像改进）。
- **正解**：`replay_picks(..., min_score=0.0)` 默认**关闭**该门槛并写明理由
  （调用方显式开启才生效）；`scripts/replay_picks.py` 直调引擎函数处同样显式传 0.0。
  线上路径（`picks.py` 路由）用引擎默认值——两条路径各按自己的量纲取值。
- **推广**：任何"全局阈值/常量"要跨模块复用时，先问**两个模块的指标是否同量纲、
  同标定口径**。同名 ≠ 同义；不同量纲共用一个常量，比各自维护一个更容易出错，
  因为错误往往是**静默的方向性偏差**而不是崩溃。

### KB-ENG-30 名额类常量要区分「上限」与「目标」
- **现象（2026-09-10）**：`MAX_PICKS = 5` 长期被当作"每天输出 5 只"的目标值使用
  （填充循环 `if len(kept) < max_picks: kept.append(c)` 不看分数），弱市里等于**硬凑**。
- **正解**：命名与注释明确区分——**上限**（capacity，只约束"最多几只"）与
  **目标**（target，要求"至少几只"）。本项目的组合只应有上限，不该有目标：
  够格几只就是几只，不足则留空。**任何"补位"代码都必须同时检查质量线**，
  否则补位就退化成凑数。
- **落地**：`MAX_PICKS` 注释改为"容量上限（不是每天必须凑满的目标数）"；
  补位分支加 `c["score"] >= min_score` 前置判断。

### KB-ENG-32 档位标签（level）是**计数产物**，不能当语义判据用
- **现象（2026-09-10）**：空仓闸门的 `level`（strong/mild）由 `len(reasons) >= 2` 决定，
  是个**计数**；而下游「要不要撤买入区间」被写成 `level == "strong"`。于是
  「相位=退潮」单条理由 → level=mild → 不撤区间；「晋级率 29% 差 1 个点擦线」也是
  1 条 → level=mild → 同样不撤。**两种性质迥异的信号被同一个标签压平**。
- **为什么会写错**：`level` 命名自带「严重程度」的含义，读代码的人（和写代码的人）
  都会自然地把它当语义判据。但它的定义里没有任何「信号性质」信息，只有「触发了几条规则」。
- **正解**：判据按**信号性质**另写（`should_strip_buy_range`：相位级 vs 阈值擦线），
  并且**结论算在产生点、下游只读**（落库 `strip_buy_range` 字段），避免前后端各算一遍。
- **推广**：用枚举标签做条件分支前，先回去看**这个标签是怎么算出来的**。凡是
  「按数量/比例分档」得到的标签（low/medium/high、mild/strong），都只携带计数信息，
  拿它表达语义（严重性、可信度、可执行性）迟早错配。

### KB-ENG-34 `describe()` 型接口：确认它描述的是"谁"的位置，再拿来当"今天"
- **现象（2026-09-10，与 KB-ENG-33 同轮发现）**：`calibration.describe(history)`
  返回的 `percentile` 是**历史库里最后一行**的分位，而库里最后一行是**上一个交易日**
  （`backfill` 刻意不回补今天，避免盘中半截数据污染分布）。
  于是线上把 `sentiment.calibration.percentile.promo_1to2.percentile` 当作
  "今天晋级率的历史分位"使用 ⇒ **每天用昨天的位置描述今天**；库一停更
  （见 KB-ENG-33）就变成**用几周前的位置描述今天**，而它看起来一直"有值、合理"。
- **正解**：新增 `metric_history.percentile_of_value(metric, value)`——把**当日实测值**
  放进历史样本里算分位。与 `describe()` 的口径差异要写进 docstring，
  并用测试锁死「传同一个值 → 两者一致；传不同值 → 结果随之改变」。
- **推广**：任何"位置/分位/排名"型接口都要先问**它描述的是谁**（主体是今天、昨天，
  还是库尾？）。把聚合结果当主体值用，错误的表现形式是"数字一直很合理"。

### KB-ENG-35 运行时覆盖：默认值**不能在签名里绑定常量**，`None` 已有语义时要上哨兵
- **场景（2026-09-10 P1-15）**：给 `apply_replacement_threshold` 加运行时参数覆盖
  （控制台改参数免重启）。
- **两个坑，都会静默出错**：
  ①**签名默认值在导入时求值**——`def f(min_score: float = MIN_PICK_SCORE)` 里那个值
  在模块载入时就固定了，覆盖层永远读不到。必须改成默认 `None`、**在函数体内解析**。
  ②**`None` 可能已经有语义**——`max_swaps=None` 表示"**不限换股**"（回放对照组/首次建仓
  在用）。拿它兼职表示"未传参"，覆盖层一启用就会把"不限"悄悄变成"上限"，
  **静默改变回放结论**。正解是模块级哨兵 `_UNSET = object()`，把"未传参"与
  "显式传 None"分开。
- **配套要求**：覆盖层放**无 app 内依赖的 core 模块**（读覆盖的是业务层、写它的是
  services 层，依赖只能单向）；**未生效的 key 不写入覆盖层**，业务侧回落代码常量，
  于是"回滚 = 删掉覆盖行"天然生效。
- **验收**：`meta` 必须写**生效值**（`effective_limits()`）而不是代码常量，否则调参后
  复盘/页头显示的仍是旧数（口径漂移）；端到端必须验"**不重启进程**重跑 → 生效值出现在
  结果里"。
- **推广**：凡是"把常量升级成可配置"的改动，先排查三件事——签名默认值求值时机、
  哨兵值是否与既有语义冲突、下游展示读的是常量还是生效值。

### KB-ENG-36 归档不是摘要：把「可执行清单」压成一句概括 = 丢失 N 个待办
- **事故（2026-09-10 复核 P1-20 时暴露）**：`system-review-2026-09-02.md` 归档时，
  其 §4.2 的**候选因子表（实际 11 项，含公式与来源）**被压缩成一句话存档：
  「产出调研结论 + **10 项可补因子**（未全实施）」。
- **代价（三个叠加）**：①**计数当场就错了**（11 → 写成 10）；②每一项的状态无从判断
  —— 不知道哪个做了、哪个没做、哪个是数据阻塞，于是这条待办在账本里躺了 8 天
  「❌ 未做」却无法推进；③**要恢复清单只能回 git 历史取原文**
  （`git show <last-commit>:docs/system-review-2026-09-02.md`），且原始文档已从工作区删除，
  不查 git 就等于永久丢失。
- **正解**：归档时区分两类内容——
  **结论类**（可以且应该压缩：「本轮用户反馈的 7 项 UI 缺陷已修 6 项」）；
  **清单类**（**必须整表搬，或至少留原文指针**：候选因子表、待办表、API 清单、
  参数表——它们是**下一轮工作的输入**，压成概括就等于把输入删了）。
  判定标准很朴素：**这句话能不能直接派活？** 不能，就说明清单被摘要吞掉了。
- **与既有纪律的关系**：这正是顶层规范「降低多文档阅读困难，但**不得遗漏重要信息**」
  的失败样本——**降阅读成本的正确手段是合并同类项与给指针，不是把列表改写成叙述**。
- **配套动作**：本次已把清单整表搬进 `summary/factor-system.md §5`（含逐项实测状态），
  并按「先 grep 再信文档」复核出 **6 已完成/等价、5 数据阻塞、0 数据具备却未实现** ——
  若不复核就动手，会把 6 个已完成项重做一遍（与 09-10 全量重盘 7 处偏差同类）。

### KB-ENG-38 「滚不动」要先查**滚动归属**：`shrink-0` + 祖先 `overflow-hidden` = 内容静默被裁
- **现象（2026-09-10 用户报）**：「参数配置向下滚动不了」。
- **实测几何（agent-browser 读真实 DOM，不是看代码推断）**：
  根容器 `scrollHeight 717 / clientHeight 508`（**差 209px 需要滚**），但根 `overflow-y: visible`、
  父级 `FadeSwap` 为 `overflow: hidden` ⇒ 多出的 209px **被直接裁掉，一像素都滚不动**；
  同时「变更单历史」段被 flex 压成 **26px**（内容 154px）。
- **根因**：`FadeSwap` 的契约是「各 tab 根 `h-full min-h-0` + **自管滚动**」，而本页
  ①根缺 `overflow-y-auto`；②三段全是 `shrink-0`，**没有任何 `flex-1` 子区块吸收剩余高度**。
  ⇒ 高度没人吸收、滚动没人负责，溢出交给父级 `overflow-hidden`。
- **为什么现在才暴露**：内容是逐步变高的（当轮给该页加了「变更存活率」卡片、参数白名单 1→5）。
  **布局类缺陷常在内容长到阈值时才现形**——所以「以前没问题」不能作为「现在也没问题」的证据。
- **诊断口径（可复用）**：判「滚不动」看三个数，**沿祖先链**逐个读——
  `scrollHeight - clientHeight > 0`（确实有内容溢出）**且** `overflowY ∉ {auto, scroll}`（这层不负责滚）
  ⇒ 该层把滚动责任推给了祖先；若祖先为 `hidden` 就是「静默裁掉」。
  **不要用"代码里有没有 overflow-hidden"来判**，也不要靠读指令推断。
- **修法与验收**：根 `overflow-y-auto` + 子段一律 `shrink-0`；**验收标准是 `scrollTop` 能实际改变**
  （实测 0 → 339）且被压扁的子段高度恢复（26 → 156px），而不是"看起来有滚动条"。
- **防回退**：滚动契约写成测试断言（jsdom 无布局，只能断言 class 契约：根含 `overflow-y-auto`、
  子 `section` 含 `shrink-0` 且不含 `flex-1`），并用变异验证确认能精确失败。

### KB-ENG-39 战法核验器：把「五道检验」做成可复用模块（附四条方法论教训）
- **背景（2026-09-10）**：核验三倍量与五步法时各写了一遍"窗口函数 + 全市场对照 + 分层"
  的脚本，大量重复。已抽成 `app/research/strategy_verify.py`（+`tests/test_strategy_verify.py`
  22 项），下游 `scripts/verify_two_thirty_five.py`（五步法）与
  `scripts/verify_candidate_b_oos.py`（样本外盲测）都是它的使用者。
- **API**：`build()` 物化特征表（chg/vr/turn/vol_step_up/ma_*/dev_short/at_limit/**mchg**/fwd{h}）
  + `mkt_fwd`（同日市场均值）+ `sigv` 视图；`funnel`/`single`/`sensitivity`/`yearly`/
  `split_sample`/`limit_up_share`/`stats`/`baseline`/`render`/`welch_t`/`horizons_of`/`date_lo`。
  每个统计量同时输出**原始均值**与**市场中性均值（扣同日全市场均值）**。
- **四条方法论教训（都会导致"看着对"的错误结论）**：
  1. **t 值 ≠ 效应量**：按 t 值在参数网格里选规则，会挑到「样本量巨大、效应极小」的组
     （实测 n=35 万、t=+11.8，超额仅 +0.12%，扣成本即负）。网格选取必须**并列**
     「max t」与「max 效应量（设样本量下限）」两种准则。
  2. **市场中性必须按日对齐**：只用混合池均值做对照，会把"顺大盘"这类**市场级 beta**
     读成 alpha（好日子天然涨得多）。正确基准 = **同一交易日**的全市场均值。
     且这条**需要逐日波动的市场因子才能测出来** —— 合成数据若每天均值几乎相同，
     连"join 键错位一天"都测不出（本条的测试就踩过：第一版数据太规则，变异逃逸）。
  3. **均值要配中位与胜（跑赢）比例**：实测某规则样本外均值 +1.33%，但**中位 −0.08%、
     跑赢比例 49.3%** ⇒ 收益全靠少数极端样本（右偏）。只报均值 = 只报一半，
     且这种分布对资金曲线极不友好。
  4. **分组静默缺失会被读成"这一档不存在"**：SQL `GROUP BY` 不返回空组。漏斗/单条件
     这类"标签集合已知"的检验必须 LEFT JOIN 补齐、显式输出 `n=0`（三态纪律）。
- **实现坑**：聚合片段拼接用空格 join → `AS s1 count(fwd3)` 语法错误（报错位置误导，
  指向 `count`）；前瞻窗口是**建表期**属性，查询期必须用 `horizons_of()` 探测，
  否则换窗口后查询会引用不存在的列。
- **关联**：[[KB-STOCK-29]] [[KB-STOCK-30]] [[KB-ENG-36]] [[KB-DEC-018]]

### KB-ENG-40 代码里的「示例固化」：映射索引 vs 结论固化的分界与处置
- **背景（2026-09-10 体系审查）**：审查"示例被当方法论固化"时，真正的风险点不在文档，
  而在**代码常驻规则**——因为文档会被读作"参考"，而代码会被当作"系统已知事实"运行与展示。
- **实例（本次实测，均为标注修正、未改行为）**：
  | 位置 | 固化内容 | 性质 |
  |---|---|---|
  | `events/chains.py` `_EL_NINO_ROWS` | 厄尔尼诺→磷化工/化肥/农业种植…，带强度值 2/2/2/1/1 | **结论固化**：把单日单案例的"弹性印象"写成了数值强度 |
  | `services/dragon_service.py` `THEME_CORE_PERSISTENCE` | 内核→持续性倾向（业绩兑现 1 / 事件传闻 −1） | **结论固化**：业界共识转述，未经本地验证 |
  | `services/dragon_service.py` `THEME_CORE_RULES` | 产业趋势关键词枚举（算力/液冷/光模块…） | **映射索引**：允许保留，但会随题材演化失效 |
  | `events/extract.py` `ENTITY_ALIASES` | 别名 → 题材 | **映射索引**：允许保留 |
- **分界判据（一句话）**：**"词 → 类目"是索引，可以枚举；"类目/标的 → 强弱优劣"是结论，
  没有数据不许固化。** 索引失效是退化（漏识别，可接受）；结论固化是误导（错排序，不可接受）。
- **处置三选一（不得悬空）**：①**加标注**（定位声明 + "未验证"），②**改造**为可验证谓词并过闸，
  ③**删除**。本次对两条结论固化取①（加定位声明，保留可解释性，零行为变更）。
- **回标要求**：代码里的固化内容必须与 KB 状态一致——若 KB 里是 `📎 示例`，
  代码里就不能表现为"已知规律"（[[KB-DEC-019]] 反固化条款）。
- **自检命令（可 grep）**：在 `backend/app/` 下搜具体题材/个股名
  （如 `grep -rn "化肥\|光模块\|商业航天" backend/app`），**命中的每一处都要能回答
  "这是索引还是结论"**——本次即由此发现 2 处结论固化。
- **关联**：[[KB-DEC-018]] [[KB-DEC-019]] [[KB-STOCK-01]] [[KB-ENG-39]]

### KB-ENG-41 注册表一物二用、待办没有出口：登记层的三个漂移点
- **背景（2026-09-10，P1-36 escalate 接线）**：判读层 docstring 写着「escalate 进任务中心
  待办」，代码却只在 `agent_triage` 落一行 ⇒ **「升级」实际等于丢弃**。接线过程中暴露
  三个通用漂移点（都属"看起来已实现"）。
- **① 一份常量不能同时充当「标签注册表」与「可创建清单」。**
  `TASK_TYPES` 既喂 `GET /agent/task-types`（前端"新建任务"按钮的数据源），又当类型
  白名单；`mutation` 在其中但**没有 handler** ⇒ 按钮可点、点了必在 `_HANDLERS[type_]`
  抛 KeyError，**建出一条必然失败的 L1 任务**。
  **分界**：可创建集合 = **handler 注册表本身**（`CREATABLE_TASK_TYPES = frozenset(_HANDLERS)`，
  单一真相源，新增 handler 自动可创建）；标签元数据可以含不可创建项。
  前端对不可创建项走**本地标签兜底**（先例 `AGENDA_LABEL`），不为省一个 map 扩大接口暴露面。
- **② 待办必须有出口；且初始态语义要精确。**
  只登记不关闭 ⇒ 无限累积 ⇒ 淹没真正要看的条目（与"降噪"初衷相悖）。所以
  `record_escalation` 必须配 `resolve`（`done`→succeeded / `dismissed`→canceled），
  且**终态幂等**（重复处置不改写、不报错）。
  初始态不能图省事用 `queued`：`queued` = **等待执行**，而本类条目**永不执行** ⇒
  永久显示"排队中"并触发前端 3s 轮询（**假承诺**）。正解 `needs_confirm` = 等待人工处置。
- **③ 枚举/标签里存在 ≠ 已实现：状态机会长出「纸面状态」。**
  `needs_confirm` 早已写进任务状态机设计（models/agent.py 状态机注释）与前端 `STATUS_META`（琥珀色"待确认"）里，
  但**没有任何生产者** ⇒ 它永远不可能出现（同族：纸面字段）。接线的真正内容是
  **"给它一个生产者 + 一个消费者（处置入口）"**，只加枚举值是空壳。
- **登记类写入口的两个范式（新增时二选一，勿用 `create_task`）**：
  | 范式 | 幂等性 | 初始态 | 收口 |
  |---|---|---|---|
  | `record_mutation` | 每次一条（可重复登记） | `queued` | `update_mutation_result` 回填结果 |
  | `record_escalation` | **确定性 id `esc-<event_id>`**（重入不刷第二条） | `needs_confirm` | `resolve_task` 人工处置 |
  `create_task` 是"创建并**启动**"（会拉起同类型互斥的执行），语义完全不同。
- **实现坑（三条，都是本次踩到/防到的）**：
  1. 登记发生在 `_save` 内，而 `record_escalation` 要另开 session ⇒ 必须在原 session
     **提交并关闭之后**再调用（SQLite 单写事务，嵌套会锁等待）；且登记失败只留痕不上抛
     （增强层降级纪律，判读落库不能被拖垮）。
  2. `record_escalation` 必须接受**可注入的 `session_factory`**：判读单测跑 tmp 库，
     若内部直接取全局工厂，**测试会往生产库写待办**。
  3. 单测里 `record_audit` 仍走模块级 `get_session_factory` ⇒ 需
     `monkeypatch.setattr(at, "get_session_factory", lambda: sf)` 才能让审计也落 tmp 库。
- **验证方式**：变异验证 2 次——①把 `if verdict == "escalate"` 改成 `notify` → 待办断言
  精确失败；②删掉 `esc-` 幂等守卫 → 两条测试以 `UNIQUE constraint failed: agent_task.id` 失败。
- **关联**：[[KB-ENG-31]] [[KB-ENG-40]] [[KB-DEC-002]] [[KB-DEC-020]]

### KB-ENG-44 实证核验脚本必须 `import` 生产模块；并常设「自检行」防静默失效
- **背景（2026-09-11，P1-34 隔夜外盘偏向）**：给规则层定权重，必须先用历史数据核验。
  但核验脚本极易变成**第二套实现**——于是核验的是"脚本的口径"，线上跑的是"模块的口径"，
  两者悄悄分叉，**核验结论就无法为线上背书**。
- **正解（两条，缺一不可）**：
  1. **口径单点收口**：核验脚本 `import` 生产模块的 `SPECS` / `evaluate` / 变化量函数，
     自己**只做取数与展示**，绝不重算规则。本轮即把 `change_of(spec, prev, last)`
     抽成模块内唯一入口（含 `×100` 的 bp 换算），脚本与线上同调一处。
     同族的「阈值/常量必须从生产 import」见 [[KB-ENG-39]]。
  2. **常设「自检行」**：分组统计（升/平/降**计数** + |Δ| **中位数**）必须打印。
     守卫作用有两个：①**抓静默失效**——本轮美债全落"平"（`升0 平2879 降0`），
     正是自检行一眼看出来的（根因：收益率以百分数报价，`last − prev` 少了 ×100）；
     ②**给读者一个可独立复核的量级锚点**（"卡在死区"还是"方向真的都是平的"）。
     ⚠️ 只打均值不够——均值会被极端值掩盖，**计数与中位数才能暴露"全判同一档"**。
- **只跑相关性会漏掉"规则能不能用"**：相关性高 ≠ 分组收益有差。核验必须**直接调
  `evaluate()` 走一遍线上规则**（三态分组 + 分组收益 + t 值 + 分年度方向一致性），
  才能回答"这套权重到底有没有区分度"。
- **分年度一致性是廉价而有效的稳健性检验**：本轮 **12/12 年方向一致**，
  比单看总样本 t 值更难造假（单年反转会直接暴露）。
- **允许并鼓励核验结论是否决**：本轮美债 10Y 同口径分组差 +0.103pp、t≈1.6 ⇒
  **权重直接置 0、仅记录不参与判定**（与 [[KB-DEC-018]] 同纪律）。
  核验的价值在"敢删"，不在"找到理由保留"。
- **关联**：[[KB-ENG-39]] [[KB-ENG-40]] [[KB-DEC-018]] [[KB-DEC-019]]

### KB-ENG-49 「关于自身能力的事实断言」必须有单一真相源 + 双向守卫——AI 认知漂移的三类失效点
- **背景（2026-09-11，用户实测）**：「明明系统里都有的数据，助手答『我没有』；它描述自己的能力时也说只有有限快照 + 最多 2 次取数」。逐项复核后是**三类**问题，不是一类：
  1. **提示词自我否认**（主因）：`assistant/prompt.py` 的能力边界写死「你没有实时行情、资金流、龙虎榜等任何实时数据」——那是**无工具时代**的文案；P0-3 接入只读工具后没跟着改 ⇒ 同一份提示词里「可用工具」列着 `longhu`、「能力边界」说你没有龙虎榜。**模型在两段冲突指令中选了"你没有"那一段。** 快照块头（`context.py`）写着「快照之外的实时信息（资金流/龙虎榜/其他个股等）你都没有」是**第二处**同样的矛盾。
  2. **工具覆盖缺口**：端点早就存在、助手侧没登记——分时/K线/个股资金流/个股龙虎榜/公司资料与公告财务/指数与市场宽度/题材梯队/自选/模拟账户/**全网资讯快讯**，共 10 类（工具 17 → 26 个）。**验收时又立刻复现一次**：修完提示词后用户再问「今天热点新闻」，助手仍答「我没有全网新闻/资讯类的实时数据源」——说明**光修措辞不够，清单本身漏项照样触发同类故障**（系统里有 `app/news/flash.py` 双域快讯采集 → `EventStore` → `/api/events*`，就是市场页事件面板的数据源）。⇒ 补 `news` 工具。
  3. **系统地图漂移**：模块图仍写着「每日精选 /picks」「研究 /research」，而这两条路由分别在 2026-09-01 / 09-08 就已 302 下线（现役 `/hunting` `/agent`）。
- **共性**：这三者都是**关于"我（系统）能做什么"的事实断言**，写成了**手写字符串**，于是系统变了、字符串没变 ⇒ 断言过期。与 [[KB-ENG-36]] 同族（清单/数字类内容当事实写死）。
- **纪律（可迁移）**：
  1. **单一真相源**：能力清单/系统地图/工具清单**只许写一遍**，其余位置**派生**渲染——工具清单 ✅ 已是从 `TOOL_SPECS` 派生；系统地图 → 抽 `app/assistant/system_map.py`，提示词由它渲染；前端导航 → 前端是唯一实现方，后端**不复制**。
  2. **双向守卫测试**（不靠人记）：`MODULES.paths` ↔ 前端 `NAV_ALLOWED_PATHS`（集合相等，差异信息要打印出"仅后端有/仅前端有"）；`TOOL_LABELS` ↔ `TOOL_SPECS`（漏标签会退化成英文键名）；能力边界文案 ↔ 工具开关（**开启工具时不许出现否定式断言**。⚠️ 反向也要守：**关掉工具时必须恢复诚实的"我没有数据"**，否则从"自我否认"翻车成"自我夸大"）。
  3. **写前自问**：凡要写下「我（不）能做到 X」这类**关于自身能力**的句子，先问——**它和代码里的能力清单会不会打架？两处冲突时模型会信哪一句？**
  4. **穷举不了就记信号**：新增数据源总会有遗漏，靠人巡检不现实 ⇒ 在服务端把「**没调用任何工具，却声称『我没有这项数据』**」记成 `warning` 日志（`assistant/cognition.py`，纯函数 + 单测），日志累积即**系统自动产出的缺口清单**。注意边界：本轮调过工具再说没有 = 真的取不到（数据源问题），**不算认知缺口**，不记。
- **连带修的两个小坑**：①同文件多 `Edit` 时 `old_string` 只截到一半，把模板的收尾 `"""` 一并吞掉 → 整段变成游离字符串（语法检查才抓到，见 [[KB-ENG-45]] 同族"改一半"）；②测试里新增的辅助函数**同名覆盖**了文件里既有的 `_run(awaitable)`，导致 4 个**既有**用例报 `TypeError: missing argument`——**在既有测试文件里加辅助函数前先 grep 同名**。
- **关联**：[[KB-ENG-36]] [[KB-ENG-42]] [[KB-ENG-48]] [[KB-DEC-018]]

### KB-ENG-55 判据的**口径**比判据本身更要命：用了一个在分布里恒真的量，闸门形同虚设（S2-11）

- **形态**：判据名对、阈值对，**唯独口径取错** ⇒ 在真实分布下几乎恒真，什么也没拦住，且**无任何报错**。
- **实例（2026-09-11 `gate_verdict`）**：[[KB-DEC-019]] 写「中位 >0 且跑赢 ≥50%」，代码取的却是**原始收益**口径
  ——候选B 测试段原始中位 **+3.33%**、跑赢 **68.1%**（判 pass），**市场中性**口径实为 **−0.08% / 49.3%**（应为 observe）。
  **上涨市里原始胜率天然 >50%**，用它当判据等于没设门槛。同族既往：`PROMO_FLOOR=30%` 落在 241 日分布之外。
  两次同一个病：**判据没用它本该区分的基准来归一化**——收益类的基准恒为**同日市场均值**。
- **修法**：只接受 `excess_median` / `excess_win_rate`；拿不到就记 `unchecked` **跳过**，绝不换口径顶替（没验 ≠ 验过）。
- **连带**：登记册里「−0.08%/49.3%」与「+3.33%/68.1%」长期并存未标口径 ⇒ 重跑对不上。
  **只写数字不写口径 = 重跑必然对不上**（已订正，见 `docs/strategy-registry.md §3`）。
- **纪律三问**：① 归一化到什么基准？② 在历史分布里是否恒真（扫一遍分布，别只看当前值）？
  ③ 拿不到所需口径怎么办（显式跳过 + 报出）？
- **判据**：一条**从未被触发过**的判据，先怀疑它恒真，再怀疑数据太干净。
- **关联**：[[KB-DEC-019]] [[KB-ENG-44]] [[KB-ENG-46]]

### KB-ENG-58 口径类常量/档位改动：先量影响面、把「常量项」边界推导到精确、并认清「档位也是口径」

**背景（2026-09-12）**：`intraday_rules` 非防守相位集合看似边角（一个相位、`fit` 项 +3.0 分），实测覆盖 123/249 = **49.4%** 的低热度日、「修复」本身占 **15.0%** 交易日 ⇒ **"影响小"必须实测**：用**生产函数**跑满既有样本（本例 `heat_axis` × 249 日指标库）成本极低、结论常反直觉。
**精确边界**：`fit` 是**相位常量项**（同相位 + 同 `defensive` 标志取值相同）⇒ 改动**不改变组内次序，只翻转「防守 ↔ 非防守」的跨组倾斜**。"会改变排序结果"是**过宽**表述：既不可判真伪，也不能据以决策。**说清语义本身就是核验的一半**。
**⚠️ 档位/阈值也是口径的一部分**：重建首版**只 import 了判定函数、没对齐档位来源**（生产走 `resolve_bands()`：env 覆盖 > 历史分位校准 > 经验值），于是"同一函数、同一输入"仍算出失真结果（退潮 78%、与产线仅 8/10）；补上校准层后产线那个「修复」日被两轴 raw/level 精确复现 ⇒ **核验脚本必须连档位一起对齐**，否则核验的不是线上规则。
**配套**：①有证据才改值、改后留**来源同一性 + 源码级**断言（常量被 import 但表达式没用它 ⇒ 仍要红——只断言常量存在是**假守卫**，实测注入时只红了 1 项）；②阻塞点**具名**；③结论给**双口径**（本例收盘口径与可成交口径结论相反，[[KB-STOCK-31]]：当日封板买不到）；④**扫描范围本身也是口径**：死引用首扫 434 处、真值 45 处（`.next` 构建产物 + 模板占位 + 同名官方文档放大近 10 倍）⇒ **先排除构建产物/占位再计数**，否则统计本身就是错的。
**关联**：[[KB-ENG-55]] [[KB-ENG-32]] [[KB-ENG-44]] [[KB-ENG-46]] [[KB-STOCK-31]] [[KB-DEC-019]]

### KB-ENG-67 「同步」≠「该搬去线程」：read-modify-write 在单线程循环上是原子的，搬进线程池才引入竞态

**决策（2026-09-12 评审批次 5 / G-2）**：同一次审计里，`picks_pipeline` 的 8 处同步 IO
**搬进 `asyncio.to_thread`**，而 watcher `dispatch_alert` 的 `append_alert`
（**读-改-写整个 JSON 简报文件**）**判定不搬**。看上去同类，结论相反——差别在"搬了是否更好"。

**为什么不搬**：同步执行时"读→改→写"之间**没有 `await`** ⇒ 对事件循环而言是**一次原子操作**；
丢进线程池反而**制造并发**：两条 alert 同时 dispatch 时，两个 worker 交错完成读改写，
**后写者覆盖前写者（丢提醒）**——除非另加锁，那是更大的改动。
⇒ **"搬线程"在这里是把一个安全操作改成不安全操作。**

**判据（三项一起看，只看第一项会做出错的改动）**：
① **阻塞时长**（毫秒级还是百毫秒/秒级）② **触发时是否在交易时段**（连累 QuoteHub 的秒级推送）
③ **搬走是否引入新风险**（并发/竞态/跨线程对象）。
`data_health_loop` 是三项全中的（秒级 DuckDB + 交易时段每 15 分钟）；
watcher 单条 alert 是小查询/小文件写，属"①低 ②中 ③高（read-modify-write）"⇒ 不动。

**搬之前必须确认的两件事**：
- 被搬函数**自己新建并使用** session/连接（本仓 `_sf()` / `_db()` 都是 per-call，且
  `check_same_thread=False`）⇒ 不跨线程复用；**持有长生命周期对象（session / 文件句柄）的函数不能整体搬**；
- 该函数**没有已经 `to_thread`**：在 worker 线程里再开线程是纯浪费（反向陷阱）。

**副产物：冗余常与阻塞同源**。管线里 `store.list_events(active_only=True, limit=30)`
**实测跑了三遍**（候选池题材反查 / regime 的 `ev_texts` / 消息命中索引）——
既白读两遍又三度阻塞。收敛成**取数单点**（与 `limit_up_pool` 的 P2-4 范式同型）一次解决两件事；
被收敛掉的那段"接 store 自己查"的函数随之变成**纯内存函数**（零 IO，反而**不该**再包 `to_thread`）。

**守卫写法（两种形态的差别要知道）**：`to_thread(fn, args)` 语法上出现 `fn(` ⇒ 反向断言看得到；
`to_thread(obj.fn)` 传的是**函数引用**，文本里没有 `fn(` ⇒ 反向断言**只在有人改回裸调用时才命中**，
该条目实际只有正向断言在守。另配"**计数桩**"钉"同查询只取一次"（范式同 [[KB-ENG-65]] 第 1 条）。

**失效条件**：纯计算函数（无 IO）搬线程收益≈0 且挤占默认 executor ⇒ 不搬；
阻塞发生在**交易时段之外**（如 15:00 后的调度路径）时优先级下降，但**手动触发的同一入口**
可能落在盘中 ⇒ 判据要按"最短的那条抵达路径"算。

**关联**：[[KB-ENG-65]]（计数桩 / 注入验证）· [[KB-ENG-66]]（守卫的文本扫描）·
[[KB-ENG-19]]（防线自己会错）· [[KB-ENG-44]]（核验要跑生产路径）

### KB-ENG-83 上游故障信号要**结构化**、并**穿透所有中间转换层**；退避按故障性质分档

- **事故（2026-09-14，用户报障「两市成交额怎么没出来了」）**：重启后端触发冷启动全量抓取
  （56 页）时，新浪 WAF 返回 **HTTP 456**，`snapshot_service` 连续两轮失败 ⇒
  全市场快照约 **6 分钟**不就绪 ⇒ 盘面/工作台成交额与全市场宽度全线为空。
  而**同一时刻命令行单发 `curl` 新浪是 200** ⇒ 限流判的是请求特征/瞬时并发，不是整 IP 封禁。
- **失效点一：信号没有结构**。原实现只抛 `ProviderError("sina stock count HTTP 456")`——
  456 这个**最明确的限流码**被塞进字符串，调用方只能匹配文本才能分辨，于是它退化成与
  "源坏了"完全同类，全部共用同一套指数退避（120s → 240s → 300s cap）。
  **实测那两轮都仍在限流窗口内**（13:31:59 / 13:33:59 失败，13:38:04 才成功）。
  修法：`SinaRateLimited(ProviderError)` 单独成类 —— **消息文案会改，类型不会**。
- **失效点二（更隐蔽）：刚建好的类型会在中间层被抹平**。分页抓取走
  `asyncio.gather(..., return_exceptions=True)`，其后的循环对每个异常统一
  `raise ProviderError(f"sina page fetch failed: {item}")` ⇒ **限流类型到此消失**，
  `run()` 的限流分支**永远命中不了**。不专门为这一步写守卫的话，代码"看起来已经修了"。
  ⇒ **纪律：异常类型也是契约** —— 任何"把异常转成新异常"的中间层（gather / 线程池 /
  重试装饰器）都必须**显式放行需要保留的子类**。
- **失效点三：`except` 顺序**。`except SinaRateLimited` 必须排在 `except Exception` **之前**；
  顺序反了会被父类兜住、判据永不生效，而**所有行为用例仍然全绿**（它只是退化成普通失败）。
  ⇒ 这类"失效却全绿"的判据必须**静态钉**（AST 读 `Try.handlers` 顺序），行为测试补不上这一格。
- **退避要按故障性质分档，且优先级显式**：限流冷却（**渐进式 240 → 480 → 900s 封顶**）
  **优先于**休市降频 `240s`（后者是按"上游正常、只是数据静止"设计的，限流期用它 =
  继续按正常节奏敲上游）。判据抽成纯函数 `_next_delay(live)` —— 原先写在 `while True` 里，
  只能靠"跑一整天看日志"验证。
  ⚠️ **为什么不是一个大固定值（实测推翻的初稿）**：初稿按"防封禁"直觉给**固定 600s**，
  实施后复核实测发现本环境限流是**间歇**的——观测窗口内**失败 5 / 成功 5 交替**
  （13:42:17✓ 13:43:17✗ 13:45:17✗ 13:49:22✓ 13:50:22✗），两次**恢复间隔都是 245s**
  （13:33:59✗→13:38:04✓、13:45:17✗→13:49:22✓）⇒ 固定 600s 会用恢复速度换来**虚假**的
  封禁保护。
  🔴 **但「245s 内必恢复」是本条自身的一次过度概括（n=2），当日即出现反例**：
  13:56:59 末次成功之后连续失败，**至 14:14:43 实测 `age` 已达 1064s、`fails=3`、仍在用旧数据**
  （`state=degraded`，值未丢——这正是 [[KB-ENG-48]] 续篇要的三态披露在起作用）。
  重试间隔实测 **240s → 480s →（900s 待发）**，与渐进档位**逐一吻合** ⇒ **反证结构化路径确已在生产生效**。
  🔻 **该反例同时暴露本档位的固有代价：恢复探测延迟最坏 900s**（旧口径在 cap 处仅 300s）——
  "更保守的退避"换来的不只是保护，还有**更慢地发现上游已恢复**。⇒ **首档 240s 与封顶 900s
  都只据 n=2 定，须更长观测后重估**；本项在账本 §6.6 行 23 中已降为**🔶 观察 / 待重估**，
  **不作为已验证结论传播**。
- **可迁移的五条**：① 上游故障码里有明确语义的（429 / 456 / 503 + Retry-After）**一律单独成类**；
  ② 结构化之后要**逐个中间层确认它还在不在**；③ "按故障性质分档"必须配一条**优先级断言**
  （否则重构时最容易被挪到默认分支后面，且不报错）；④ **退避档位不是"越大越安全"**——
  幅度必须由**实测恢复窗口**定，而不是由"防封禁"的直觉定。
- 🔴 **本环境有两种限流形态，「不是整 IP 封禁」这条只覆盖第一种**（2026-09-14 21:30 实测补充）：
  ① **间歇性特征限流** —— 间歇 456 而 `curl` 单发仍 200（13:31 那次观察），分钟级自愈；
  ② **IP 级封禁** —— 响应体换成「拒绝访问」页，明写「**你的 IP 存在异常访问…已被新浪安全部门封禁**，
     停止异常访问一段时间后（**5~60 分钟**）会自动解封」，并附拦截详情 ID / 时间 / IP。
  ⇒ **判别要看响应体（JSON 还是拒绝页），不能只看状态码**——两者都是 456，处置完全不同。
- **探测动作本身会触发/加深封禁，且恢复窗口远大于直觉**（同轮实测）：矩阵探针只留 **20s** 档间间隔，
  而实测恢复窗口是 **245s 量级** ⇒ 后两档测到的是**前一次的残留**而非「配置差异」。
  **纪律：探针档间间隔必须 ≥ 实测恢复窗口。**
- **轮内节流无法对抗 IP 级封禁**（同轮实测）：`concurrency=1` + 批间隔 `0.15s`（≈2.7 请求/秒）在窗口已被
  占用时仍跑到**第 32 页**被封，而**生产全速（≈10+ 请求/秒）那轮反而成功** ⇒ 判据不是瞬时速率，而是
  **时间窗内累计请求数** ⇒ 「轮内分批/串行 + 令牌桶」**结构上**救不了首请求。2026-09-14 21:45 已按
  「错峰」实现（首轮延迟，见 `COLD_START_SNAPSHOT_DELAY_SECONDS`），实测两轮连续成功，**但增量价值未被证实**
  （对照推算：不延迟时窗口内请求数几乎相同）——判别方法、备源链等**新成因**见 [[KB-ENG-88]]。
- **封禁期重试成本很低**：`fetch_market_snapshot` 的首个请求就是 `stock count`，它 456 会**立即**
  `raise`（不进 56 页分页）⇒ 一轮失败 = **1 个请求**，故退避重试本身不会显著延长封禁。
- **关联**：[[KB-ENG-65]]（判据失效却全绿）· [[KB-ENG-72]]（守卫覆盖面）·
  [[KB-ENG-48]]（空态吞披露：本轮同源的另一半）

### KB-ENG-88 上游配额故障要查「备源链级联」与「同域其他调用方」——只看直接调用方会找错源头

- **场景（2026-09-14，`OPS-001`）**：新浪返回 456。第一反应是查 `sina_market` —— 本仓唯一的
  「全市场快照」入口。**但配额是「IP + 域名」级的，不由调用方决定**，只查直连模块会漏掉真实来源。
- **漏点一：备源链会把请求**汇入**目标源**。`core/config.py` 的 `provider_fallbacks`
  = `"tencent,eastmoney,sina"`（主源 `ths`）⇒ `CompositeProvider` 逐级降级：
  **ths / tencent / eastmoney 任一环异常，请求就往下推一档，最终落到新浪**。
  ⇒ **上游越不健康、目标源收到的请求越多** —— 故障期与高负载期重合，恰是最脆弱的时刻。
  （本次冷启动实测该链未大量触发：新浪仅收到 5 个突发请求；但**假设仍成立**，只是未复现。）
- **漏点二：同域还有别的调用方**。除 `market/sina_market.py` 外，至少还有
  `data_providers/sina.py`（行情 / 板块 / 资金流）、`market/minute_backfill.py`、
  `market/fund_flow.py`（`quotes.sina.cn`）、`services/akshare_ext.py`。
  域名不完全相同（`vip.stock.finance…` / `quotes.sina.cn` / `hq.sinajs.cn`）
  ⇒ **限流粒度要先确认是「按域名」还是「按主域」**，否则会把总量算错。
- **纪律**：定位「谁打满了上游配额」时，搜索面 = **① 直连该源的模块 + ② 把该源列为备源的上游链**。
  缺任何一项，都会得出「我这边才发了几十个请求，不可能超」的错误结论。
- **反向教训：「放大分页」这类"最直接"的降总量手段未必可用**。实测 `num=500` 返回
  **HTTP 200 但只有 100 行** —— 服务端**静默截断**且**不报错**。只看状态码会当成成功；
  本仓 `len(rows) < total * 0.9` 恰好兜住，但它**本意是「快照不完整」**，属偶然覆盖。
  ⇒ 服务端对分页参数的静默截断，是「参数看起来生效了」的典型陷阱（同族 [[KB-ENG-82]]）。
- **关联**：[[KB-ENG-83]]（故障信号结构化 / 退避分档）· [[KB-ENG-82]]（静默改写与丢弃）·
  [[KB-ENG-71]]（守卫覆盖面）· [[KB-ENG-65]]（判据失效却全绿）

### KB-ENG-89 入库前的「预判」是假设不是结论——要在全族上实测，不能只验证一个假设

- **场景（2026-09-14，`RSH-003` 入库 `mfi20` / `obv20`）**：新增因子前在 `library.py` 的 `note`
  里写了两条预判，**两条都被真实库实测修正**。共同根因：**把假设写成了结论**。
- **预判一（方向）**：note 写「预期正 IC（净流入强=资金关注）」。实测 **IC 为负**
  （`mfi20` −0.046 / `obv20` −0.042），与 `mom20`（−0.064）同号——A 股 20 日尺度上反转效应主导，
  「资金流入强」在该尺度是**反向**信号。
  - ⚠️ **本仓已有明文纪律**：`backend/tests/test_factor_report.py` 写明「方向**必须由实测 IC 定**，
    不能取 `library.FactorDef.note` 里的"预期方向"」⇒ 这类预判**已知不可靠**。
  - 实测复核：40 个因子里 **6 条** note 含方向预判，**4 条命中、2 条写反——写反的正是最新加的两条**。
  - **正解**：note 里**不预设方向**，或写「方向以实测为准 + 实测日期与读数」。
    （判定本身不受影响：准入只取 **|IC| / |ICIR|**、不看符号——这也是写反了仍 PASS 的原因。）
- **预判二（冗余对象）**：note 写的比对面是 `vsumd20`（理由「方向取自量变化，本因子取自价格方向」），
  实测相关 **0.62** ⇒ **该判断成立**。**但它漏掉了族内真正的近邻**：
  `mfi20`~`sump20` **0.963**、`obv20`~`sump20` **0.948**、`mfi20`~`obv20` **0.969**（均远超 0.70 线）。
  - **根因（方法论）**：`vsumd20` 是**量**方向的 RSI 同构，而 MFI / OBV / SUMP 三者都是**价**方向的
    RSI 同构（`sump20` 等权 / `mfi20` 额加权 / `obv20` 量加权）⇒ **族内核是「方向取自哪里」，
    不是「名字像不像」**。按「同族名」找比对面，恰好跳过族内核上最接近的一个。
  - **纪律**：去重预判**不是「挑一个最像的对象算相关」**，而是
    **① 先按「结构不变量」（方向来源 / 口径 / 加权方式）把族画全 → ② 族内逐个算 → ③ 只把
    「你认为最像的」当作待证假设、而非验证对象**。
    ⚠️ 只验证一个假设的缺陷：**「验证成立」与「找全了」是两件事**——`vsumd20` 的 0.62 完全正确，
    却给出「不是同一件事 ⇒ 有独立价值」的错误整体印象。
- **附带读数（可复用基线）**：`sump20`~`vsumd20` 实测 **0.627**，与两枚新因子~`vsumd20`
  （0.6248 / 0.6247）**几乎相同** ⇒「价方向 RSI 族 ~ 量方向 RSI」的天然相关稳定在 **~0.63**
  ——**远低于 0.70、却也不独立**，可作「同族不同方向」的相关水平参照。
- **处置**：两枚因子**仍入库**（与既有惯例一致——同族冗余的 PASS 一律「保留登记、不重复计权」，
  见 `docs/factor-lifecycle-governance.md` §4.3），但 note 已改为「方向不预设 + 实测读数 + 族内近邻」。
- **关联**：[[KB-ENG-86]]（注入要改序——同为「预判与实测不一致」）· [[KB-ENG-65]]（判据失效却全绿）·
  [[KB-ENG-72]]（清单覆盖 ≠ 真实覆盖面：**这里缺的是「邻居覆盖」**）

### KB-ENG-92 双向可达的两个弹窗层要「状态与渲染宿主分离」；改点击行为不等于废掉链接
- **需求**（2026-09-15 用户）：工作台右栏的个股/指数详情是全站最重的面板，其他页面看某只个股只能
  跳 `/workbench`，**丢掉当前页的列表与 tab 上下文**，看完还要按「← 返回来源页」回来。
  改造＝把详情面板封装成**全站通用弹窗**，所有"跳工作台看详情"的入口改走弹窗。
- **决策一｜复用面板，不复制实现**。`StockDetailPanel` 是**自治**的（给 `symbol` 就自行取
  K线/盘口/逐笔/分时/资金/财务/资讯，并自带 WS）、**不读 URL、不依赖页面 context**，且已被
  `/stock/[symbol]` 复用 ⇒ 弹窗内原样渲染同一个组件。收益：面板任何能力增强**自动惠及弹窗**，
  不存在两份真相源；代价：弹窗必须给足尺寸（它是完整功能面，不是摘要卡）。
- **决策二｜两个弹窗层互相要求对方在"内层"——嵌套解不了（本条目最值钱）**：
  - 详情弹窗里的面板要调 `useDetailModal()`（相关事件行）⇒ 弹窗必须渲染在 `DetailModalProvider` **内层**；
  - `detail-modal.tsx` 的「查看个股详情」要调 `useSymbolDetail()` ⇒ 它必须在 `SymbolDetailProvider` **内层**。
  - 两个 Provider 直接嵌套**必然有一侧落在外层拿到 noop 默认值**，症状是"点了没反应"——本仓
    2026-09-09 已在通知抽屉踩过同款（Provider 没包住 NavBar）。这是一个**偏序矛盾**，加一层包不住。
  - **解法**：把 **state** 与 **渲染宿主** 拆开——Provider 只提供 context + state，弹窗由
    `<SymbolDetailModalHost/>` 渲染，Host 放进 `DetailModalProvider` 的 children 内，两个方向同时满足。
  - **一般化**：A 的渲染产物需要 B 的 context、B 的渲染产物也需要 A 的 context 时，用
    「**Provider 供状态 + 独立宿主在需要处渲染**」，不要试图用嵌套。
- **决策三｜改点击行为，不等于废链接**。左键点击＝就地弹窗（`symbolDetailClick` 统一点击判定，
  ⌘/Ctrl/Shift/中键**放行**浏览器默认行为）；`href` 仍保留 `workbenchUrlWithBack(symbol)`。
  三条收益：① 右键/中键/⌘+点击仍能新标签打开真实 URL；② 分享链接、`/stock/[symbol]` 中转页、
  助手深链全部继续有效；③ 既有断言 `a[href^="/workbench?symbol="]` **一条都不用改**
  （本轮实测：改造后前端既有用例全绿，增量全在新增文件）。
  **反例警示**：把 href 一并删掉会同时废掉跨页分享能力与既有断言，而"点击弹窗"这个新行为
  **不会因此更正确**。
- **决策四｜深链意图要能反解析**。`?ct=` / `?rt=` 原本只有解析侧（`app/workbench/page.tsx` 读 query）。
  弹窗化后助手回复里的"600519 的资金流向图"必须**直接开在资金图页签** ⇒ 新增
  `parseWorkbenchDetailUrl`（`lib/detail-tabs.ts`），与 `lib/nav-targets.ts` 的构造器**互逆**，
  并由交叉断言钉住——构造 ↔ 解析漂移**不会报错，只会静默开错 tab**。
  ⚠️ 安全边界：只解析**站内相对路径**；带协议的绝对 URL 与 protocol-relative（`//host/…`）
  **一律拒绝**，否则 `https://evil.com/workbench?symbol=600519` 会被当成站内详情弹窗。
- **决策五｜"弹窗 vs 页内切换"要有唯一判据**。工作台右栏就是同一份面板 ⇒ 在工作台上再弹一层
  既冗余，又会让「左栏点自选＝切右栏」与「搜索框选股＝弹窗」行为分叉。故 Provider 在
  `pathname === "/workbench"` 时**回落为 `router.replace` 页内切换**，并原样保留 `from`
  （否则跳过来再切股会丢掉「← 返回来源页」）。
- **循环依赖断环**：`detail-modal → symbol-detail-modal → stock-detail → stock-events → detail-modal`
  是真实存在的环。把 context 抽到**零业务依赖**的 `symbol-detail-context.ts`，让 `detail-modal`
  只依赖该文件，环即消失（也顺带让业务侧不必把 957 行的面板子树拉进依赖图）。
- **验收（实测，非推理）**：盘面页点涨停池个股 → 弹窗且 URL 仍 `/tape?tab=limitup`；市场页点指数卡 →
  `aria-label="指数详情 sh000001"`、右列自动收窄为「涨速/板块」、图表区只留 K线/分时
  （**指数专属分支在弹窗内同样生效**）；尺寸实测 **1229×582 @ 视口 1280×633** ＝
  `min(1600px,96vw) × min(1000px,92vh)`；Esc / 关闭按钮 / 点遮罩均可关；工作台内选股 →
  **不弹窗**、URL 变 `/workbench?symbol=600519`。
- **同日第二轮｜5 个弹窗收敛到统一外壳**。第一轮只解决"标的详情能被任何页面打开"，**弹窗外壳仍是
  5 份**（news / concept / pick / detail / symbol）——`detail-modal` 自己的头注就写过"此前三者各写各的"，
  结果从 3 份涨到 5 份。后果不是丑，是**行为不一致**：遮罩点击有的 `onMouseDown + target 判定`、
  有的 `onClick + stopPropagation`（后者要求面板显式阻止冒泡，**漏一处就"点正文即关闭"**）；
  `role="dialog"` 有的挂遮罩、有的挂面板；遮罩浓度与毛玻璃混用。
  ⇒ 抽 `components/ui/modal-shell.tsx`（尺寸三档 + `zIndex` + 头/体/尾三槽 + 统一关闭按钮），
  5 个弹窗全改走它，用 **10 条判据**钉住共同不变量。**两条可复用判断**：① `sm|md` 高度随内容
  （`max-h-85vh`）、**`lg` 必须给确定高度**——详情面板靠 `flex-1` 撑图表；② 业务侧一律从**零依赖的
  `symbol-detail-context`** 取 `useSymbolDetail`，别把面板重子树拉进依赖图。
  ⚠️ **代价要显式认领**：关闭按钮由文字改 × 图标（`aria-label="关闭"`）⇒ 上一轮按文案写的
  `getByText("关闭")` 当场变红；**改外壳必然翻掉按文案写的断言**，改按无障碍名查即可。
- **关联**：[[KB-ENG-16]]（高度链——弹窗内容区必须用 grid 让面板拿到**确定高度**，否则图表撑不开）·
  [[KB-ENG-85]]（能力描述失真：文档里写"跳工作台"就会持续误导后来者）

### KB-ENG-96 宽泛 `except` / `suppress` 会把 `ImportError` 吞成**静默失效**（2026-09-15）

**场景（实测，两条独立事故）**：

1. **本轮新写的代码**：`api/routes/picks_intraday.py` 把 `attach_participants` 写在
   `from app.picks.tradability import ...` 的导入行里——它实际在 `app.picks.intraday_opportunity`。
   该导入位于 `try/except Exception`（诚实降级用），于是 `ImportError` 被吞成 payload 里的
   一句 `题材联动挖掘失败（ImportError）`：**功能恒空、204 个前端用例 + 3067 个后端用例全绿**。
   **是启动服务看真实渲染才暴露的**——不是测试抓的。
2. **既有的代码**：同一文件的台账登记段 `from app.market.trading_status import in_trading_window`
   ——该函数在 09-11 的 P1-3「交易时段窗口收口」里被移到了 `app.market.trade_calendar`，
   **调用点没跟着改**。它同样在 `contextlib.suppress(Exception)` 里 ⇒ 该段**自写入以来从未执行过**
   （题材候选一只都没进过台账）。
   新增守卫同日又抓出第三处：`picks/morning_brief.py::_daily_plan` 的两行导入指向
   **根本不存在的模块**（`app.picks.review_store` / `app.models.review`）⇒ 简报「今日计划」的两段恒为空。

**为什么危险**：`except Exception` 在选股链路上是**正确**的（数据源缺失要降级不要 500），
但它对**拼写错误零容忍度为零**——"数据源挂了"与"我写错了模块名"在观测上完全同形，
而前者天天发生、后者一次就永久静默。判据越宽，事故越像正常。

**修法（已落地，通用而非定点）**：`tests/test_import_lint.py::test_function_level_imports_are_resolvable`
—— AST 取出 `app/` 全部 `from app.x import y`（忽略 `*`），`importlib.import_module` + 名字校验；
名字取不到时**再试子模块**（`from app.api.routes import picks` 导入的是模块，只查 `hasattr` 会误报）。
配套 `test_dead_import_exemptions_are_still_real` 保证豁免清单不会长成只增不减的垃圾清单。
**两条纪律**：
- 宽泛 `except` 里的**每一个导入**都值得怀疑——它们是"永远不会报错的失败点"；
- 日志要带 `exc_info=True`：本处第一版只打 `"挖掘失败: %s"`，从零重查花的时间远超多打一行的成本。

**关联**：[[KB-ENG-95]]（同类：判据面选错 ⇒ 本地恒绿 / CI 恒红）· [[KB-ENG-94]]（推演型守卫的假绿）·
[[KB-ENG-61]]（陈旧数据比缺失更危险，同族"观测不到"问题）
