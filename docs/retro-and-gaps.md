# 项目盘点（2026-08-29）——欠缺 / 布局待优化 / 技术债

> 触发：用户要求"回看还有哪些欠缺、哪些布局不合理、哪些需要完善优化"。逐项诚实盘点，
> 作为后续迭代的**唯一明细账本**（阶段级索引见 README/PROJECT-MASTER，跨计划复盘见 plan-review.md）。
> 完成一项划一项。

## 一、功能欠缺（按用户影响排序）

| # | 项 | 说明 | 计划 |
|---|---|---|---|
| ~~1~~ | ~~**交易前端**~~ | ✅ 已完成：右列"交易"页签（买卖表单+持仓+挂单+成交记录） | — |
| ~~2~~ | ~~**真实 B/S 点**~~ | ✅ 已完成：`candle.setMarkers()` 已调用（此前 markers 构建后从未挂载，且循环重复两次） | — |
| ~~3~~ | ~~**持仓视图**~~ | ✅ 已完成：持仓列表 + K 线黄虚线成本线 + 左栏持仓组（2026-08-30：仅有持仓时渲染，行点击选中该股，休市日盈亏 null 显示 --） | — |
| ~~4~~ | ~~**新闻/公告无摘要**~~ | ✅ 已完成 v1（2026-08-30）：`app/news/`（规则摘要器 + LLM 可插拔降级，与 `app/review/` 同构）+ `GET /api/news/digest/{symbol}`；每条附重要度（加权分+命中项）、消息面情绪（+命中词）、事实摘要、关键数字。**东财部分 summary 是行情表原文，检测到即整段丢弃并回退标题**（`digest_source` 说明原因），绝不润色补全。资讯页签已渲染徽章与摘要 | 仅余 LLM 增强层（待凭据） |
| ~~5~~ | ~~概念题材 chips 混入风格标签~~ | ✅ 已完成：`classify_boards()` 拆成 行业/地域/概念题材/风格·指数 四组分别渲染，`boards` 保留全量不丢数据 | — |
| ~~6~~ | ~~**强势题材梯队看板**~~ | ✅ 已完成：`/api/themes` + `/themes` 页面；以题材容器重组涨停池，输出连板天梯/成建制分级/阶段判断/健康度结论/断板股；题材归一化去重避免重复 symbol；快照按交易日匹配保证溢价口径正确 | — |
| ~~7~~ | ~~**选股器 / 评分系统**~~ | ✅ 已完成 v1（2026-08-30）：`/screener` 页 + `/api/screener`——快照截面过滤（涨幅带/成交额/换手/排 ST/北交/次新）→ 候选池 Top150 拉 TDX 日K（QFQ）→ 六维评分卡（趋势/MACD/KDJ/RSI/量价/流动性，权重和 1，可解释依据+失效条件）；防飞刀三修正（零轴下 MACD 不计 bull、空头排列超卖衰减、放量下杀≠温和放量）；缓存 30 分钟，真实跑 17.6s 148/150 | 预警通知另行 |
| ~~8~~ | ~~回测引擎 + 历史回放~~ | ✅ 全部完成：回测引擎 v1（2026-08-30）`app/market/backtest.py` 代码级防泄露（as_of 视图越界抛 FutureDataError / T+1 / 一字板拒 / 停牌无 bar / 费用全配置化）+ 12 个防泄露测试先于引擎合入 + `/api/backtest/run` + `/backtest` 页（净值 vs 基准曲线+指标卡+交易表）；内置 ma_cross / ma_breakout。**历史回放 ✅ 同日完成：`replay-chart.tsx` 逐 bar 推进（播放/单步/3 档速度/起点选择），技术评估随切片实时重算，不画当前成本线防未来函数** | — |
| ~~9~~ | ~~预警通知~~ | ✅ 已完成 v1（2026-08-30）：`alert_rule`/`alert_event` 表 + alembic 迁移 + `AlertEngine` 5s 轮询行情 + 通知通道抽象（默认 in_app/log，真实通道可插拔）；REST API（规则 CRUD、事件列表、确认、通道列表）；`/alerts` 管理页（新建规则/启用停用/触发记录/确认）。待用户选择后接入 email/企微/飞书/telegram/webhook 等真实通道 | Phase 8 后续只余通道接入 |
| ~~10~~ | ~~左栏自选 sparkline 迷你走势~~ | ✅ 已完成（2026-08-30）：`GET /sparkline` 批量 TDX 日K收盘（30 根，5min 缓存，失败跳过）+ `Sparkline` 纯 SVG 组件（红涨绿跌按区间首尾），workbench 左栏自选每行渲染 | — |
| ~~11~~ | ~~交易日历节假日依赖 ths~~ | ✅ 已完成（2026-08-30）：成功抓取后持久化 data/trade_calendar.json，双源全挂时读兜底文件（带时间戳的权威快照，非猜测）+log warning；无兜底文件才抛错，"拒绝猜测"语义保持 | — |
| ~~12~~ | ~~🔴 情绪判定：非交易日日期回退 → 自指计算~~ ✅ 已修复（sentiment-phase-review.md：交易日锚定+哨兵值） | **周六调用时 `push2ex` 静默回退到周五，今日池=昨日池=周五，再查周五收盘快照 → 涨停股查自己涨停那天的价，恒等于 +10%。产出"昨涨停均值10.38%/翻红率100%/再涨停率100%"的假指标，阶段误判为「高潮」且置信度"高"。正确结论应为「分歧」。** | **P0，见 `docs/sentiment-phase-review.md`** |
| ~~13~~ | ~~🔴 情绪引擎缺晋级率~~ ✅ 已修复（promotion_rates，1进2/2进3/3板+，权重 0.25） | `docs/sentiment.md` 清单写了 1进2/2进3/3板+，`engine.py` 从未实现。业界公认 1进2 是最敏感指标（≤25%=冰点/退潮，≥40%=升温）。实测 8/28 为 16.4%，连续 4 日 11%–20% | **P0** |
| ~~14~~ | ~~阶段规则：热度指标无反向校验~~ ✅ 已修复（赚钱效应轴可否决热度，双轴矩阵） | 高潮判据只看 高度/均值/涨停数 三项热度指标，赚钱效应转弱无法否决 → 六阶段里的「分歧」实际永远触发不了。**题材梯队看板已修复此原则（赚钱效应可否决热度）；情绪引擎尚未同步。** | P0 |
| ~~15~~ | ~~只用均值不用中位数~~ ✅ 已修复（median_pct 进赚钱效应轴） | 8/28 均值 3.17% vs 中位 1.97%，背离 1.2pct，均值掩盖多数人体感。**题材梯队看板已改用中位数；情绪引擎尚未同步。** | P1 |
| ~~16~~ | ~~炸板率未用真实炸板池~~ ✅ 已修复（优先真实炸板池家数，缺失退回差值法并标注近似） | 用"价格法−封单法"差值近似，且不参与阶段判定；真实炸板池 `getTopicZBPool` 已实测可用（8/28：16 只） | P1 |
| ~~17~~ | ~~休市日数据未标 stale~~ ✅ 已修复（2026-08-30：QuoteHub 休市状态沿触发，非交易日/非交易时段 mark_all_stale("market_closed")，日历不可用不干预） | 周六 `received_at=08-29T11:53` 但 `ticktime=16:29`（周五收盘），仍报 `is_stale=False/is_realtime=True` | P1 |
| ~~18~~ | ~~情绪周期是单点截面，无序列~~ ✅ 已完成（2026-08-30）：`sentiment_history` 表（alembic a7c3e91d2f44）+ save_report 钩子自动落库 + 报告回填（幂等不覆盖）+ 交易日 15:05 后惰性补录 + `/api/market/sentiment-history`（强/中/弱分段周期起点定位）+ market 页 10 日温度条形序列 | P2 |
| ~~19~~ | ~~🔴 下单价被千分位截断~~ ✅ 已修复（2026-08-30：`lib/format.ts` 新增 `parseNum()` 去掉千分位再解析） | 价格输入框回填 `fmt(price)`（zh-CN 千分位），`parseFloat("1,297.40")` 在逗号处截断成 **1** → 预估金额显示 100.00、风控按 ¥1 计算（可买上限虚高到 249700 股），**提交时实际下单价变成 1 元**。仅交易表单受影响（`alerts`/`screener` 用的是 `type=number` 输入框无此问题）。已加 2 组回归测试 | **P0** |
| ~~20~~ | ~~风控预检查体量口径错误~~ ✅ 已修复（2026-08-30）：持仓缺实时价回退**成本价**；`account_summary` 补 `initial_cash` | ① 总仓位原回退到「本次订单价」，600519 的 200 股按 11.50 计成 2300 元，总仓位显示"当前约 0%"、70% 上限形同虚设；② `account_summary` 不返回 `initial_cash`，回撤保护 `initial=equity` 导致 pnl 恒为 0、永不触发（现改为显式告警而非静默退化） | **P0** |

## 二、布局待优化

| # | 项 | 现状 | 方向 |
|---|---|---|---|
| ~~1~~ | ~~右列宽度固定 300px~~ | ✅ 已完成（2026-08-30）：右列左缘拖拽条调宽（260-480px clamp，localStorage 持久化，切股重挂载后恢复；grid CSS 变量注入，窄屏单列不受影响） | — |
| ~~2~~ | ~~图表区副图高度均分固定~~ | ✅ 已完成（2026-08-30）：副图顶缘拖拽条（0.10-0.45 占比 clamp，localStorage 持久化），拖拽时 priceScale.applyOptions 动态调整不重建 chart，主图 bottom 随之让位 | — |
| 3 | 盘口仅 L1 五档 | L2 付费（¥298/年），无免费源 | 预留 Provider，未来接入 |
| ~~4~~ | ~~分时图无均价线~~ | ✅ 已完成（分时 P0：均价黄线=cum_amount/累计量） | — |
| ~~5~~ | ~~自选列表无 sparkline~~ | ✅ 已完成（2026-08-30）：workbench 自选每行渲染 30 日收盘迷你走势 | — |
| 6 | 指数卡收起后左栏上部空 | 收起时仅剩一行 | 可接受（用户主动收起） |

## 三、技术债

| # | 项 | 风险 | 处置 |
|---|---|---|---|
| ~~1~~ | ~~StockDetailPanel ~600 行单文件~~ | ✅ 已完成（F3：424 行壳 + components/detail/ 6 个子组件） | — |
| ~~2~~ | ~~前端零测试~~ | ✅ 已完成基建（2026-08-30）：vitest + lib 纯函数 25 测试 + **组件渲染测试 5 个**（jsdom + @testing-library/react，TradeForm 覆盖千分位解析/整手校验/风控拦截禁用/可买上限展示）。~~**已知缺陷：analyze 下跌中 KDJ/RSI 超卖计 bull**~~ ✅ 已修复并补测（commit 9bf74e7 + 2026-08-30 补量价维度）：防飞刀三条修正现与后端 `tech_score` 完全对齐——① 零轴下 MACD 不计 bull；② 空头排列下 KDJ/RSI 超卖衰减为 neutral；③ **放量下杀 ≠ 温和放量**（新增量价维度，量比阈值 4.0/2.5/1.0/0.6 与后端一致）。组件渲染测试 ✅ 已扩展（jsdom + RTL，5 个 TradeForm 用例）；更多组件按需增补 | — |
| ~~3~~ | ~~Next 15.5.24 outdated~~ | ✅ 已完成（2026-08-30）：升级到 next@16.3.3 + eslint@9.39.4 + eslint-config-next@16.3.3；`.eslintrc.json` 迁移为 `eslint.config.mjs` 原生 flat 配置；`next lint` 被移除，CI 改为 `npx eslint .`；加 `allowedDevOrigins: ['127.0.0.1']` 允许 127.0.0.1 来源访问 dev HMR/chunk；tsc/eslint/vitest/next build 全绿，CI 绿。**挂账**：20 条 `react-hooks/set-state-in-effect` warn（全部在 effect 异步回调写状态的经典取数模式），已降为 warn 并注释在 `eslint.config.mjs`；彻底清零需把取数收敛到 use()/Suspense 或数据层，独立重构 | — |
| ~~4~~ | ~~新闻/公告每次切股重新拉取~~ | ✅ 已完成（2026-08-30）：announcements/news 进程内 60s TTL 缓存（meta.cached 标注），源只打一次 | — |
| ~~5~~ | ~~paper_matcher 5s 轮询挂单~~ | ✅ 已完成（2026-08-30）：match_pending 返回剩余挂单数，无挂单 30s 降频、有挂单 5s 密集轮询 | — |
| ~~6~~ | ~~全局仅一个模拟账户，无重置入口~~ | ✅ 已完成：`POST /api/paper/reset` + 交易页签重置按钮（二次确认） | — |
| ~~7~~ | ~~error.tsx 全局错误边界未做~~ | ✅ 已完成（F1：error.tsx/global-error.tsx/loading.tsx） | — |
| ~~8~~ | ~~`GET /api/themes/reconciliation` 不带 `?date=` 必 502~~ | ✅ 已修复（2026-08-31，commit fb833f1）：路由先过 `_default_trade_date_async` 再拉池；回归测试的桩复刻真实契约（None 即抛），不解析日期时必红 | — |

## 四、已确认的行为基线（勿回退）

- 布局锁定一屏：容器内滚动，无整页滚动条
- 红涨绿跌；tabular-nums 对齐；所有数据带 来源/时间/质量 标注
- mock 数据永不冒充实盘；数据源失败标 stale 不伪造
- 撮合规则：T+1/涨跌停拒/整手/费用/停牌拒——不可绕过
- 技术结论只给偏向+依据，禁止确定性买卖建议

## 五、增量账（2026-09-03 ~ 09-04，保持唯一账本有效性）

| 日期 | 项 | 说明 |
|---|---|---|
| 09-03 | ~~联动切片 E（L7/L8/L10 跳转）~~ | ✅ eea9e66：热力图格/回测条/题材卡 → 详情/题材页，全经 `lib/routing.ts`（+`themesUrl`） |
| 09-03 | 工作台刷新「非法」瞬态修复 | ✅ a9ea71e：开盘「未建立」形态 high/low=0 不再判越界（validator 落 `unset_high_low` 降级），+4 测试；待下个交易日开盘实测 |
| 09-03 | ~~联动切片 G（剩余 x→详情入口）~~ | ✅ 6ce8d75：市场页指数卡/涨停速览、每日精选卡片/复盘行、复盘 findings 内嵌代码 → `workbenchUrlWithBack` |
| 09-03 | 盘中 unknown + 自动刷新/状态条 | ✅ 6dee7fe：intraday 60s 轮询 + 全失败警示 + 数据时间行；飞书异动提醒 v1（`picks_watcher_channels` 含 feishu，等 webhook）；WS 连接状态恢复渲染（stock-detail） |
| 09-03 | ~~ESLint `set-state-in-effect` 26 warn 清零~~ | ✅ a155d49：B 类 11 处收编 `use-polling-fetch`（latest-ref）、A 类 9 处渲染期 adjust-state、C 类 3 处豁免；现仅剩 1 条既有 exhaustive-deps |
| 09-04 | 分时图纵轴按板块涨跌幅限制 | ✅ ad74262：`lib/price-limit.ts`（主板±10/创业科创±20/北交±30/ST±5/指数回退），涨停/跌停虚线贴边；canvas 像素采样实测三标的 |
| 09-04 | 自选动态分组（每日精选/盘中跟踪） | ✅ ba3c201：workbench chips 两动态分组（`top_watch_stocks` + `GET /api/picks/intraday-top`），60s 自动更新 |
| 09-04 | 复盘新增 picks 准确率维度 | ✅ ba3c201：逐股归因自动触发（复盘前置步）+ 准确率/失误逐股 findings/类别占比建议；review-tab 补 findings 渲染（原整层丢失） |
| 09-04 | ~~sentiment P2 #14 盘中情绪监控本体~~ | ✅（本轮）：`app/sentiment/intraday_monitor.py` 三类纯规则事件 + 告警链 + /api/system/providers 快照，+9 测试；阻塞解除（飞书通道已落地）后完成 |
| 09-04 | ~~龙虎榜"数据全空"（system-review §六.3）~~ | ✅ 复核排除：实测 `/api/longhu` 59/77 条正常，非代码缺陷 |
| 09-04 | ~~AGENTS.md "24 warn 挂账" 过时标注~~ | ✅（本轮）：门禁注释与 eslint.config.mjs 注释同步为实际状态 |

**仍开放（触发条件未到）**：P2-4/5 逐笔历史与聚合、P2-6 题材事件树、P2-7 营业部图谱/筹码/解禁/两融/大宗、P2-8 MCP 封装、Docker 部署（等环境）、LLM 凭据、消融验证（2026-10 中旬）。
| 09-04 | ~~§六拍板四项~~ | ✅ 97f845f：竞价溢价比因子（P0 第三项补齐，晋级率/情绪分位核实已在）+ 事件影响力 Tab（四级分类+L1/L2/L3）+ K线布局方案 B（评估条入图内浮层+右列可收起，canvas +52%）；停牌标识核实 09-02 已落地 |
| 09-04 | ~~7 个损坏 parquet~~ | ✅ 全量扫描实为 31 个（kill -9 累积产生），用户确认后全部删除，复扫 8354 文件 0 损坏 |
| 09-04 | ~~Docker 生产化~~ | ✅ apps/web/Dockerfile 多阶段 + docker-compose.prod.yml；顺带修 dev compose 两处错误（数据卷路径、CORS 直连）。本机无 Docker，用户侧 `docker compose -f docker-compose.prod.yml up -d --build` 验证 |
| 09-04 | 8000 重启（用户授权） | ✅ 08:05 盘前窗口重启，intraday-top / events-impact / auction-premium 三新端点实测生效；历史生效项全部落地 |
| 09-04 | ~~市场页事件 Tab 列表无法滚动（复发 ×2）~~ | ✅ a009f97：根因 = Panel 根节点漏 h-full 被内容撑到 7159px，overflow-y 永不触发、被页面 overflow-hidden 静默裁剪；修复 1 行 + panel.tsx 固化「全高面板契约」防复发（agent-browser 实测 canScroll + scrollTop 持久） |
| 09-04 | 事件排序与盘面/情绪关联（可解释可追溯） | ✅ a009f97+2b1254f：`app/events/ranking.py`——题材共振 25 分（官方成分等权涨幅）+ 个股联动 10 + 情绪相位乘数（冰点政策×1.5/热点×0.7 等）+ 影响力 45/27/9 + 时效/来源；rank_score/reasons/factors 全程可解释；上下文 60s 缓存、快照整体失败不缓存防粘住；实测冰点期政策类登顶、热点澄清降权 |
| 09-04 | 事件分类体系 + tag（对照同花顺补覆盖） | ✅ a009f97：derive_tags 派生 业绩/公告/异动/资金/行业 多标签（无命中不臆造）；前端排序 tag 切换（最相关/最新/影响力）+ 标签筛选行 + 条目徽标；顺带修 sort 切换需等 60s 轮询的 UX 缺陷（ref 守卫 effect 立即补拉，实测 2s 生效） |
| 09-04 | 总览事件驱动面板形态决策 | ✅ a009f97：自主决策改「盘面相关性 Top4 摘要」（原时间序个股流水账在 ~148px 面板只露 2 条；一屏纪律锁死高度扩量不可行）；消费 relevance 排序，总览给结论（分数+依据+相位）、明细走事件 Tab 入口；实测 Top4 = 99/99/75/75 分 相位冰点 |
