# 项目盘点（2026-08-29）——欠缺 / 布局待优化 / 技术债

> 触发：用户要求"回看还有哪些欠缺、哪些布局不合理、哪些需要完善优化"。逐项诚实盘点，
> 作为后续迭代的待办清单。完成一项划一项。

## 一、功能欠缺（按用户影响排序）

| # | 项 | 说明 | 计划 |
|---|---|---|---|
| ~~1~~ | ~~**交易前端**~~ | ✅ 已完成：右列"交易"页签（买卖表单+持仓+挂单+成交记录） | — |
| ~~2~~ | ~~**真实 B/S 点**~~ | ✅ 已完成：`candle.setMarkers()` 已调用（此前 markers 构建后从未挂载，且循环重复两次） | — |
| ~~3~~ | ~~**持仓视图**~~ | ✅ 已完成：持仓列表 + K 线黄虚线成本线；左栏"持仓"组仍未做 | 左栏持仓组待办 |
| 4 | **新闻/公告已接入但无 AI 摘要** | 列表+原文链接 ✓；情绪分析/重要度排序是 Phase 7 | Phase 7 |
| ~~5~~ | ~~概念题材 chips 混入风格标签~~ | ✅ 已完成：`classify_boards()` 拆成 行业/地域/概念题材/风格·指数 四组分别渲染，`boards` 保留全量不丢数据 | — |
| ~~6~~ | **强势题材梯队看板** | ✅ 已完成：`/api/themes` + `/themes` 页面；以题材容器重组涨停池，输出连板天梯/成建制分级/阶段判断/健康度结论/断板股；题材归一化去重避免重复 symbol；快照按交易日匹配保证溢价口径正确 | — |
| 7 | 选股器 / 评分系统 | 多因子技术评估已做单股版；全市场选股未做 | Phase 5 |
| 7 | 回测引擎 + 历史回放 | Parquet 数据已开始积累 | Phase 6 后半 |
| 8 | 预警通知 | 无 | Phase 8 |
| 9 | 左栏自选 sparkline 迷你走势 | 同花顺有；需分时缓存 | Phase 5 |
| 10 | 交易日历节假日依赖 ths | ths 不可用时回退周末规则（国庆仍会误判） | 缓存持久化兜底 |
| **11** | **🔴 情绪判定：非交易日日期回退 → 自指计算** | **周六调用时 `push2ex` 静默回退到周五，今日池=昨日池=周五，再查周五收盘快照 → 涨停股查自己涨停那天的价，恒等于 +10%。产出"昨涨停均值10.38%/翻红率100%/再涨停率100%"的假指标，阶段误判为「高潮」且置信度"高"。正确结论应为「分歧」。** | **P0，见 `docs/sentiment-phase-review.md`** |
| **12** | **🔴 情绪引擎缺晋级率** | `docs/sentiment.md` 清单写了 1进2/2进3/3板+，`engine.py` 从未实现。业界公认 1进2 是最敏感指标（≤25%=冰点/退潮，≥40%=升温）。实测 8/28 为 16.4%，连续 4 日 11%–20% | **P0** |
| **13** | 阶段规则：热度指标无反向校验 | 高潮判据只看 高度/均值/涨停数 三项热度指标，赚钱效应转弱无法否决 → 六阶段里的「分歧」实际永远触发不了。**题材梯队看板已修复此原则（赚钱效应可否决热度）；情绪引擎尚未同步。** | P0 |
| **14** | 只用均值不用中位数 | 8/28 均值 3.17% vs 中位 1.97%，背离 1.2pct，均值掩盖多数人体感。**题材梯队看板已改用中位数；情绪引擎尚未同步。** | P1 |
| **15** | 炸板率未用真实炸板池 | 用"价格法−封单法"差值近似，且不参与阶段判定；真实炸板池 `getTopicZBPool` 已实测可用（8/28：16 只） | P1 |
| **16** | 休市日数据未标 stale（红线 2） | 周六 `received_at=08-29T11:53` 但 `ticktime=16:29`（周五收盘），仍报 `is_stale=False/is_realtime=True` | P1 |
| **17** | 情绪周期是单点截面，无序列 | 缺近 10 日情绪分曲线与本轮周期起点定位 | P2 |

## 二、布局待优化

| # | 项 | 现状 | 方向 |
|---|---|---|---|
| 1 | 右列宽度固定 300px | 资料页 chips 换行多、盘口紧凑 | 可拖拽调宽（resizer） |
| 2 | 图表区副图高度均分固定 | VOL/MACD 同开时挤压主图 | 副图高度可拖拽 |
| 3 | 盘口仅 L1 五档 | L2 付费（¥298/年），无免费源 | 预留 Provider，未来接入 |
| 4 | 分时图无均价线 | 有 cum_amount/volume 可算 | 下一刀顺手加 |
| 5 | 自选列表无 sparkline | 同花顺每行有分时缩略图 | Phase 5 |
| 6 | 指数卡收起后左栏上部空 | 收起时仅剩一行 | 可接受（用户主动收起） |

## 三、技术债

| # | 项 | 风险 | 处置 |
|---|---|---|---|
| 1 | StockDetailPanel ~600 行单文件 | 改动易破坏（已发生过 3 次） | 拆分为 BookPanel/ProfileTab/FlowChart 子组件 |
| 2 | 前端零测试 | UI 回归靠人工截图 | 引入 vitest + 关键组件测试 |
| 3 | Next 15.5.24 outdated | 安全/功能更新 | Phase 8 统一升级 |
| 4 | 新闻/公告每次切股重新拉取 | 无缓存，切股闪加载 | 后端 60s 缓存或 SWR |
| 5 | paper_matcher 5s 轮询挂单 | 盘后空转 | 挂单存在时才轮询（微优化） |
| ~~6~~ | ~~全局仅一个模拟账户，无重置入口~~ | ✅ 已完成：`POST /api/paper/reset` + 交易页签重置按钮（二次确认） | — |
| 7 | error.tsx 全局错误边界未做 | 渲染异常白屏 | 顺手补 |

## 四、已确认的行为基线（勿回退）

- 布局锁定一屏：容器内滚动，无整页滚动条
- 红涨绿跌；tabular-nums 对齐；所有数据带 来源/时间/质量 标注
- mock 数据永不冒充实盘；数据源失败标 stale 不伪造
- 撮合规则：T+1/涨跌停拒/整手/费用/停牌拒——不可绕过
- 技术结论只给偏向+依据，禁止确定性买卖建议
