# W05 智能体价值与权限

> 定位：最终融合方案的阶段任务文档；由 [总账 §6.0](../retro-and-gaps.md#60-阶段索引) 唯一索引。任务状态只在本页更新。
> 调度：本页 W 编号只表示领域归属；实际施工必须按 [总账 §5.9](../retro-and-gaps.md#59-阶段门优先级与跨阶段治理) 的“阶段门 → 门禁角色 → P0/P1/P2 → 门内序 → 硬依赖/效果前置”。页面上下顺序不是施工授权。

- **阶段目标**：三类真实任务和无 AI 对照，核引用、预算、权限，比较一个编排中枢。
- **依赖边界**：W00；消费 W02/W03 证据。不要求依赖阶段整体清零，按对应接口/证据切片判断。
- **排期**：本领域以 P1 智能体价值为主，危险能力止险可为 P0；实际主切片顺序只按总账 §5.9 的阶段门治理计算。

## IMP-046

**Jev Agent Router：受限工具组与条件能力路由**

- **状态**：部分完成
- **优先级**：P1
- **阶段门**：G4
- **门内序**：30
- **门禁角色**：非阻断
- **依赖**：无
- **效果前置**：无
- **方案依据**：docs/ai/jev-integration.md §23 及真实 JevRouter smoke。
- **U51 Jev 承接子范围（2026-09-24 已批；沿本任务原状态/门序/优先级）**：P1/G4：只在多个真实获准工具竞争且选择不明显时保留 Jev 路由；记录建议、权限/schema 过滤、实际工具执行和退回原授权集合。核助手必要工具漏用/误否认时先用结构化事实，再比较 Jev 对残余歧义的增量；不恢复宿主 model/effort 自动切换。 方案与反例见 [Jev 附录 A](../ai/jev-integration.md#附录-a-2026-09-24-已批准的全系统应用可视化与维护方案v04)。
- **范围**：AShare 助手按只读工具组做 bounded routing；全局仅在至少两个真实可用能力竞争且选择不明显时调用 capability router。执行授权仍由 TOOL_SPECS、actor permissions、risk、confirmation 和 input schema 控制。
- **验收**：Jev decision 不直接执行能力；无权限候选必须过滤；浏览器复杂场景可回退既有 browser/computer-use；真实券商、资金和风控修改永不进入 Jev 动作空间。
- **证据**：PR #31 已合入项目 Jev adapter/助手工具路由底座，PR #32 完成价值审计并收敛为条件 capability routing；真实 TypeSafe 选择 edit_code 后，Router 因 caller 缺 write_repo 正确过滤并安全回退；业务助手 6 个只读工具组 shadow 已接线；jev-browser 已完成 CLICK/TYPE_TEXT 真实链路 smoke。
- **下一步**：继续收集业务 route coverage、升级率和 token 数据；自动 model/effort 切换继续暂停，除非真实会话 A/B 证明净收益。
- **恢复**：关闭 Jev routing 即回到现有确定性工具选择/LLM 路径，不改变权限或交易状态。

## IMP-045

**Jev 语义决策中间层与 Universal Verification**

- **状态**：部分完成
- **优先级**：P1
- **阶段门**：G4
- **门内序**：20
- **门禁角色**：非阻断
- **依赖**：无
- **效果前置**：RSH-030
- **方案依据**：docs/ai/jev-integration.md §28–29。
- **U51 Jev 承接子范围（2026-09-24 已批；沿本任务原状态/门序/优先级）**：P1/G4：复用统一 adapter/三态 verifier，把事件修订、claim↔证据、提醒残余语义及助手引用做版本化有限问题；先选一条真实事件→机会理由变化纵切，记录建议/采纳/弃权/回退与独立人工反例。影子调用不得拖延规则提醒；不另建总语义网关。 方案与反例见 [Jev 附录 A](../ai/jev-integration.md#附录-a-2026-09-24-已批准的全系统应用可视化与维护方案v04)。
- **范围**：事件/提醒的 bounded Choice/Noul 与助手 evidence→claim 三态 verifier；不自由生成题材、不检索事实、不直接改选股分或交易结论。
- **验收**：默认 off/shadow 不改变用户可见结果；私人上下文、非公共工具或 deterministic grounding 已失败时跳过 verifier；任何 cascade 阈值只能来自独立人工标签。
- **证据**：PR #31 已合入事件/告警 Jev shadow 接线，PR #33 已合入 `semantic_verify.py` 三态 verifier 与 gold-set 工具，PR #34 已合入预标注/审核优先级产物；三态 smoke（supported/contradicted/insufficient）与专项测试已完成，原 DeepSeek 用户可见路径保持不变。
- **下一步**：完成 verifier 人工 claim/evidence 集与 RSH-030 v1 人工标注后再评估常开 shadow/cascade、KB/rerank 和题材候选 Noul。
- **恢复**：关闭 Jev 模式后确定性 grounding、规则和现有 LLM 行为保持不变。

## IMP-052

**Agent 权限、预算与能力就绪**

- **状态**：已完成
- **优先级**：P0
- **阶段门**：G4
- **门内序**：10
- **门禁角色**：阻断
- **依赖**：无
- **效果前置**：无
- **方案依据**：主方案 §6.7–§6.9；智能体附件 A/E
- **范围**：先核本项目 C 类宿主执行、参数晋级与权限/预算，再比较外部候选。涉及 backend/app/services/code_executor.py、agent_params.py、experiments.py、evolution.py；不以候选 SDK 修复替代本项目止险。
- **验收**：无批准/无完整证据不写线上策略覆盖；不运行应用内未隔离补丁；提示注入、缺policy、取消及未知用量失败关闭；现有合法低风险能力不退化；返回任务状态反映实际副作用。
- **证据**：前轮C纯提案与影子止险已取证并保留。2026-09-22 本纵切继续关闭参数晋级权限缺口：新增独立 `agent_param_promotion_approval`，人工先读 exact review package，再用独立于普通 API token 的 `ASHARE_AGENT_PROMOTION_TOKEN` 创建最多 24h 的一次性批准；批准绑定候选/当前基线/影子证据三组 digest 与仓库内 `docs/review|docs/research|artifacts` 真实效果证据文件的 SHA-256。消费时再次重算全部身份，并把 candidate CAS、approval consume、live-baseline CAS、参数写入及 30 日后置实验创建放进同一事务；任何候选/证据/基线/文件漂移、批准撤销/过期/重复消费、负面或不足 shadow verdict、实验基线采集失败均不写运行值。提交后 runtime refresh 若失败不伪装“无副作用”，返回 `runtime_refreshed=false/restart_required=true`，同一批准幂等重试只做 reconciliation。#100 合并后的 U49 复核又发现首版专用 token 只挂在 HTTP dependency，内部代码可绕过路由直接调用 approval service；后续加固把同一 token 判定下沉为 `core/auth.py` 唯一判据，创建/撤销 service 也必须显式传入并通过，HTTP 只负责 transport。迁移已在隔离 worktree DB 验证 upgrade/downgrade；定向/全量与最终精确提交见 handoff/发布回执。预算/取消终片另见 `docs/review/imp052-budget-cancel-20260922.md`：跨进程 slot 竞争、真实 DB 副本部署日 backfill、unknown token、I/O/time/retry 高水位、跨 worker cancel、Jev/LLM metadata sink、前后端完整门均有反例/验收。PR #103 合并后的 U49 又发现 `asyncio.Task.cancel()` 无法杀掉已进入 `to_thread()` 的同步 worker：首版可能先写 terminal、线程仍继续；follow-up 改为 running cooperative cancel/timeout checkpoint，in-flight bounded stage 真实 drain 后才确认终态，并补“线程未释放前 DB 仍 running”的反例。
- **下一步**：2026-09-22 最后一纵切已补齐 `agent_resource_usage` 持久预算/usage ledger、北京日原子 slot、部署日 legacy backfill、unknown token fail-closed、输入/输出/timeout/retry 高水位、跨进程 `cancel_requested_at` 与 owner-confirmed 终态；结合前序 C 纯提案和 promotion authority，本任务工程验收闭环。后续仅按 U48/GOV-027 做机制生命周期复核；若预算策略、模型 provider 或进程拓扑变化再重开，不另建第二预算系统。
- **恢复**：撤销有风险能力，保留只读服务与审计；worktree 不当 OS 沙箱。
- **实施步骤**：①补贴合现状的权限矩阵；②C 类不因禁改 tests 就安全，测试导入 app 同样执行源码；③将结构有效/风险未剧变与效果达标/批准晋级分开，保留实验与CAS恢复；④覆盖取消、凭据最小化、输入/输出/时间/重试预算；⑤核源码默认与运行有效值差别，禁止修改真实配置来验收；⑥候选累计模型预检额度单独核实，不因换任务重置。
- **开工前置**：不依赖全仓审计完成或模型付费评测；先验证本项目确定性权限与副作用边界。
- **本轮边界**：最终形态仍不改真实策略参数/交易权限：C 类只产待审 patch；A 类只产 shadow 候选，只有独立 promotion-operator 批准可晋级。预算层只约束资源与任务真实终态，不把 token/字符预算变成金融策略阈值；普通业务 LLM 只写统一 metadata telemetry，不偷占自主 8 次额度。部署日旧模型用量不可追溯时按 unknown fail-closed，不通过重启/换 task 重置额度。
- **后台化**：进化/参数/任务/仓库追踪/诊断控制退出普通前台，但后台已有安全权限、暂停、审计和恢复保留。不能只隐藏按钮或把运营动作塞进用户聊天框；先核后台维护入口与鉴权。应用内代码仍纯提案，无证据不得晋级。


## BUG-025

**KB 索引不可用时不得伪造已引用证据**

- **状态**：已完成
- **优先级**：P0
- **阶段门**：G0
- **门内序**：70
- **门禁角色**：非阻断
- **依赖**：无
- **效果前置**：无
- **方案依据**：主方案 §6.7/§6.9；实施校准 v9.1 §4；RSH-027 的真实性子切片
- **范围**：backend/app/picks/kb_routing.py 的 validate_kb_citations、snapshot_citations；关联模型/前端枚举只在实际消费时同步，不重建检索系统。
- **验收**：不可用/空索引、缺条目、损坏索引不能返回 cited；无引用不误报引用失败；正常引用、场景权限、去重均保持；旧快照不被重写。
- **证据**：a9d4adc 基点反例已复现；不可用/空/完整性与条目身份校验、requested 与验证通过 IDs 分离均已落地。集成 head 3e657c6 的 changed-backend 专项 455 passed、本地后端全量 3926 passed / 80 skipped，前端 692/692（本地与 TZ=UTC）、tsc/eslint/build 全绿；PR #39 GitHub CI run #340 三项 required check 全绿。
- **下一步**：本缺陷本体闭环，不重复开发；正文检索是否真正使用知识、引用内容哈希与增量价值仍归 RSH-027，不因本项完成冒称 KB 效果已证明。
- **实施步骤**：①复用唯一解析器和三态，不加迁移；②不可用/空/部分损坏/重复编号/未知册不认证，保留解析诊断；③校验条目ID/册/标题/状态；④requested保序去重并兼容一次性迭代输入；⑤隔离库核新旧快照、汇总和通知归档，重放不读当前索引；⑥正文检索/引用有效性和消融仍归RSH-027，不冒称已做。
- **验收边界**：正常索引的五组输入输出除新增requested外与旧版保持；每次快照只读索引一次。局部微基准不代替生产性能，索引损坏时显式拒绝认证。
- **恢复**：保留旧数据和可读兼容；关闭该引用功能可恢复只读基线，不回退到伪造 cited。
- **分工**：本轮由ChatGPT临时代执行；Codex恢复后先核最新代码/证据，不重复开发。长期分工及后续审阅按协作规范。

## RSH-027

**知识引用真实性与 KB 增量**

- **状态**：部分完成
- **优先级**：P1
- **阶段门**：G3
- **门内序**：40
- **门禁角色**：非阻断
- **依赖**：BUG-025
- **效果前置**：无
- **方案依据**：主方案 §6.7、§6.9；智能体附件 A/B
- **U51 Jev 承接子范围（2026-09-24 已批；沿本任务原状态/门序/优先级）**：P1/G3：先修检索召回/引用身份，再比较 Jev 对候选 KB 适用性和 claim↔引用的增量；记录真实引用、拒绝、反证与有无 KB 消融。引用次数或模型相关分不是有效性，失败回现有检索并标未验证。 方案与反例见 [Jev 附录 A](../ai/jev-integration.md#附录-a-2026-09-24-已批准的全系统应用可视化与维护方案v04)。
- **范围**：复用已有场景路由与引用记录；引用必须能解析到实际使用内容，失效索引不得假称已引用。
- **验收**：未命中/冲突/失效可见；同任务有/无 KB 对照含错误引用和无增益，KB 不越过金融硬门。
- **证据**：路由契约、引用校验与覆盖守卫已交付；影子消融与候选成功检索链仍缺证据。
- **下一步**：核真实语料和检索失败方向，再冻结小任务集，不增加无消费者知识副本。
- **恢复**：退回已验证只读检索；保留引用与失败证据。
- **实施步骤**：①先交付 BUG-025；②实际引用须对应取回正文、来源位置/内容哈希及版本，合法ID不等于确实读过正文；③盘前/盘中/复盘场景各做可复查小任务；④支持/冲突/未命中分开；⑤同任务有无KB对照，未证增益继续仅解释。
- **KB累计**：明确区分实现状态与实证有效性；保留KB-STOCK-01～37的出处/负结果/域声明，修正无过拟合等过强表述。语义引用必须能回到实际正文版本，不能以索引存在冒称模型已用。


## IMP-051

**三类真实任务与单中枢挑战**

- **状态**：待条件
- **优先级**：P1
- **阶段门**：G4
- **门内序**：40
- **门禁角色**：非阻断
- **依赖**：RSH-027, IMP-052
- **效果前置**：无
- **方案依据**：主方案 §6.7、§6.9；智能体附件 C–F
- **U51 Jev 承接子范围（2026-09-24 已批；沿本任务原状态/门序/优先级）**：P1/G4 候选替代路线：在原盘前/盘中/盘后三类真实助手任务上，把现有编排、现有编排+Jev 窄判断和拟引入重框架作同质量、完整成本、时延与维护对照；轻量路线胜出才建议不引入框架，原三类任务和无 AI 对照验收不退出。 方案与反例见 [Jev 附录 A](../ai/jev-integration.md#附录-a-2026-09-24-已批准的全系统应用可视化与维护方案v04)。
- **范围**：盘前研究、盘中解释、盘后复盘对照无 AI 基线；同模型比较现有编排与 WeKnora Lite/DeepTutor。
- **验收**：事实正确、任务完成、成本/时延与接管有同条件证据；一个编排中枢，只有单者不足且增益明确才组合。
- **证据**：附件只有隔离启动、组件与失败反例，无质量胜负；不预设保留自研或必须替换。
- **下一步**：满足以下条件再实施：先核累计预检预算和服务端上限，再评测；旧工具、对比表、写告警与点赞功能按真实任务需要取舍。
- **恢复**：保留已验证只读路径与协议适配；不得把生产金融内核交给外部 Agent。
- **实施步骤**：①继承原附件固定版本、启动与失败证据，不重复安装竞赛；②基线按无AI/模板/单次模型/受限Agent比较；③同任务、同模型、同数据与权限，计纠错和阅读成本；④核检索成功、取消/恢复/引用与预算；⑤单一中枢足够则不组合，外部不优也可只借机制。
- **产品输出**：面向用户的助手只解释当时事实、检索证据和导航；发送/取消/重试/历史恢复/删除每一动作有状态与权限。内部工具编排和模型参数不展示，未完成输出不得在恢复后伪造成功，外部文本不能改变系统授权。


## BUG-009

**晨报今日计划的真实消费链**

- **状态**：待执行
- **优先级**：P1
- **阶段门**：G2
- **门内序**：50
- **门禁角色**：非阻断
- **依赖**：无
- **效果前置**：无
- **方案依据**：主方案 §7.4、W05 真实任务
- **范围**：核旧账本指出的失效惰性导入与静默空计划，确认现版消费者后修复或移除无效承诺。
- **验收**：真实存在的计划能读到；无数据和调用失败可区分；不能靠空数组吞错维持“成功”。
- **证据**：4e7bfcb E1：_daily_plan 导入 app.picks.review_store 和 app.models.review，相应模块/包均未找到；两段被 suppress(Exception) 包围。尚未读取用户实际晨报/生产数据。
- **下一步**：定位当前晨报入口与模型，先反例复现；已被替代则用代码/提交证据销账。
- **恢复**：回退读取适配并保留明确降级，不写入伪造计划。
- **实施步骤**：①核 backend/app/review/storage.py 和实际模型/报告字段；②将确有内容、合法无内容、读取失败分别呈现；③限制返回数量按最终结果截断；④保留无LLM规则拼装及来源日期；⑤在 backend/tests/test_morning_brief.py 覆盖非空结果和故障，不能只测试空数组。

## IMP-025

**代码提案与实际执行状态一致**

- **状态**：部分完成
- **优先级**：P1
- **阶段门**：G4
- **门内序**：50
- **门禁角色**：非阻断
- **依赖**：无
- **效果前置**：无
- **方案依据**：主方案 §6、§14 Agent 真实性
- **范围**：核 C 类议程的 executed 徽标；应用内产物是 patch/隔离分支提案，不能呈现为已落地。
- **验收**：前后端同一事实语义；提案、审查、合并不混用；金融与代码授权主体不混淆。
- **证据**：本轮IMP-052 C切片同步修复新proposed/历史executed的界面与复盘回写含义，保留原始记录；其它任务中心/汇总/历史消费面的完整审计仍待后续，不由单个徽标修改销账。
- **下一步**：本轮同步A类影子未生效标记、复盘回写和摘要；新旧C类边界继续保留。任务中心其它历史消费面及实验“已回滚/恢复未执行”等实际效果仍须核验，不凭前端标签全部销账。
- **恢复**：回退展示适配，不重开自主落地能力。

## 已交付基线

- 代码执行器提案边界、会话持久化和部分只读工具已交付；助手候选外部试跑仅为工程证据。

旧编号、退出理由和原文恢复入口见 [历史处置表](../archive/ledger-transition-20260917.md)。本节只留仍支撑本阶段的成果，不保存逐轮长日志。
