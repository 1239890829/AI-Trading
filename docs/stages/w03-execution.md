# W03 执行快照一致性

> 定位：最终融合方案的阶段任务文档；由 [总账 §6.0](../retro-and-gaps.md#60-阶段索引) 唯一索引。任务状态只在本页更新。
> 调度：本页 W 编号只表示领域归属；实际施工必须按 [总账 §5.9](../retro-and-gaps.md#59-阶段门优先级与跨阶段治理) 的“阶段门 → 门禁角色 → P0/P1/P2 → 门内序 → 硬依赖/效果前置”。页面上下顺序不是施工授权。

- **阶段目标**：页面、提醒、模拟动作共用可追溯执行事实，生成态保持可回放。
- **依赖边界**：W00；外推切换依赖 W02。不要求依赖阶段整体清零，按对应接口/证据切片判断。
- **排期**：本领域含 P0 核心正确性任务；实际主切片顺序只按总账 §5.9 的阶段门治理计算。

## BUG-029

**曾封板与当前开板的候选可达性一致**

- **状态**：已完成
- **优先级**：P0
- **阶段门**：G1
- **门内序**：10
- **门禁角色**：阻断
- **依赖**：无
- **效果前置**：无
- **方案依据**：用户要求断板走趋势/再接力；KB-STOCK-21；v9.3机会状态契约。
- **范围**：backend/app/picks/tradability.py、intraday_opportunity.py、pre_limit_radar.py、api/routes/picks_intraday.py、services/picks_pipeline.py、opportunity_learning.py及实际前端/归档消费者；统一“曾封板身份”与“当前封板状态”，不把全部涨停股变可成交。
- **验收**：保留日内曾封板身份，同时按有效实时状态重评；真正开板可进入评估而非自动获准，仍封死/缺时点报价/权限不足拒绝或 unknown；开板→回封→再开板的版本和去重一致，主屏/参考区/盘后组合/离线重放不自相矛盾。
- **证据**：PR #55 / 代码提交 `be3ccd1`。修前 7 类定点反例真实判红：`linkage_candidates` 在读 current 快照前按涨停池成员永久拒绝；`sealed_no_entry → board_reopen` 因 registered filter 不可达；机会缓存不含快照版本/状态；主候选与 reference 可重复；无可信时点仍可形成冲突判断。修后统一 `ever_sealed + current_sealed + snapshot_state + version`：只有 `ready + as_of` 能证明曾封板股当前已开板；current 仍封拒绝，缺时点/陈旧为 unknown，权限/涨幅/成交额硬门保持；机会缓存键加入 snapshot state/version；`board_reopen` 按 `(trade_date,symbol)` 去重；完整涨停身份从未裁剪的 theme board ladder 提取，不从 UI 配额反推。
- **真实源证据**：2026-09-18 东财涨停池 78 只中 49 只有 `break_count>0` / 开板再封证据。国芳集团 `601086`：`break_count=9`、首封 `09:53:17`、末封 `13:12:02`；腾讯 5 分钟线按前收 14.60、10% 涨停价 16.06 复核：09:55 到 16.06，10:00 低至 15.97/收 16.03（已开板），10:05–10:10 再到 16.06，午后亦出现低于封板价后再封。证明“在当日涨停池”只是 ever-sealed 身份，不能永久等同 current sealed。
- **证据版本**：机会归档从 `stock-opportunity-funnel-v1 / pit-evidence-v1` 升到 `v2`；旧 v1 行 append-only 不改写，legacy `sealed_pool=True` 仍按旧语义 replay 为 rejected；v2 以 `ever_sealed/current_sealed/snapshot_state/version` 重放，开板、回封、unknown 均可复算。未改 `LINKAGE_MIN_PCT`、成交额、临板区或涨停阈值等策略数字。通用数据契约沉淀为 `KB-ENG-117`。
- **验证**：相关后端套件与 `pyflakes app tests` 全绿；全后端 **4090 tests collected**、完整 pytest exit 0；前端全量 **693/693**、TypeScript、ESLint 全绿；干净 `npm ci` 后 Next.js 16.3.3 production build 通过；`PickCard` 最新文案定点 35/35 通过。
- **下一步**：按阶段门重算，G1 下一可行动阻断项为 **IMP-006**，之后 IMP-044。BUG-020 仍为 G0 `待条件`；其交易会话条件一旦变为可行动，必须重新抢占 G1。
- **恢复**：保留旧 v1 归档、ever-sealed 身份和 current-state 版本；若回退本片实现，不得恢复“涨停池成员=当前永久封板”“首封历史=全天不可买”或“开板=自动成交资格”。原始涨停池来源仍不直接授权，开板后必须重新通过 current 快照、权限、联动与流动性门。

## IMP-006

**统一执行快照与证据重放**

- **状态**：已完成
- **优先级**：P0
- **阶段门**：G1
- **门内序**：20
- **门禁角色**：阻断
- **依赖**：无
- **效果前置**：无
- **方案依据**：主方案 F01、§5、W03；U47 / hunting-decision-design §4.1。
- **范围**：复用 `OpportunityDecisionSnapshot`、现有 buy-point gate、AlertEvent 与 PaperTradingEngine；不新建第二事实库。页面/提醒/模拟动作引用同一当时事实，生成态和历史 decision 仍 append-only。
- **验收**：统一 snapshot/decision/version；年龄、参数与迟滞可解释；`reference_entry`（组合生成/首见参考）与 `executable_snapshot`（动作时重检）及 `PaperOrder.filled_price`（真实模拟成交）永久分名。动作时行情非 `ready` 必须 fail-closed，只留参考/拒绝证据；页面 GET 不生成新样本。
- **证据**：PR #60 的准确被审 HEAD `87b099540a4b6c3d54c56a9d57293dc1963ee450` 已于 2026-09-20 合入 `master`，merge commit `fa0185412db5c0d3f47445081da822f121cceff1`；独立网页审核回执绑定同一 HEAD（PR comment `5749077515`），结论 `APPROVED / MERGE_IF_GATES_PASS`。GitHub 原生 self-approve 因连接账号同时为 PR 作者被平台拒绝，不作为单账号仓库的必要门禁；exact-head `release_check.py`、required CI run `35502888440` 三 job 全绿且无阻断 thread 后完成合并。主体实现建立 `execution-facts-v1`：同日同场景同股 `decision_id` 稳定，`decision_version` 只由会改变判定的物质事实生成，通知发送/去重或单纯采样时间刷新不伪造新交易版本；动作时快照携 source/as_of/received_at/freshness，`stale/degraded/unavailable/unknown` 从命中降为明确拒绝；乱序晚到旧 notification snapshot 仍 append-only 保留供回放，当前读视图只按 `as_of` 选择较新版本。复核又发现“动作先发生、权威快照后落库”的孤儿引用窗口，限定整改 `d46d3ee` 已改为**完整 decision 先落库，归档失败则当拍提醒/自动模拟动作 fail-closed**；dispatch 状态继续由 AlertEvent/通道事实拥有，不反写交易 decision，`eligible` 仍进入结果标签链。
- **消费者闭环**：完整事实唯一留在 `OpportunityDecisionSnapshot.evidence`；AlertEvent、watch ledger、position plan 只存 `decision_id/version` 与必要价格身份引用，避免复制第二事实源；自动模拟仓沿同一引用下单，真实成交仍只认 order/fill；`GET /api/picks/today` 只读挂最新执行版本，PickCard 同屏分开显示“参考价 / 执行快照”，并对陈旧/降级/不可用显式标注“不是成交”。
- **验证**：主体候选此前完成完整后端 `4015 passed, 80 skipped`、全量 `pyflakes app tests scripts`；前端 `PickCard` 37/37、TypeScript、ESLint、默认时区与 `TZ=UTC` 两轮 `73 files / 695 tests`、Next.js 16.3.3 production build 均全绿；`git diff --check`、`public_repo_scan.py`、`workspace-hygiene.py`、`doc-health.py` 通过。PR #60 曾暴露一处治理测试把“当前必须 READY”写成测试前提，`488594f` 已改为测试内自行构造 READY 反例；随后准确 HEAD `9840499` 的 required CI run `35502367565` 三 job 全绿。复核新增的 P0 顺序修正 `d46d3ee` 又通过 opportunity-learning / buy-point / watcher / position-loop 定向回归与相关 pyflakes；其合入 PR 后必须重新以**最新 HEAD** 跑 required CI，旧 `9840499` 绿灯不自动继承。未改变选股阈值、仓位参数、真实券商/权限或策略收益口径。
- **发布结论**：独立网页审核与合并前 exact-head 门禁已闭环；合并后 `master` CI run `35503775542` 的 backend/frontend/docs 也全部 `completed/success`。因此本项于 2026-09-20 正式闭环为 `已完成`。该结论只覆盖执行事实一致性，不证明买点策略收益。
- **下一步**：已从合并后最新 `master` 重算阶段门。BUG-020 仍为真实交易时段 `待条件`，故当前 G1 下一可行动阻断项是 **IMP-044**；若 BUG-020 外部条件转为可行动，G0 重新抢占。
- **恢复**：回退本片可停止新 execution contract/read-view，同时保留旧 `OpportunityDecisionSnapshot`、AlertEvent、paper order/position plan 与历史 v1/v2 证据；不得回退为“参考价=成交价”、允许 stale 行情执行或用通知发送状态制造交易版本。
- **开工前置**：已满足；本片不等待 BUG-020/IMP-044 全任务完成。
- **发布前置**：相关 BUG-020 数据字段代码契约已复用；BUG-020 的真实交易时段生产会话仍按原任务 `待条件`，不被本片冒充完成。任何外发可靠性切换仍受 IMP-044 约束。
- **v9.3/v9.8契约**：统一 opportunity/decision 版本及 first_seen/trigger/asof/失效；`first_seen` 不覆盖，盘中新物质事实形成可追溯 decision version，旧判定仍可重放。GET 只读、业务判定由后台拥有；页面打开不能生成研究样本、收费推理或动作。


## IMP-053

**猎场动态机会自动影子执行与双轨验证**

- **状态**：待条件
- **优先级**：P1
- **阶段门**：G2
- **门内序**：15
- **门禁角色**：阻断
- **依赖**：IMP-006, IMP-049
- **效果前置**：RSH-026, IMP-020
- **方案依据**：U47；hunting-decision-design §4/§6/§8；product-closure-design；现有 `watch_ledger.py`、`shadow.py` 与 `PaperTradingEngine`。
- **U51 Jev 承接子范围（2026-09-24 已批；沿本任务原状态/门序/优先级）**：P1/G2：若上游以后采用获准 Jev 理由特征，影子执行仍只消费同版可执行事实，独立记录 fill/no-fill/reject/exit、费用与完整分母；reference 与实际成交永久分开，语义结果不得越过撮合、风控或预算硬门。 方案与反例见 [Jev 附录 A](../ai/jev-integration.md#附录-a-2026-09-24-已批准的全系统应用可视化与维护方案v04)。
- **范围**：复用现有撮合、T+1、整手、费用、涨跌停、停牌与资金约束，为“猎场已达到可执行条件”的 decision/version 建独立 hunting-shadow scope；不污染用户手工模拟账户，也不把现有“每日精选→次日开盘” shadow 偷换语义。观察/等待/被拒绝对象只进参考轨，不因出现于猎场就自动占用模拟资金。
- **验收**：①参考轨保留 first_seen/trigger/reference_price，执行轨另记 submit/fill/reject/no_fill/expired/exit、成交价、费用、滑点与持有规则；两轨统计和 UI 名称不可混用；②动作前以 IMP-006 同版 snapshot 重检，早盘等待/拒绝可在后续新 decision version 条件成立后首次提交，开盘状态不冻结全天；③同一 decision/version 幂等，重复轮询不增仓/增样本，新一轮独立机会必须有新版本/episode；④资金/仓位采用可复算、版本化的标准化协议，不连接真实券商；⑤拒单/未成交/过期也进入分母，成交收益只能按真实 shadow fill 与合法退出计算；⑥回放时只用当时可见信息，未来低点、后续涨停和盘后原因不得倒填；⑦ **IMP-020 readiness 的 execution-owned 证据契约必须机械绑定** `strategy_version / feature_version / execution_version / cost_model_version / exit_rule_version`、`PaperOrder.filled_price` 身份、费用/滑点已计入、`opportunities = filled+rejected+no_fill+expired+pending`、`pending=0`、`exited=filled` 及净均值/中位/胜率；研究侧不得提供构造 actual fill 的 helper，只消费本项产物。
- **证据**：当前 `watch_ledger` 以首见价对照收盘，能验证“当时发现后价格怎样”，但不是成交；当前 `picks-shadow` 主要在每日精选定稿后的下一交易日晨窗按开盘价进入。两者均不能代表“猎场每个盘中可执行买点已自动模拟成交”，因此本项是明确缺口，不据此声称策略已有增益。2026-09-21 多窗口审计又对照了 IMP-020 最终 `strategy_readiness` 与被淘汰的 promotion-v2 草案：最终版正确禁止 research 自造 fill，但消费侧还未机械核完上述五类 version identity、paper filled-price、成本/滑点与完整分母成熟度；这些事实应由本项补齐，不复活旧 research promotion 模块。
- **下一步**：满足以下条件再实施：IMP-006 与 IMP-049 已完成，并能用同一 opportunity/decision version 区分 reference、actionable 与动作前重检事实。条件满足后只选一个已准入、可动作的猎场场景做最小纵切，对照 reference→recheck→shadow order→fill/no-fill→exit→review 全链，再扩到其它情境。UI 只消费同版事实，不先造漂亮胜率。
- **研究边界**：工程链路完成不等于买点有效；胜率、净收益、早发现与“更低位置后涨停”的效果主张必须等待 RSH-026/IMP-020 的点时全分母、成本与 OOS/前向证据。
- **恢复**：停 hunting-shadow 新动作即可；保留 reference 记录、订单/拒单/成交审计和旧 daily-picks shadow。回退不得把参考价重命名为成交价，也不得删除失败/未成交分母。


## IMP-007

**模拟动作预检与恢复一致性**

- **状态**：待执行
- **优先级**：P1
- **阶段门**：G2
- **门内序**：20
- **门禁角色**：非阻断
- **依赖**：IMP-006
- **效果前置**：无
- **方案依据**：主方案 §5.7、§14 成交/组合/恢复
- **范围**：核模拟草稿、提交前重检、幂等与恢复；复用现有撮合/风控，不接真实券商。
- **验收**：T+1、整手、费用、停牌、涨跌停、资金和 scope 硬拒绝不退化；重复/超期动作不产生额外成交。
- **证据**：既有 paper 引擎与对账器已有硬约束；新增草稿 UI 或机制须先复现现有路径的缺口。
- **下一步**：用真实模拟动作链找过期预检或重复提交窗口，只有确凿缺口才新增机制。
- **恢复**：停新动作入口保留订单审计，按模拟账本对账恢复。
- **小动作闭环**：模拟价格输入/使用现价/改数量/预检/提交/撤单/重置分别核权限、身份和反馈；费用预估改读后端同版结果，不保留前端独立费率公式。手动券商成交记账不是自动下单，成交日期、费用、撤销与修正链单独检验；不在交易前台保留破坏性重置调试。


## IMP-048

**新闻事件来源谱系、修订与影响证据闭环**

- **状态**：进行中
- **优先级**：P1
- **阶段门**：G1
- **门内序**：40
- **门禁角色**：非阻断
- **依赖**：无
- **效果前置**：无
- **方案依据**：主方案 §5.3/§5.7/§7.3；数据源附件快讯断档与多标的关联；实施校准 v9.1 §7
- **U51 Jev 承接子范围（2026-09-24 已批；沿本任务原状态/门序/优先级）**：P1/G1：先落实来源谱系、可见时点和修订事实；仅对规则/别名无法解决的否认、转述和关键更正做冻结样本 Jev 候选判读。关键更正召回与误失效用独立人工对照；失败保留原文及 pending，不自动改机会版本或扩大 G1 到 G4。 方案与反例见 [Jev 附录 A](../ai/jev-integration.md#附录-a-2026-09-24-已批准的全系统应用可视化与维护方案v04)。
- **范围**：backend/app/models/event.py、backend/app/events/store.py、extract.py、impact.py、chains.py、verify.py、ranking.py 与 backend/app/news/ 下实际采集/水位消费者；复用现有事件模型和查询入口。
- **验收**：同题多源、正文更正、晚到/撤回、一事多股均可追溯；首发/当时可见/接收时间不同字段；历史决策绑定当时版本，不被后来解释污染；LLM 归因标为待验证假设，不直接改变股票分数。
- **证据**：4e7bfcb E1：EventStore.add_event 对同指纹只补缺 url/summary；EventCard/EventDirection 有来源与方向但未展示修订关系。需核其它消费者，不能将局部缺字段等同全仓完全无版本机制。
- **已发布首片（2026-09-24）**：隔离 SQLite 已复现同题更正仍读旧摘要、第二来源丢失、同来源改标题断链。PR #116 准确 HEAD `d68615234ca8ef1e2f24a8635686d044d79c2572` 已合并为 `cb1860ff4c358a39b054e175c97f8e53609a3ef5`；本地全后端 **4284 passed / 80 skipped**，PR CI `35947195580` 与合并后 master CI `35947702375` 三项均成功，`DegradedRelease` 回执 `5806402402` 绑定准确 HEAD，分支已删除。该片新增来源观察、内容哈希、来源发布时间/本机接收与可见时间、完整来源标的列表；同一来源条目 ID 的修订追加而不改写旧解释，未复核变体暂停活跃事件、标的池、板块异动归因与 LLM 辅助写入；详情可读观察及原解释的 observation ID。旧库升级不伪造旧观察。
- **已发布第二片（2026-09-24）**：PR #117 准确 HEAD `b439f618dd27a5c9dce8372e2b0f429aa766bdd7` 已合并为 `c2144952cd5f9368b82f3fbc3c235edd1e340e11`；本地全后端 **4291 passed / 80 skipped**，PR CI `35949419124` 与合并后 master CI `35949819655` 三项均成功，`DegradedRelease` 回执 `5806689355` 精确绑定该 HEAD，分支已清理。新增解释版本快照、准确观察 ID 的显式采纳/保留/撤回、按当时可见时间查询、每日精选及通知归档中的全部计分事件版本引用。旧事件不伪造历史解释版本；该片仍不代表 IMP-048 完成。
- **已发布第三片（2026-09-24）**：PR #118 准确 HEAD `ea7367977c7ec253196fe8486002f6ade6eef9fb` 已合并为 `b37b01a2d171395a41c41a67f7833dabd798f192`；本地全后端 **4303 passed / 80 skipped / 0 failed**、定向 **86 passed**，PR CI `35952187356` 与合并后 master CI `35952611453` 三项均成功，`DegradedRelease` 回执 `5807148587` 精确绑定该 HEAD，发布检查返回 `exit 0`，分支已清理。多标的快讯逐股保留方向待判关联；持久快讯频道水位用上一 code 重叠、受控翻页回看和逐条入库成功作为前移条件，失败/超限显式留下 gap。首次基线前历史和补采页上限外不宣称完整；该片仍不代表 IMP-048 完成。
- **已发布第四片（2026-09-24）**：U49 反证发现旧水位落在 20 页之外时常规轮询持续重复同一区间；同日上游只读实测 `sortEnd` 连续翻到第 21 页。PR #120 准确 HEAD `393018f2ace646b013565726720bc801ce6013db` 已合并为 `e484eca3442eb0c517e8f7e6f5f33b4268eedbfb`；`DegradedRelease` 回执 `5808970524` 精确绑定 HEAD，临合并 `release_check.py` 返回 `exit 0`。PR CI `35964305599` 与合并后 master CI `35964893798` 三项均成功，功能分支已清理。新增默认只读、显式频道/旧 code/21–100 页界限的手动恢复脚本；应用及调度器停止后才可 `--apply`，先做忽略区 SQLite 一致备份，再沿原入库与覆盖门写持久水位。旧 code 不重叠、条目入库失败或水位并发变化时不得虚报闭环。隔离快讯测试 **28 passed**，本机全后端 **4306 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **713 passed**，tsc、eslint、Next build、全量 pyflakes、文档体检、卫生和公开仓扫描通过。未在真实运行库执行补采，不把只读上游探测当历史完整性证明。
- **已发布第五片（2026-09-24）**：只读上游实测新快讯 ID 无稳定旧 ID 关联字段；隔离复现新 ID 通知与旧利好分成两张事件卡，旧卡仍可消费。PR #122 准确 HEAD `0d5ccf3176fa87e72cd98148762f2d2c742fd201` 合并为 `d5e6b710b1a4a998cedf50386c443bb183b826b3`；作者 `DegradedRelease` 回执 `5809567103` 绑定该 HEAD，临合并两次 `release_check.py` 均返回 `exit 0`。PR CI `35968041358` 三项成功，合并后 master CI `35968711716` 三项通过。新增写鉴权的人工关联入口：须给准确旧观察、新通知观察、当前解释版本和人工依据；同来源不同且非空来源 ID、旧事件活跃且无待审修订才追加撤回解释与关联。旧解释历史保留，新通知仍是独立事件；同参数重试幂等，冲突/过期/跨源失败，数据库唯一约束防同一旧解释被并发重复关联。详情显示通知来源、标题、URL 和解释版本。定向 **46 passed**，本机全后端 **4309 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **713 passed**，tsc、eslint、Next build、全量 pyflakes、文档体检、卫生和公开仓扫描通过。未迁移真实运行库，也未做运行态加载验收；未知关联不按标题自动推断。
- **已发布第六片（2026-09-24）**：PR #126 准确 HEAD `793f72896b6d505c7abc2ba22e75ba336f74a1c9` 合并为 `da75bc4d8707c6e71ac128e688e662925b1c0a8b`；作者 `DegradedRelease` 回执 `5810751533` 绑定该 HEAD，临合并 `release_check.py` 两次返回 `exit 0`。写鉴权的逐股人工审定只接受多标的来源观察中原为待判的个股，要求准确观察/当前解释版本、来源依据、传导链与人工记录；旧版本、待复核修订、非来源标的和重复审定拒绝。另一标的继续待判，旧解释版本保留；拟议事件强度上限不变，后续更正暂停活跃消费。详情标明人工来源且不把发布时间冒充审定时间，实际时间见解释版本。定向 **46 passed**，本机全后端 **4313 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **713 passed**，tsc、eslint、Next build、全量 pyflakes、文档体检、卫生及公开仓扫描通过；PR CI `35975926415` 与合并后 master CI `35976396578` 三项均成功、后端均为 **4313 passed / 80 skipped**。未加载正式运行实例，也未执行任何真实方向裁决；功能分支已清理。
- **已发布第七片（2026-09-24）**：盘前简报原先把活跃事件题材方向计入持久化方向分数，却不记录当时的事件解释版本。PR #128 准确 HEAD `cc77550ae5c1812f5bfe4216471182e2b3152c86` 合并为 `72f22fa6446b3f9ff78a7d9e19878056d86405e0`；作者 `DegradedRelease` 回执 `5811249744`，临合并 `release_check.py exit 0`。现将每个入选方向的关联事件引用随简报 JSON 归档，旧事件无版本时保持 `unknown`；同题材利好/利空均保留。定向 **27 passed**，本机全后端 **4315 passed / 80 skipped**，前端常规/UTC 各 **713 passed**，tsc、eslint、Next build、全量 pyflakes、文档体检、卫生及公开仓扫描通过。PR CI `35979524984` 与合并后 master CI `35980024222` 三项均成功；本地/远端功能分支已清理。未加载正式运行实例，旧简报不反填猜测版本；同日显式重生成仍按既有覆盖语义。
- **已发布第八片（2026-09-24）**：板块异动告警消息归因原从当前 EventCard/Direction 取题材方向、归档文字但不记解释版本；查询失败也被视为无匹配。PR #130 准确 HEAD `b333670589e8205402d0068f136239c56a1c1537` 合并为 `6b611e4cb0aabda6fabd8687b326bfaab88ee81b`；作者 `DegradedRelease` 回执 `5811759525`，临合并 `release_check.py exit 0`。现将实际入选消息的 event/interpretation/observation、可见时点与状态随 AlertEvent 快照归档，旧事件缺版本时明确 `unknown`；同事件多方向只占一个 Top2 名额。消息归因查询失败时检测器仍产出告警并标为 `unavailable`，与真实无匹配区分；若共享告警库整体不可用，归档和投递仍受既有持久化路径约束。触发阈值、方向、通知渠道不变。定向 **16 passed**，本机全后端 **4317 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **713 passed**，tsc、eslint、Next build、全量 pyflakes、文档体检、卫生及公开仓扫描通过。PR CI `35983176817` 与合并后 master CI `35983543678` 三项均成功；本地/远端功能分支已清理。未加载正式运行实例，不倒填历史告警的猜测版本。
- **已发布第九片（2026-09-24）**：恢复脚本原默认 dry-run 不读取上游，真正 `--apply` 后若旧 ID 未找到仅留 `watermark_not_found`，无法区分扫满页限、空页、游标缺失或抓取错误。PR #132 准确 HEAD `27585531024ab1b708f05d74792f8c07f4c217bb` 合并为 `a67509b47d31dedc013bd488e89fda8549f1ebf3`；作者 `DegradedRelease` 回执 `5812253314`，临合并 `release_check.py exit 0`。现新增显式 `--probe`：仅按给定频道/旧 code/21–100 页只读抓取，记录所选来源域、实际页数、终止条件、重叠和探测前后水位一致性到忽略的本机恢复回执；抓取失败或异常也留最小回执并返回非零。默认 dry-run 仍不访问来源；`--apply` 的停服、备份、入库与旧水位重叠门不变。本机定向 **30 passed**、全后端 **4319 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **713 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生及公开仓扫描通过；PR CI `35986480041` 与合并后 master CI `35987233131` 三项均成功。本片没有探测真实上游或操作运行库，本机回执只供调查，不是覆盖权威或人工关闭决定；功能分支已清理。
- **已发布第十片（2026-09-24）**：事件库已为活跃列表附解释版本引用，但 API 序列化丢弃，盘面事件/个股相关事件及现有弹窗只有方向而无该版身份；测试先复现缺字段。PR #134 准确 HEAD `f25a17841b11005269a5d25379d1fe499dcfc5ed` 合并为 `393ce2e65d3f8c80e8aa03578a2ec7ffb71ea881`；作者 `DegradedRelease` 回执 `5812773549`，临合并 `release_check.py exit 0`。现于列表、影响力、详情、标的池、个股相关及手动注册响应中带当前解释引用；盘面与个股事件弹窗展示列表读取时的版本/状态/可见时点，旧事件明确 `unknown`，待复核引用不冒充活跃结论；单事件及最新版本同一 SQL 语句读取。没有改评分、人工裁决或通知。定向事件后端 **37 passed**，本机全后端 **4320 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过；PR CI `35990297489` 与合并后 master CI `35990658930` 三项均成功，功能分支已清理。本机旧库尚无新增事件与快讯水位表，未做运行态加载或真实上游探测。
- **已发布第十一片（2026-09-24）**：U49 反证发现 LLM 辅助判读在模型响应期间遇到更正复核，仍能把旧标题的利好方向写到新解释并标“已判过”。PR #136 准确 HEAD `e7b44acf7ae820332e886a12e617090e65e24d5b` 合并为 `09839f64de74948f1cc1c1f9f80d1de93af46932`；作者 `DegradedRelease` 回执 `5813442332`、临合并 `release_check.py exit 0`。候选抓取当前解释 ID，结果落库以条件写入核准确版本、活跃/待审/已判状态及标题摘要；方向、观察引用、解释快照和已判标记共用事务，过期正向或中性结果均不落库。无版本的旧卡继续无法追认历史解释；已判卡在后来人工复核后是否重新候选仍待单独界定。本机定向最终 **17 passed**、全后端最终 **4324 passed / 80 skipped / 0 failed**；PR CI `35994837402` 的后端 **4324 passed / 80 skipped**、三 job 成功，合并后 master CI `35995144262` 三 job 成功。前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过；代码分支已清理。未调用正式 LLM、迁移运行库或加载生产实例。
- **已发布第十二片（2026-09-24）**：PR #138 准确 HEAD `f6e82c0f1afd26508823982ba48eed3e0d2a4281` 合并为 `8a929376b87fb608e75cc356bb830e81f95683a6`；作者 `DegradedRelease` 回执 `5814774317`，临合并 `release_check.py exit 0`，PR CI `36003876184` 三项成功。修前隔离复现：人工已采纳/保留的中性事件若从未被 LLM 判过，仍会进入 LLM 候选；较新的已审事件还可占满有界候选窗口，遮蔽较旧的合法候选。现于 SQL 限额前排除已人工审定或待复核的解释，结果落库时再核相同状态，保留已判预算标记，不改阈值或人工方向。本机定向 **59 passed**、全后端 **4329 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生及公开仓扫描通过；功能分支已清理。合并后 master CI `36004290222` 第一次因 agent task 0.1 秒启动时限测试失败（**1 failed / 4328 passed / 80 skipped**）；同一合并 SHA 的完整第二次运行三项成功，后端 **4329 passed / 80 skipped**，保留首次失败记录供波动追踪。未调用正式 LLM、迁移运行库或加载生产实例。
- **已发布第十三片（2026-09-24）**：PR #140 准确 HEAD `e3ed7f0e86324e4b2b18f0a37cc4cc44b02fabd4` 合并为 `95629e68cdad645939937cedc955a76681545d64`；作者 `DegradedRelease` 回执 `5815777386`，临合并 `release_check.py exit 0`，PR CI `36011023056` 三项成功。U49 交错反例在修前复现：卡片与方向先读、版本后读期间提交更正，可把旧利好方向绑定到新 `pending` 解释 ID。`EventStore.list_events` 现于一条 SQL 读取卡片、全部方向和最新解释版本，避免精选、简报等持久证据出现混版；新增多方向 `limit` 边界测试。定向 **170 passed**、本机全后端 **4331 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生与公开仓扫描通过。准确合并 SHA 的 master CI `36011501624` 三项均成功。本地与远端代码分支已清理；未迁移运行库或加载生产实例。
- **已发布第十四片（2026-09-24）**：PR #142 准确 HEAD `355213a456065882322c4cff7162866f0b59b0eb` 合并为 `3611c03aaa24c3248bb29760324be89a074f7208`；作者 `DegradedRelease` 回执 `5816411857`，临合并 `release_check.py exit 0`，PR CI `36015332852` 与准确合并 SHA 的 master CI `36015810867` 三项均成功，后端均 **4332 passed / 80 skipped**。修前交错测试复现：单事件详情先读旧解释版本，随后来源更正完成审定，详情再次查询方向时把新利空配给旧版本。`EventStore.get_event` 现于同一 SQL 读取卡片、方向及最新解释；详情、标的池和手动注册响应复用该方向快照，不改变评分、裁决或通知。定向 **66 passed**，本机全后端 **4332 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生及公开仓扫描通过。本地与远端代码分支已清理；未迁移运行库或加载生产实例，不能以此宣称 IMP-048 完成。
- **已发布第十五片（2026-09-24）**：PR #144 准确 HEAD `fa2ff0f2e45bff99a56494598467054f6b4ffc5e` 合并为 `14e53158e3527059fd527ec7a4a25ae4b4c12ebc`；作者 `DegradedRelease` 回执 `5817092472` 经格式修正后绑定该 HEAD，临合并 `release_check.py exit 0`，PR CI `36020344685` 与准确合并 SHA 的 master CI `36021060993` 三项均成功。修前隔离 SQLite 复现：90 张较新的过期传闻占满活跃列表查询窗口，遮蔽较早但仍有效的长期政策卡，影响精选与盘前简报的事件依据。现于 SQL 限额前按原状态、待复核和严格半衰期条件排除过期卡，微秒边界与现有 `is_active` 一致；同一查询仍读取方向及最新解释版本，旧事件缺版本保持 `unknown`。本机定向 **91 passed**、全后端 **4334 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生与公开仓扫描通过。隔离合成 10,030 张卡的预热查询约 5.3 ms，只是本机样本，不代表生产延迟。代码分支已清理；本机旧 `data/ashare.db` 无目标事件/水位表，未对准确运行库做只读缺口探测、迁移或生产加载；IMP-048 仍为 `进行中`。
- **已发布第十六片（2026-09-25）**：PR #146 准确 HEAD `2de7c90468c1e0f23251b23d270ad7225db5f7d9` 合并为 `e7f48481c5f0c593c4465b74dbd55b3742e6c9e2`；作者 `DegradedRelease` 回执 `5817590539` 绑定该 HEAD，临合并 `release_check.py exit 0`，PR CI `36024090034` 与准确合并 SHA 的 master CI `36024606016` 三项均成功。修前隔离复现：板块异动消息归因先截最近 300 条任意题材方向，再按目标题材匹配；301 条较新的无关消息会遮蔽六小时窗内较早的真实匹配事件。现先在 SQL 按原有名称互含、长度及 Python 去空白口径筛目标题材，再限 300 行；方向与最新解释版本仍在同一查询读取，原状态/待复核守卫、最终去重、告警阈值和通知路径不变。定向 **17 passed**，本机全后端 **4335 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。隔离合成 10,001 张卡查询约 16–19 ms，仅是本机样本；代码分支已清理。匹配消息不等于已证实先于走势启动，本机旧库仍无目标事件/水位表，未做真实缺口探测或生产加载，IMP-048 仍为 `进行中`。
- **已发布第十七片（2026-09-25）**：PR #148 准确 HEAD `ad04293baae68325ffeed1d98c8e72e1fbcc7bd0` 合并为 `d830bff9e055d5dc04f7d21ea765da0c41a49aea`；作者 `DegradedRelease` 回执 `5818084920` 绑定该 HEAD，临合并 `release_check.py exit 0`，PR CI `36027413973` 三项成功，合并后 master CI `36027998484` 三项成功。修前注入活跃事件库读取异常，持久化精选卡的消息依据仍写“无活跃事件命中”；现在卡片标明读取失败，50 分仅为既有计算占位，持久化元数据区分 `unavailable` 与成功读取空集的 `available`。查询次数、评分权重、入选和风险门不变。定向 **24 passed**、本机全后端 **4337 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。Jev Review 仅为作者反馈；没有运行库加载或生产验收，IMP-048 仍为 `进行中`。
- **已发布第十八片（2026-09-25）**：PR #150 准确 HEAD `1299c3a2cb975a05932503896d52deba37995b13` 合并为 `76b877746ca1a9a5aa8e0969f3a5ae27a4edd13b`；作者 `DegradedRelease` 回执 `5831604260` 绑定该 HEAD，临合并 `release_check.py exit 0`，PR CI `36078936585` 三项成功，合并后 master CI `36129397673` 三项成功。修前注入旧事件行的无效来源等级，消息索引构建失败后持久化精选仍标 `available` 并写“无活跃事件命中”；现在保留 `unavailable/failure_stage=index` 和真实读取条数，卡片明确索引失败，50 分仅为既有计算占位。评分权重、排序、候选和风险门不变。定向精选/事件测试通过，本机后端完整复验 **4338 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。首次全量在严重内存压力下出现一条无关定时用例失败；该例单独与完整重跑均通过，未放宽测试。未加载生产实例，IMP-048 仍为 `进行中`。
- **第十九片（2026-09-25；发布证据见 PR #154）**：U49 隔离库反例先判红：10:00 的板块告警会把 10:01 才发布或才形成解释版本的消息写成“可能诱因”；归因窗口此前仅有六小时前的下界。现在用检测拍的北京时间作为查询上界，排除该拍之后的发布时间与解释可见时间；无版本旧卡仍以 `unknown` 保留，不伪造可见时点。文案改为“相关消息（未核走势启动先后）”，避免把同题匹配说成已证实原因。板块触发、封板序列、通知和评分未改变。本机定向 **19 passed**、全后端 **4340 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生及公开仓扫描通过。此片仅限定告警拍的证据，不证明消息早于走势启动；未加载生产实例，IMP-048 仍为 `进行中`。
- **第二十片（2026-09-25；发布证据见 PR #155）**：U49 隔离反例先判红：盘前简报在事件列表前一行已累加分数/版本引用、后一旧数据行来源等级无效时，虽归档“事件读取失败”，前一行仍以部分事实进入方向排序。现把本批事件强度、利好/利空计数、标的与解释版本引用先写入局部结果，整批处理成功后才一并交给简报；失败保持原有 `missing` 说明且四类事件证据均为空。成功批次的计分、排序、引用与涨停池证据不变。定向盘前简报 **28 passed**、全后端 **4341 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。未加载生产实例，IMP-048 仍为 `进行中`。
- **第二十一片（2026-09-25；发布证据见 PR #156）**：U49 隔离库反例先判红：10:00 的共用活跃事件列表会返回 10:01 才发布的卡片，以及 10:01 才生效的最新解释版本；前者还被 `is_active` 视为活跃。现于 SQL 限额前要求发布时间不晚于查询时点，且最新解释已生效；未来最新版本暂不进入活跃列表，避免把当前方向绑定到尚未可见的版本。无版本旧卡继续保持 `unknown`，`active_only=False` 的审计读取仍可见原记录。精选与盘前简报沿用该共用入口，评分、排序和通知规则未变。定向事件及直接消费者 **115 passed**、本机全后端 **4342 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。未加载生产实例，IMP-048 仍为 `进行中`。
- **第二十二片（2026-09-26；发布证据见 PR #157）**：U49 隔离 API 反例先判红：未来发布时间的卡虽从活跃列表消失，按 ID 读取的详情仍显示“已判定”的利好方向，标的池也继续返回结果；未来才生效的最新解释有相同漏口。现使详情与标的池共用发布时间、解释版本可见时点判断；尚未可见时方向和判定暂列 `unknown`、标的池暂停，原始解释版本仍留在详情供审计，到达可见分钟后恢复正常。标的池判断和序列化使用同一拍时间，避免边界跨秒混态；无版本旧卡不伪造解释时间。定向事件及直接消费者 **116 passed**；本机全后端首次 **4342 passed / 80 skipped / 1 failed**（无关调度器短时探针），单例通过且同树完整重跑 **4343 passed / 80 skipped / 0 failed**；前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。未加载生产实例，IMP-048 仍为 `进行中`。
- **第二十三片（2026-09-26；发布证据见 PR #158）**：U49 定向反例先判红：精选候选池先收下前一条事件标的，后一条旧事件方向读取损坏时虽记录事件来源失败，前一条仍以部分事实进入候选；独立涨停来源正常。现将事件来源的全部标的先在局部集合展开，整批成功后才并入候选；失败丢弃本批事件候选，其他来源及成功批次顺序不变。未改变计分、排序、通知或交易规则。定向精选 **26 passed**、本机全后端 **4344 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。本片未加载生产实例，IMP-048 仍为 `进行中`。
- **第二十四片（2026-09-26；发布证据见 PR #159）**：U49 隔离反例先判红：事件发布时间已到、最新解释要 10 分钟后才生效时，LLM 辅助层仍把该事件选入模型候选。现于 SQL 限额前要求最新解释已到生效时点；无版本旧卡保持原路径，生效后该事件重新入选。定向 LLM/事件 **67 passed**、本机全后端 **4345 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。没有实际调用模型或调整阈值，未加载生产实例，IMP-048 仍为 `进行中`。
- **第二十五片（2026-09-26；发布证据见 PR #160）**：U49 隔离反例先判红：5 条活跃事件中一条旧来源等级无效，消息索引失败且结果标为 `unavailable`，但 5 只事件标的已进入持久候选、业绩标题仍影响炒作阶段。现于候选装配前建立整批事件索引；只有同批索引成功才向候选、阶段判断和消息分数供给事件。失败保留真实读取数与 `failure_stage=index`，独立涨停候选仍可用；成功批次顺序、单次取数和原计分规则不变。定向精选 **27 passed**，首次全量在测试补强后被人为中断，两条 DuckDB 查询报 `Query interrupted`，不计验收；最终固定代码重跑 **4346 passed / 80 skipped / 0 failed**。前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。Jev Review 仅作作者反馈，没有据评分扩大改动；未加载生产实例，IMP-048 仍为 `进行中`。
- **第二十六片（2026-09-26；发布证据见 PR #161）**：U49 隔离反例先判红：来源 09:00 发布、当前解释 10:10 才可见时，旧核验把 09:45 封板算为“事件后新涨停”，且 30 分钟窗口从来源发布起算；跨日边界还可能把解释可见日之前的封板算在之后。现用来源发布与当前解释可见的较晚时点计算窗口和封板先后；旧卡缺版本/可见时间保持 `unknown`，不倒填历史。API 说明明确同题时序不证明事件因果，未改变板块资金口径、评分、通知或交易。定向事件 **61 passed**、本机全后端 **4349 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生与公开仓扫描通过。代码提交 `4ff999c9d689d09c9321e0eeb7bcbaf2454d878a`；未迁移本机旧库或加载生产实例，IMP-048 仍为 `进行中`。
- **第二十七片（2026-09-26；发布证据见 PR #162）**：助手资讯工具消费活跃事件时，原输出只有方向与依据，没有当前解释版本；旧卡的方向无法追溯，却与版本化结论同样展示。定向测试先判红。现逐事件显示解释版本、可见时点和状态；旧卡或不完整引用明确未知，仍保留来源/发布时间/方向依据。进一步用 30 条事件复现 1500 字截断抹掉末尾推断与非买卖建议说明，将该口径前置并锁定回归。没有新增模型调用、事件读取或交易动作。定向测试通过，最终代码本机全后端 **4349 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。代码提交 `2fc77dd7dff4d4fe1c61a038993ebbc6b1f05d1d`；未加载生产实例，IMP-048 仍为 `进行中`。
- **第二十八片（2026-09-26；发布证据待本轮 PR）**：U49 隔离反例先判红：来源 11:00 才发布，解释版本 10:00 已写入，10:30 的时点回放仍返回该版本。`EventStore.interpretation_at` 现于同一 SQL 同时要求来源发布时间和解释生效时点均不晚于查询时点；11:00 到达后仍按原解释版本返回，不改原始审计读取、评分或通知。定向事件/核验 **62 passed**、本机全后端 **4350 passed / 80 skipped / 0 failed**，前端常规/UTC 各 **717 passed**，全量 pyflakes、tsc、eslint、Next build、文档体检、卫生和公开仓扫描通过。隔离旧库副本从 Alembic `b5c9e7a2d4f1` 升至 `a4e8c2d9f6b1`，重复升级与 `quick_check` 通过；原 **11101** 张事件卡可读，抽样旧卡解释版本保持 `unknown`，无历史观察/解释倒填。源库未写入，副本已清理；该旧库缺新版表且含未来时点，不能代表真实水位与修订样本。代码提交 `3c0da121d7adb7a81e38e9f55cd8b99dcc689707`；IMP-048 仍为 `进行中`。
- **下一步**：继续核其他实际持久决策消费者的事件版本、可见时点及部分失败语义；精选事件读取或索引不可用时，现有 50 分虽明确标为占位仍参与排序，改变排名须另做业务门与回放评估。旧来源无稳定关联时保留未知；超过 100 页的来源缺口须在具备准确水位的目标库只读探测，不因页限或空页宣称完整。板块异动若要称“诱因”，还需有走势启动时点与消息可见时点的先后证据。部署搁置期间不把本机旧库或新代码视为运行验收；Jev 候选仍需冻结样本和独立人工对照。
- **实施步骤**：①明确 raw observation、事件归并、解释版本三种身份；②来源/修订追加、保留原文引用与哈希，不能依标题去重丢原始证据；③绑定多标的方向、依据、置信/未知及有效期；④按 available_at 做回放；⑤快讯水位与断档补采幂等；⑥将变化原因接入现有详情/复盘，发送仍须 IMP-044 的渠道策略。
- **发布前置**：仅观察/查询的切片不等待通知平台全完成；外发必须复用可靠投递与既有用户偏好；策略入模另经 RSH-026/IMP-020。
- **恢复**：追加式迁移先扩展/对照/切换，旧事件可读，撤回/更正不删除历史；不新建图数据库或第二套新闻平台。
- **分工**：ChatGPT 已界定缺口与消费者范围；Codex 完成复现、接口/存储设计、迁移及跨模块回归。
- **因果边界**：事实/竞争假设/反证分栏；消息首发、系统可见和价格启动先后明确。新闻标题、原文、标的池、主题跳转、L1/类别/标签与排序逐项验证；未找到新闻不等于无催化，涨后解释不算提前发现。


## 已交付基线

- 既有 execution gate、decision ledger、页面读时重检和 RSH-026 决策归档作为基线；不重复建立。

旧编号、退出理由和原文恢复入口见 [历史处置表](../archive/ledger-transition-20260917.md)。本节只留仍支撑本阶段的成果，不保存逐轮长日志。
